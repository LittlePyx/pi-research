# 生产验收记录 · 2026-09-17

本轮核对线上 v243 与本地 main；不修改用户阅读状态，不触发人工扫描，不恢复旧 Codex 自动任务，邮件接入继续暂停。

## 已核实

- Sites 当前版本为 243，线上地址为 https://pi-research-agent.qiudao-pika.chatgpt.site/。
- 量子方向空间 `5a9c7342-6a85-4952-9842-65e6069e1397` 的期刊配置为 Quantum Science and Technology、Optics Letters、Physical Review A、Metrologia、Optics Express、Physical Review Applied。该空间的简报内容与交接中的量子时钟同步测试空间一致。
- 该空间 2026-09-16 简报累计保留 5 篇推荐。记录的末轮指标为 56 篇初筛、5 篇深评、0 篇新增推荐；累计推荐不能解释为末轮新增，也不是 9 月 17 日的每日目标完成数。
- 简报覆盖同步安全、深空量子链路、同步综述、时间传递以及弱测量。最后一项属于量子计量相关材料，不能仅凭推荐摘要认定它直接解决时钟同步；本轮未取得其完整评审依据，尚未通过逐篇相关性验收。
- 量子空间库内论文 `17cdc1f1-c7a6-4f3e-af1d-89c9cb62f219` 已有后台引用查询记录，2026-09-17 04:23 UTC 状态为 partial，返回了频率估计、量子传感、光钟等参考文献。工具截断了关系 JSON，不能报告完整关系数量。这证明引用维护有产出，不证明 v243 新增的一跳发现已完成入库、深评和推荐。
- 该空间最新 monitor_runs 为 ready；下一次调度日期为 2026-09-17 09:31:12 UTC（北京时间 17:31）。核对时尚未到期。v241 没有提前改写历史调度日期，因此不能用当前快照判断每日补检索是否成功。
- 已完整读取 learning_paths 与 learning_plan_previews 的工具分页，两表都没有该空间记录。没有自动创建目标或替用户确认学习计划。
- 该空间 research_maintenance 最近一轮为 route/idle、routeCursor 为空。源码中它表示没有选出可处理的路线材料；不能单凭此记录区分“没有活跃路线”和“没有到期材料”。数据库工具未返回 research_tracks 表名，未猜表名或绕过限制读取。
- 其他空间确有后台引用整理成功记录，包括一项返回 63 条关系；同时也存在 graph/error 和 route/retryable。不能据局部成功宣称后台整理全部正常。

## 未通过的验收项

1. **真实页面交互与移动端视觉**：CUA getState 返回 nodeRepl.fetch request failed，无可用浏览器。未执行点击、导航、截图或手机视口验收。
2. **每日目标与一跳发现闭环**：发布后自然调度尚未为本空间提供可核实的完整新轮次。本轮不把历史深评计入当天，也不强行制造通过记录。
3. **推荐到研究/学习的连续流程**：没有量子空间学习计划，未验证从推荐选择研究方向、生成路线、确认学习目标到完成阅读任务的真实流程。
4. **路线归类重试根因**：几个空间出现 retryable；当前 catch 只保留通用状态，不能从现有记录断言是模型、数据或运行时问题。需要有分类且不含密钥/论文全文的服务端诊断。

## 下一步实现与验收顺序

先补齐真实用户的研究启动流程：从已有合格论文提出少量有材料支撑的方向建议，展示支持论文和适用范围，由用户选择或编辑目标；确认后再生成路线/学习计划，后台接续材料整理。不把引用相关、主题相近自动升级为正式路线证据，不替用户确认目标或标记已读。

随后验证自然调度的每日深评去重计数、补检索预算、近期回首页和一跳来源解释。后台异常保留结构化诊断供维护使用，用户主界面仍以可读成果为主。

上述是尚待完成的验收与设计，不是本轮已上线的新功能。此轮仅做只读核对和记录，未修改或重新部署业务代码。
