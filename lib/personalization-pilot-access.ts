// Closed after the local network was blocked before experiment execution.
// Reopening requires a new short-lived capability and an authorized execution session.
export const PILOT_ACCESS_HASH = '';
export const PILOT_EXPIRES_AT = 0;
