import {getDatabase} from '../../../../db/repository';
function token(request:Request){return new URL(request.url).searchParams.get('token')||'';}
export async function GET(request:Request){const t=token(request);if(!/^[a-f0-9-]{72}$/.test(t))return new Response('Invalid link',{status:400});return new Response('<!doctype html><html lang="zh"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>退订 Pi Research 简报</title><main style="max-width:560px;margin:15vh auto;padding:24px;font:18px/1.7 system-ui"><h1>退订每日邮件简报</h1><p>退订只停止此研究空间的邮件，不会删除论文或笔记。</p><form method="post"><button style="padding:12px 24px;font:inherit">确认退订 / Unsubscribe</button></form></main></html>',{headers:{'Content-Type':'text/html; charset=utf-8','Referrer-Policy':'no-referrer','Cache-Control':'no-store'}});}
export async function POST(request:Request){const t=token(request);if(!/^[a-f0-9-]{72}$/.test(t))return new Response('Invalid link',{status:400});await getDatabase().prepare('UPDATE email_subscriptions SET enabled=0,updated_at=CURRENT_TIMESTAMP WHERE unsubscribe_token=?').bind(t).run();return new Response('已退订。你可以在 Pi Research 的邮件订阅设置中重新开启。 Unsubscribed.',{headers:{'Content-Type':'text/plain; charset=utf-8','Cache-Control':'no-store'}});}
