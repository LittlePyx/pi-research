# Pi Research 优化与推进实施计划

更新时间：2026-09-10
前端整理起始基线：`main@7dafac2`（v177）

本次复核基线：`main@337cd4c`（v193）；原未跟踪 `docs/` 保留、不纳入本次修改。

本计划由前端整理扩展为产品整体推进计划，继续使用同一份文档。下方历史阶段与当时证据原样保留；历史段落中的“下一项”和优先级，以本节最新排序为准。发布完成、代码测试通过、真实业务验收完成分别记录，不能互相替代。

## 目标

近期产品目标：让已发现的论文能够可靠地进入质量评估，让合格证据支持研究路线和学习步骤，再让真实阅读反馈指导下一轮发现。衡量推进是否有效，主要看用户能否找到值得读的论文、理解当前研究问题并完成下一步；候选量、发布次数和新增页面数只作辅助记录。

让每个页面只回答一个主要问题，并把运行状态、审计信息和低频设置放到次级层级。保留现有研究能力、状态语义、论文历史和数据，不以删功能代替界面整理。

**用户使用顺手、理解无歧义，优先于视觉效果。** 先保证能完成任务、知道当前状态与操作后果，再改善可读性，最后处理装饰。克制配色和直角标签是设计约束，不是完成可用性验收的替代品。

每项界面修改都必须回答：用户要完成什么、现在卡在哪里、改后少了哪一步或哪一种误解。不能只以“更高级”“更统一”作为实施依据；重要状态和必要解释不能为精简而删除。

## 不变约束

- 今日只展示通过共享质量评估的推荐。
- 研究路线、缺口、引用网络、综合发现与学习路径仍共用同一质量队列。
- 不隐藏来源降级、证据不足和待确认状态，但不让运行审计抢占主要工作区。
- 重扫、界面改版和筛选切换不得删除历史论文、反馈或阅读记录。
- 数据库引用、相似关系、Pi 推断和阅读顺序继续明确区分。
- 桌面与窄屏使用同一计数和状态来源。
- 只使用摘要和可获得的结构化证据，不引入全文核验；书目身份确认不能代替质量通过。
- 背景质量核对自动进行，不要求用户确认、不新增用户质量审计台；正式路线证据仍须用户确认。
- 用户在网页配置模型 Key；不读取、打印、保存或提交 Key。隔离验收不得代替用户做阅读、反馈和正式路线确认。
- 开发阶段不恢复总模型额度或累计重试上限；技术超时、单次任务边界、持久化租约、来源退避及最终质量门槛继续保留。以下工期是实施时间盒，不是产品费用上限。

## 当前优先级与调整规则（2026-09-07）

### 2026-09-10 两篇修订与原8篇交付继续核对（进行中）

- 本轮按用户再次确认继续。main@8825e8d、线上v210、public均核实；完整重读交接。05:38只读确认两篇在05:34修订轮后仍pending，无新正式推荐。
- computer-use生产页面核验遇连接失败：第一次连接失败、重置后选中既有in-app browser且空标签列表；新建隐藏生产标签调用30秒超时，随后inventory仍失败，未取得页面或点击任何扫描/重扫/阅读动作。**D1 last_user_activity_at=05:40:44证明页面请求确实到达**，访问补偿已经发生；后续不能一概说纯Cron。未知标签创建结果不重复开新标签，不复制Cookie/Key、不绕过旧受阻入口。
- 05:44只读原9e4bab7f确认已深评5/8、剩余3篇。Hamilton–Jacobi和Sandwich Slice均verificationDeferred=true，属于保留草稿待后续核对，不算质量拒绝。新增doi:10.3934/mine.2023083（paperId=79542fde-0670-4dbd-b99e-396ea0e41d5a）摘要318字符、相关55/质量65、not_required、未推荐：理由为奇异结构/点质量最优传输与KLS、对数凹测度及函数不等式方向缺乏直接联系。新增Optimal mean width and metric entropy estimates for convex bodies（paperId=f87edfd1-dc98-49c4-9280-b4c89ac9095e，title:366017…）摘要686字符、相关85/质量88、pending/未发布，也已被列入verificationDeferred。3篇待核对、0正式推荐，不能将延后算拒稿。
- 为区分正常内容修订、传输失败及终态证据不足，补充现有已保存诊断：队列记录逐篇verificationAttempt；结果记录correctionRequested/correctionCompleted/限长已保存verificationReason；可靠性事件仅在deep_reviewing/verifying_recommendations记录固定字段与错误码白名单、尝试次数，不记录原始错误/响应/Key/记忆。没有改模型、门槛、超时、批量、调度或重试规则。新增1项回归通过，构建及572项、离线基准已通过，完整lint仍在运行；发布待后续记录。

### 2026-09-10 后续核对身份修复与8篇实际结果（进行中）

- **05:24原任务第一轮独立核对已保存**：05:23自然external_watchdog运行后，同一两篇的review_result_saved分别为Hamilton–Jacobi coverage90、Sandwich Slice coverage92，均verificationStatus=pending、verificationRetryable=true、published=false；D1于05:24:03仍verifying_recommendations，最终核对完成0/2、正式推荐0。它们从原coverage0推进到实际初核结果；结合verifyRecommendationBatch初核分支，这是需要保守修订的状态，不是终态质量通过，也不把coverage超过80当完成。既有修订流程继续等待自然名额，无人工唤醒。8篇总体仍3已深评/5未深评/0延后，其中2待最终核对。下表05:14作为历史首次逐篇快照保留，输出ledger已更新05:24的分数。

- **05:14生产逐篇进展**：同一9e4bab7f、attempt14在05:13外部调度下使用v210自然推进。队列日志完整取得8个身份：3篇deepCompleted=true、0延后、5篇未深评；其中2篇verificationQueued=true，均未完成核对。D1于05:14:06进入verifying_recommendations，正式推荐0。这证明新日志在生产可用及任务继续消费，尚不证明本批独立核对或8篇交付完成。快照outputs/review-9e4bab7f-ledger.json。

| 原8篇身份/题名 | 05:14 UTC确切状态 |
| --- | --- |
| doi:10.1093/imanum/drag006 | 深评已完成、未入核对、未正式推荐；该篇在v210前完成，具体分数/原因尚未独立取得 |
| doi:10.1016/j.jfa.2026.111388 — Hamilton–Jacobi equations for controlled gradient flows: cylindrical test functions | 摘要854字符；相关78/质量75；草稿已保存、独立核对pending、未发布；paperId=8aedbebb-ef09-4629-a2dd-49bcc66c2c09 |
| title:44ac7934a1fbd997dba25368b5acea96841fcf0239a64e450d5cd46a044567c6 — Sandwich Slice: Optimal Transport Potentials Are Optimal Generalized Slicers | 摘要1176字符；相关78/质量85；草稿已保存、独立核对pending、未发布；paperId=de8427aa-e517-41cf-94f5-3f1fe670f2e0 |
| title:3660179005e1ff723d92c2292d85c5dca3fffd4a59fffd375f98c36456a99b68 — Optimal mean width and metric entropy estimates for convex bodies | 尚未深评、未延后、未入核对 |
| doi:10.3934/mine.2023083 | 尚未深评、未延后、未入核对 |
| title:0dd2e141a0a91f8f9b5f86a47722e8c9cd6ea50ad8decc4a3846a6f6ef2f84da — Space-Time Log-Sobolev Inequality and Hypocoercive Hypercontractivity for Underdamped Langevin Dynamics | 尚未深评、未延后、未入核对 |
| title:6087dea65e84a06ca91801669cc828b91dd921a91ee1600ec80045aceee60cbc — Mirror Langevin diffusions: Convergence rates and Markov chain approximations | 尚未深评、未延后、未入核对 |
| title:111fd1f7bca2308af5c6499e42c89b29bf74844c9ebb4d7ef40c6ee0aa152eae — Improved Analysis for Hessian-free High-resolution Monte Carlo Sampling | 尚未深评、未延后、未入核对 |

- 两个DOI的题名由出版社页面独立核对：drag006为[On discrete Sobolev inequalities for nonconforming finite elements under a semiregular mesh condition](https://academic.oup.com/imajna/advance-article-abstract/doi/10.1093/imanum/drag006/8708288)，mine.2023083为[Singular structures in solutions to the Monge-Ampère equation with point masses](https://aimspress.com/article/doi/10.3934/mine.2023083)。仅作书目识别，不当作D1摘要补齐、质量通过或真实推荐证据；没有读取全文。

- **v210已发布**：2026-09-10 05:07:10 UTC成功，源码9b94224a7d068896b116659b5d126e40d3ed6a40已推送GitHub与Sites绑定main；版本appgprj_6a83f86ecca081919f3094b285bc2b1d~appgver_9d4371ce999081918b02c2a9c0b9743c，部署appgdep_6aa23af2d47c81918d8c9d979627b248。public/环境修订6/DB不变，无迁移。CI34439733019已成功。原任务下一次自然推进与逐篇验收仍待取得，不能写成8篇已完成。

- 用户确认按当前最高优先级执行。完整重读交接与README、核对当前计划；main@7c540e0仅原docs/未跟踪，线上仍public v209。未恢复旧Codex自动任务、未人工推进/重扫/清退避。
- 04:59只读原任务9e4bab7f确认04:53已完成摘要补证阶段，checkpoint=deep_reviewing，显示8篇证据充分候选；05:04:16更新为“已处理1/8篇”，0正式推荐、0待核对草稿。processed包含完成和延后，不能仅凭此计数判定第一篇质量拒绝；rejected_count=59也不能当作59篇深评拒稿。
- 修复独立初核、修订核对和非阻塞路线归属对canonicalId的HTML清理，另修深评不完整草稿报错中的身份文本。身份专用整理保留合法尖括号，不解码实体、不猜配未知身份；重复身份不采用。初核/修订缺失或重复身份走原有失败重试，保留待核对草稿，不再误算为证据质量拒绝。路线只采用唯一已知身份、有效路线和完整双语理由，已有归属保留。质量/证据门槛、模型、超时和调度均未变。
- 增加已保存队列逐篇状态和已落库深评结果日志，区分深评完成/延后、核对排队/完成/延后与实际published。只记录书目、摘要字符数、分数、保存状态及限长原因，不记录Key、请求头、记忆、提示词或模型原始响应；记录失败不改变评审。
- 新4项回归覆盖实际生产初核/修订/路线函数中的真实尖括号DOI、实体/删尖括号/缺失/重复身份、证据不足仍拒绝、原路线和输入保留，以及诊断状态/隐私边界。Node24完整lint、生产构建571项测试和离线发现基准通过，0失败0跳过。发布与逐篇生产结果待本节后续补充，不以代码测试或processed计数关闭8篇验收。

### 2026-09-10 用户确认优先解决45/59：请求级证据与针对性恢复

- **04:34 UTC原断点生产验收通过**：同一任务9e4bab7f-285d-4327-a975-e4e15b24289c在既有external_watchdog自然接续，v209 trace=47b2528c-4b49-4995-9dda-213797d0a85b，04:33:45.552开始，第一次请求15,184ms完成；14返回/14匹配/14完成，missingIds、duplicateIds、invalidIds均空、unexpectedCount=0，HTTP200/stop，4677输入与1384输出token。04:34:26只读D1确认reviewed_count从45升至59、rejected_count=50、discovered_count=360、recommended_count=0、error=null；checkpoint=enriching_abstracts/status=deep_reviewing，current_source为“正在为8篇高潜力论文补全摘要证据”。旧45条保留，新14条实际保存，已离开45/59并进入深评准备。未人为唤醒、换任务、清进度或降低门槛；该任务初始visit与本次自然推进仍区分记录。证据保存在outputs/screening-9e4bab7f-recovery.json。
- 本次最高优先级“45/59固定断点”已修复并通过原任务生产验证，不再重复等筛选恢复。后续8篇仅排入深评准备，不能算8篇已深评或已推荐；正式推荐目前0，后续真实深评、独立核对及双领域基础交付继续按原计划验收。其他canonicalId清理风险见本节审计记录，未冒称全链路已关闭。
- **04:16确定性根因复现**：该批最后一篇canonicalId为doi:10.1130/0091-7613(1990)018<0812:lbotao>2.3.co;2。旧quickScreenBatch使用cleanText（HTML正文清理）规范模型canonicalId，正则/<[^>]*>/删除合法DOI片段<0812:lbotao>，造成自身身份匹配失败。用git中v208实际函数与v209实际函数、相同14篇完美模拟响应对比（不调用模型/生产）：v208两次调用后仍“did not screen every candidate”、0结果，损坏ID变为018 2.3.co;2；v209一次返回14且原DOI完整。这与生产HTTP200→validate失败证据一致，纠正此前偏重网络超时/模型漏写的判断。原模型输出未记录，故不宣称独立看过它；本地确定性缺陷本身已证实。
- v209已于04:12:15 UTC发布，源码00c2bc8a695e06330ad0f9565046470a2ed15888，版本appgprj_6a83f86ecca081919f3094b285bc2b1d~appgver_bdf15a8a12508191977dc4f7b33b67e3，部署appgdep_6aa22e1465a08191a7ea1e2550a22bfa，public/环境修订6不变，CI34436191627通过。新matcher已采用身份专用空白整理，不再HTML清理；缺失/重复/非法字段仍pending、有效部分保留。随后仅补真实DOI及实际cleanText依赖回归，未变产品构建的完整567项通过。复现脚本outputs/reproduce-screening-doi.mjs仅为隔离证据，不把模拟分数/14篇完成说成真实评审。
- 04:13外部调度只推进一个其他空间，原9e4bab7f最后仍04:04更新、45/59。等下一次该任务获得自然名额，未打开页面、未调用扫描接口、未改调度器或恢复旧Codex自动任务。需按实际trace/持久化筛选及后续深评验证，不能用修复上线关闭生产恢复验收。
- 同类风险审计仅记录、未扩大本批：monitor的两个独立核对结果映射及非阻塞路线归属映射仍对canonicalId使用cleanText，包含尖括号的合格DOI在这些后续路径也应做身份专用处理；深评主byId本身使用原ID，另一次cleanText仅用于报错文本。本批先验收原45/59恢复，不在等待期追加无关发布。真实DOI回归提交6b98b5b的CI34436649151也已通过。
- **04:04 UTC取得原断点关键证据**：9e4bab7f在现有external_watchdog调度下接续，首请求trace=f91905e4-7c5d-4b2a-8cb9-8c6d0370fadd，04:03:45.890开始，14篇/18948字符prompt，14,583ms返回HTTP200、finish_reason=stop、4677输入/1333输出token、4277字符内容，但phase=validate/errorCode=invalid_model_output。该部署代码此阶段对应未找到某个请求canonicalId，整批被抛弃；不是首请求24秒网络超时。第二请求trace=6b3bbda7-03fc-4ef6-b6d0-5bb032e3b513仍发14篇（首错不是timeout，v207缩批分支未生效），12,000ms在headers阶段超时。因此最终timeout掩盖了先前身份匹配失败；不能将这次归因于JSON截断、模型推理过慢或24+12越过waitUntil。具体哪篇缺失/拼错及原始模型内容未记录，不臆断。
- 首请求14篇实际书目与传给模型的摘要已从逐片日志完整复原（9篇有摘要，其中8篇取900字符、1篇686字符；5篇无摘要），全部片数/字符数匹配、truncated=false，保存于未跟踪输出outputs/screening-9e4bab7f-exact-inputs.json。不再把D1长单元格截断作为该批身份未知的理由。输入混有数学候选、暖通论文和1990年地质论文，是否通过筛选仍由原门槛判断，未人工剔除/推荐。
- 针对性修复：fast筛选只匹配请求内唯一身份且基本字段完整的结果，保留全部逐项通过原分数与矛盾校验的记录；缺失/异常/重复身份留原队列，未知身份不猜配、不覆盖，不标拒稿。部分成功直接持久化，不再重发整个14篇；下一轮仍由既有remainingIds选择未评论文。rescue保持完整覆盖要求；诊断增加返回/匹配/未知数量、缺失/重复/无效的请求身份。11项针对性测试通过，包括双领域13+1接续、重复和非法布尔/缺分数、真实HTTP空行超时；Node24完整lint、生产构建和566项测试全部通过。未改模型、超时、总额度、来源退避或最终推荐门槛。
- 用户明确“按你说的做”，首要任务是剩余14篇输入与每次请求耗时，而非继续界面优化。诊断分支codex/screening-request-diagnostics基于main@b7b6471，未纳入引用UI草稿PR#1。旧自动任务不恢复，不人工重扫/清退避。
- 现有快速筛选只在整个组失败后写llm_stage_degraded，无法区分等响应头、读响应体、解析、校验或用量落库。增加结构化请求轨迹并关联真实scanJobId、traceId、尝试次数和批量；记录HTTP状态、各阶段耗时、finish_reason、输出长度与真实token数，无模型内容。失败记录沿用实际传给模型的论文记录逐条分片（每片1200字符，每篇最多16片，超长明确truncated），可取得书目与实际900字摘要输入；不记录Key、请求头、用户记忆或模型原始输出。复用现有可靠性事件，无迁移；日志失败不改变扫描。24/12秒边界、两次请求、缩批规则和最终门槛保持原样，待自然证据再决定修复。
- 针对性7项测试已通过，覆盖headers/body/parse/validate差异、超时无伪筛选、14→7原行为、请求与resume任务绑定、输入复原/超长诚实标记、日志失败隔离和敏感字段排除。Node24完整lint、生产构建、562项测试及离线双领域发现基准均通过；发布和原任务自然取证另记，不以诊断上线宣布恢复成功。
- 03:50 UTC只读核对发现最新active_job_id已为9e4bab7f-285d-4327-a975-e4e15b24289c，run.last_trigger=visit，updated03:35:12；对应上一轮恢复原页面的访问补偿。旧96767的next04:16仍是其历史值，不能继续当作当前运行时间。此次未打开生产页面、未触发扫描，后续跟踪实际9e4bab7f并区分访问起始与自然接续。
- 诊断v208于03:54:22 UTC发布成功，public与环境修订6不变；源码d5376aa90261c6d7b58faf5f527c500862c0d3cb，部署appgdep_6aa229e113b48191826d28a5225a72a0，版本appgprj_6a83f86ecca081919f3094b285bc2b1d~appgver_2465bd19cd588191ade2c8bea13adb12。GitHub CI34434906703成功。首次保存发现GitHub与Sites绑定仓库不同，未生成版本；随后用短期按命令认证快进Sites源码，不保存凭据。最初以dist为归档根不符合入口约定，工具拒绝；正确归档保留dist/server/index.js与根.openai/hosting.json，校验后成功保存371文件。没有重复部署或数据库迁移。
- 发布后本地补真实HTTP200+空行保活测试，两个截止都得到phase=body/httpStatus=200/completed=0，没有伪结果；与headers未返回、模型内容解析失败明确区分。8项针对性与未变业务构建的完整563项通过。现有外部调度实读02:23–03:53每十分钟正常运行；03:53发生在v208上线前。当前9e4bab7f尚45/59，待下一次自然推进的确切trace，未人工触发，未恢复旧Codex自动任务。

### 2026-09-10 新会话接续：原任务核对与独立引用清单批次

- **03:30 UTC收口补充（优先于本节较早快照）**：v207恢复任务96767cfd-4d78-4ec7-804e-c9de1fa26c6c于03:13:46自然领取，attempt13、resume_of=d2c8a1ee。03:14:16生产页面访问前的第一轮仍45/59。03:30:07取得最终结果：03:16:38.924 timeout、retry_pending，failure_source为DeepSeek Pro已快速筛选45/59；360候选/45筛选/41拒绝/0正式推荐，深评未知，next04:16:38.924 UTC。v207这次未突破，不能用领取或候选量宣布成功。后续正常生产访问可能触发visit_backstop，终态不全部归于纯Cron；未点击扫描/重扫。下一P1是取得最后14篇确切输入与分请求耗时，保持质量门槛和技术边界，不能无证据继续改超时。
- 浏览器随后恢复，引用清单200%文字验收完成：实际组件390px有效宽375/scrollWidth375，标题24px、来源30px且完整换行，无横向溢出、来源transform=none、同快照2条；700px英文有效宽685/scrollWidth685、两端选择正确。夹具使用font-size important覆盖产品主题的important，避免假放大。实际截图已目视，无标题/来源重叠。业务提交bad7c66 CI34431959257及交接1a8634e CI34432216737成功；本地完整562项绿色仍适用于未变产品代码。草稿PR#1：https://github.com/LittlePyx/pi-research/pull/1 ，未部署。
- 生产交互子项通过但交付总项未关闭：原主数学KLS方法材料doi:10.4007/annals.2024.199.3.2打开详情再返回，方法阶段保留，桌面与390px均17可用/3评估、路径2篇、0/5完成；既有Yilin信息论高斯极值路径方法材料doi:10.1109/tit.2022.3194725同样通过，18可用/21评估、路径4篇、0/5完成。两篇未加入阅读，未代用户接受/完成。Yilin路径并非指定60bfce13，不能代替后者基础交付验收。基础材料两端仍为空；生产KaTeX只读到DOM，不冒称完成公式目视验收。
- 已恢复正常viewport和原主数学空间，关闭本轮创建的生产/QA标签，停止8113预览。旧自动任务继续停用；未部署、未新建空间、未复制凭据、未改用户阅读或确认状态。原P1仍需确切筛选输入及指定信息论路径材料，整体7–10日周期未完成。
- 收尾补查近30分钟Worker错误日志：03:16:40.276 UTC，POST /api/monitor返回502，wallTimeMs=30946，请求5bd132cc74bd1b445c4487ccccf32759；与任务03:16:38终态时间接近，但日志无job ID/剩余论文/上游分段耗时，不能仅凭邻近时间确定请求归属或模型超时根因。另03:35:27正常返回原空间期间同路由请求canceled、22443ms，属于浏览器验收时段，不作为独立Cron失败样本。只保存必要审计字段，不保存请求头或身份数据。v207原部署再次核实succeeded、环境修订6不变。
- 已按用户本轮要求完整阅读 SESSION_HANDOFF.md，并核对 README、当前周期和最新落实；旧 Codex 自动任务不恢复、不新建。本地接续 main@b7b6471，仅用户原有 docs/ 未跟踪。Sites 现行 v207、源码5016a4d、部署成功、public范围与环境修订6一致。
- GitHub main 实查落后32个提交（43c8a29）；已快进同步到b7b6471，未强推、未触发生产扫描或发布，代码CI 34431072569正在运行。旧09-08的绿色检查不代表v207已获远端验证。
- 02:48–02:52 UTC只读分页核对216条扫描任务，最新主数学失败任务为d2c8a1ee-35de-4464-b885-b3deb57436c4，attempt12，resume指向1aeabc38，02:04:18超时，360候选/45筛选/0正式推荐；深评及剩余输入因work_queue_json截断仍未知。下次03:04:18 UTC，尚未到期，不能判定v207恢复失败，也不人工唤醒。完整resume链仍保留。
- 31条学习步骤、2条阶段review核对：原数学附件的ID/stageKey/quote仍一致且unread，信息论原基础及主数学KLS基础仍resources=[]、未完成。当前会话内置浏览器无既有标签，浏览器总清单连接失败；未以新空间或旧夹具替代生产验收。
- 独立批次沿用17C/15B-1（P2），只在上述自然重试等待时推进：现代码仍把Semantic Scholar嵌入25px箭头列、标题单行截断、窄屏旋转来源文字。用户影响是不能辨认两端论文；实施为独立来源行、明确引用角色、标题换行和键盘可选，保留既有onSelect与数据库source/target方向。成本为局部组件/CSS及隔离浏览器回归，无数据库/质量/调度变化。验收为同快照桌面、390px及200%文本放大、完整标题/来源无重叠、两端选择正确、引用计数不变。P1到期后回到原任务；后续泛化美化继续后置。
- P1逐项取证补充：当前信息论验收基础步骤2b830bbf实际挂接bd4bab8b，命中的是Costa原文doi:10.1109/tit.1985.1057105，不是旧04948d10空间的Shannon任务。bd4bab8b于09-09 21:13执行至attempt6/queued6、共享基线0、OpenAlex退避，下一次09-10 09:13 UTC。主数学KLS任务6786d1bf为attempt7/queued6，下一次07:03 UTC；旧Shannon任务e9934cd0为attempt8/queued5，下一次03:13 UTC。以上累计queued都不是唯一论文数或质量通过数。按真实space/step/job/DOI分别保留，未重扫、清退避或替换阶段材料。
- 逐篇评估读取边界更明确：paper_insights按paper_id排序，连接器由工作区限制offset最多10000；该边界实读主键85832907，已知数学附件feafc02f超出可达范围。未扫描全论文表、未绕过偏移限制，也没有因此推断该附件现行深评字段或完整摘要；持久化资源/review的字段一致性与当前paper_insights资格复核仍分别记账。
- 17C落实在codex/citation-ledger-readability：来源移到独立次级行，标题完整换行，明确“被引论文/引用它的论文”，按数据库端点选择且保留倒序年份，使用分隔行和中性色；仅箭头在窄屏旋转。实际生产组件及其原过滤/映射函数通过AST载入隔离浏览器，不手写替代清单，不连接API/Worker/凭据。旧版英文标题scrollWidth976>clientWidth514且来源列25px已复现。
- 1440桌面与390×844窄屏实际截图/DOM验证同为2条引用，Pi推断边排除，标题与来源无横向溢出；窄屏有效页面宽375、scrollWidth375，按钮宽299。鼠标选择citing与键盘Shift+Tab/Enter返回cited均对应正确焦点，2px键盘轮廓可见；来源transform=none。英文700px结构已读取，200%文字压力入口随后浏览器执行超时并失联，尚未取得200%或生产页面结果，也未把工作台焦点选择冒充生产论文详情返回验收。
- 本地Node24完整lint、生产构建通过。首轮562项中561通过，唯一失败是旧“被引工作→后续论文”文案断言；更新为明确角色断言，并以新实际组件两端ID/年份逆序/来源独立性测试覆盖语义后，完整562/562通过，最终改动测试/夹具单独lint通过。没有质量/发现/调度改动，本批未重复调用实时来源。GitHub原b7b6471的CI34431072569于02:54:08成功，新分支CI另记，不冒充同一提交。此次尚未部署，线上仍v207；200%与生产双领域读取受浏览器连接限制，P1未关闭。

### 2026-09-10 用户要求交接并暂停

已将旧Codex自动任务pi-research设为PAUSED，并复核持久化状态；未暂停网站自身扫描或定时器。最新接续入口为根目录SESSION_HANDOFF.md，旧docs/保持原样。新任务接手前不再自动开发/发布，不自动恢复旧任务；v207恢复效果和双领域完整交付仍待验证。

### 2026-09-08 用户确认的完整周期（7–10 个工作日估计）

1. 后台可靠性收口（1–2 天，含至少 24 小时自然观察）：修复已复现调用失败，核对六空间资格、领取、同任务进度与历史保留；至少三次成功自然调用，不以部署/人工触发替代。
2. 研究与学习材料闭环（2–3 天）：逐篇追踪既有 Shannon/KLS 候选的摘要、筛选、深评、质量结果、阶段匹配；原文不足时仅用合格的明确标注的桥接材料；两领域打开材料→返回原阶段可用。
3. 逐页可用性整顿（2 天）：今日→路线→学习→详情→论文库，优先动作、状态、计数和返回，再做引用布局、冗余文案与克制视觉。桌面/窄屏同快照计数和操作验收。
4. 个性化与整体验收（1–2 天）：隔离空间验证反馈对下一轮发现的实际影响，双领域回归、跨重扫保留、恢复与性能检查，按证据决定发布。

- 本周期按完整阶段交付，不再以几分钟的小修改作为完成。内部连续执行定位、实现与验证，阶段结束统一记录结果、遗留风险及改序；只在新权限、用户研究判断或真实外部阻塞时交还用户。
- 自然观察与来源等待不占住独立工作；自动观察任务负责后续采样，不声称对话结束后会在未安排任务的情况下持续开发。阶段 1 当前实施中，其余阶段未完成；真实阅读/路线确认不代用户执行。
- 紧急可靠性修复可单独上线，其余按完整验证批次发布。用户未跟踪 docs/ 保留；不扩大图谱、来源或架构，不降低门槛。

### 2026-09-08 实测后改序（本节优先于下方历史排序）

#### 阶段2实施包：快速筛选超时后的缩批接续（2026-09-10）

- 发布落实：最终完整lint通过；v207于02:38:19 UTC按原public范围发布成功，源码5016a4d937bc05350f5cdf7687dc598fa3ee90a9，部署appgdep_6aa2180e26b88191814130d0ac20ffa3，环境修订6。原Sites打包脚本已从本机移除，使用系统tar仅归档已验证dist并核对入口/manifest/迁移；上传首次网络超时，查版本确认未新增后同包重试成功，无重复部署。后台任务不打开产品页面，避免污染自然恢复验收。上线不代表原45/59已经突破，下一轮按确切任务检查。
- 针对反复相同45/59断点，落实小范围恢复策略：快速筛选首次超时后，既有第二次请求只处理原批次前半；成功结果仍经原完整字段/分数矛盾校验并按ID持久化，后半不被拒绝、不被标为已筛选，由原remainingIds下一轮接续。初次正常请求、非超时错误及rescue临界复审仍全批处理。没有增加单轮请求次数、延长24/12秒截止、放宽质量门槛或清空历史；最终质量评估仍是原共享队列。
- 这是已知失败路径的负载收缩改进，不宣称已证明网络/模型的底层超时原因。新增缩批期望在旧实现失败（14≠7），改后通过；另验证缩批成功只返回实评ID，剩余下一轮覆盖、原输入不变，rescue和非超时不缩批。保留此前双领域部分成功/致命错误回归。Node24完整构建与559项测试通过，来源检查及实时双领域发现通过；最终lint和发布结果另记。来源3健康，不能把来源降级视为无合适论文。
- 无数据库迁移、前端变更、用户阅读/正式路线确认；不能以隔离恢复测试关闭真实论文交付。下一验收仍是原任务自然恢复是否超过45/59、后续深评与今日实际结果；不人工触发或重扫。此改动不再等待被工具截断的完整work_queue_json才提供安全的恢复改进，但精确剩余输入/生产耗时仍为未取证项。

- 2026-09-10 02:23 UTC 自动核对：实际执行时间晚于心跳标记的09-09 17:16 UTC，以时钟与生产更新时间为准。主数学 monitor_runs 最新02:04:18仍为保存的超时、active_job_id=null，下次03:04:18 UTC；未到重试时间，不手工唤醒。当前沙箱报告只读，但本次计划补丁实际已获工具执行，不能据此声称所有写入被拒绝或要求用户重复授权。尚未取得完整剩余输入，不把旧558项回归冒充本轮新测试；没有业务修改或发布。

#### 阶段2生产验收包：五路径轮转与双领域阶段结果（2026-09-09 15:16 UTC起）

- 只读承接 0854f44/v206，不打开产品页面、不人工触发或重扫。五条 learning_stage_dispatches 均已从最初 pending 获得自然执行，最近更新时间为14:30–15:13 UTC，租约均释放，当前均为empty。empty是本次无新增附件，不代表所有历史续评均无结果；必须结合持久化 review 和步骤资源核对。独立轮转“原五路径都能获得名额”子项已通过，不再重复等首轮领取。
- 数学验收空间0c91daae-3a8c-4c76-89e0-dda1cb521ea1的13:53:50 review已完成。步骤66352ace-9d0d-4f37-8286-03322c1d2a71（随机泛函等周不等式）实际保存论文 On stochastic forms of functional isoperimetric inequalities，DOI 10.1007/s13163-026-00575-7，monitor:feafc02f-537b-4f71-9aeb-2a8c9402b859，来源daily-scan、已有质量分80。模型判为primary并附原摘要引文；逐字段比对持久化review与resources的canonicalId、stageKey和quote一致，readingStatus仍unread、completed_at仍null。路径active不等于已读或完成。这是既有质量通过材料的阶段归属落地，不是新发现/新推荐，不将它冒充KLS原始经典。
- 信息论验收空间60bfce13-8286-4672-9d06-96f8f932d7ef在13:34:03完成两篇阶段判断：doi:10.1109/tit.2025.3548961及title:25939faeec3b414a46e343c8d19eb3f6f8d22c00b90fa0c7eed2807cf2191fd5均为unsuitable。这里是阶段不匹配，不是共享质量拒绝。熵功率/高斯极值基础步骤2b830bbf-3240-4f07-8f3d-0504296d4489仍resources=[]、pending、未完成，路径waiting_evidence。双领域整体材料交付未通过，不能用数学单例关闭。
- 主数学空间01fbe995-9f58-4039-906e-da25514993c7的原KLS基础仍为空。任务42abba85在13:44:04再次以45/59筛选点超时；monitor_runs最新14:54:03又为超时、active_job_id=null，下次15:54:03 UTC。不能把这次运行层最新失败擅自归给42abba85，后继任务ID尚未读取。既有超时重复，不另发失败通知；没有修改超时、额度、退避或扩大候选量。
- 验收边界：生产证据支持五路径公平消费、一个有依据的数学附件、信息论诚实保留缺口。未重新核对本轮今日推荐漏斗或浏览器打开返回，也未审计完整论文库历史；本次无代码变更、无新测试或部署，不重报558项为新运行。下一P1仍为原数学剩余筛选批次与超时根因、信息论基础真实材料；随后补生产阅读返回和今日投影，不以继续增加调度器代替交付。保留docs/及所有用户记录。

#### 阶段2诊断包：固定筛选断点与失败保留（2026-09-09，自动接续）

- 承接 b29ff83/v206，无业务变更或再次部署。只读按任务主键定位 b5d3c070-24ab-44f7-9927-201c88ca0ccc 与当前 42abba85-00b1-4fde-92aa-fa3e1a710bd7：前者 12:33:35 开始、12:35:55 超时，failure_source 为“已快速筛选 45 / 59”，last_success_stage=enriching_screening_abstracts；当前任务 attempt=3、retry_count=2、resume_of_job_id 精确指向前者，13:04:07 仍保留 45/59、360 候选、screening。45 个筛选计数未清零，不代表逐条历史已全部独立核对。D1 工具截断 work_queue_json，因此不能声称读取到最后 14 篇的完整身份与摘要。
- 定位范围已收窄到模型快速筛选阶段，而不是经典检索再次无结果。代码每组 14 篇、两组并发；单组先 24 秒、同一输入再 12 秒，两个无结果轮次后保存 retry_pending；恢复会重置本轮失败计数并继续原候选。隔离执行实际 quickScreenBatch 验证两次请求输入相同且无伪造分数/成功用量记录。此证据不能区分生产网络等待与模型服务耗时，也不能证明缩批或单纯延长时间必然解决，暂不盲改超时与费用边界。
- 新增 3 项实际函数回归：信息论/应用数学均验证一组超时不丢另一组合格筛选；全部超时仍为错误而非拒稿，凭据类致命错误继续上抛；24/12 秒有界重试失败不产生假筛选结果。未使用真实 Key/模型网络。新增文件 lint 通过；复用未变的 v206 构建运行全部 558 项测试，0 失败。此次只增测试，不把上一轮实时来源测试冒充本次，也不重复发布。
- 生产错误日志近45分钟仅取到3条取消的 monitor 请求（5–2135ms）；不是原失败时段的上游调用日志，不据此判定根因。未打开产品页面、未手工触发或重扫。原24小时自然观察已按08:53结论关闭，不再等待；已更新同一自动任务的过时提示，继续材料交付，不增建任务。
- 下一 P1：补取得确切剩余筛选输入及同任务阶段耗时，按真实瓶颈决定是否对失败批次渐进缩小且持久化恢复；保持合格结果/历史/来源与最终质量门槛。之后验证原 Shannon/KLS/Chen 的实际阶段材料。此诊断包完成，不关闭完整材料交付，也不重复通知已知超时。

#### 阶段2推进：独立后台阶段续评与 LaTeX 显示（2026-09-09）

- 自动验证：Node 24 完整构建及 555 项测试通过（0 失败），包含真实 SQLite 调度租约/公平轮转、阶段接口竞态、公式语义与 React 转义回归。发布前重新运行 lint 与 discovery live；生产交付不能由这些隔离测试替代。
- 发布落实：最终 lint 通过；测试加载器换行修正后 8 项组件回归通过；test:live、独立 test:discovery 与 test:discovery:live 均通过。v206 于 2026-09-09 13:10:30 UTC 按原公开范围发布成功，源码 1d54640668cf769f8919021aee6dbd4b36cebc13，部署 appgdep_6aa15ab90e008191822c60c3b05f81f1，环境修订 6；0057 表已在生产生效。用户未跟踪 docs/ 未纳入提交。
- 上线后真实调度证据：13:13:27 UTC，learning_stage_dispatches 已登记 5 条路径，其中一条由独立后台实际执行并落为 empty，其他 4 条 pending。这证明脱离学习页面的领取链工作，不代表新增合格材料；没有主动重扫、手工触发续评或代替阅读。原数学扫描在 13:11:17 UTC 为 screening（42abba85-00b1-4fde-92aa-fa3e1a710bd7），仍需核对 checkpoint 和最终交付，不以再次运行宣布稳定恢复。
- 浏览器边界：本地桌面/窄屏公式已验收，但发布后浏览器连接报 nodeRepl.fetch request failed；返回的旧 QA 快照不能当生产画面证据。已请求原标签页打开线上地址，生产公式目视复核尚待浏览器连接恢复；没有为此读取 Cookie/Key 或伪造验收。下一优先级仍是反复超时与双领域材料实际交付。
- P1 已实现独立阶段续评：Cron/外部定时唤醒并行启动一次学习续评，不依赖学习页或 visit_backstop，也不等待论文来源发现结束。复用 owned-space 的 review-stage 接口和 v205 阶段模型缓存；新 learning_stage_dispatches / 0057 迁移仅保存路径轮转、租约与结果状态。按到期时间公平轮转；失败退避并让出名额；过期执行者不能覆盖新结果；暂停、长期不活跃、已完成与被替代路径不参与。没有创建新研究空间、重扫、阅读记录或正式路线证据。后台沿用现有服务器凭据机制，未配置则不发起；不读取/复制浏览器 Key。
- 用户新增小问题作为 P2 可读性修复并入同批：今日、论文库、详情、引用/路线材料、学习材料、演示及分享页的主要论文标题使用共用 MathText。支持四种明确公式分隔符，仅在展示层渲染，原始标题/检索/规范身份不变；纯文本由 React 转义，KaTeX trust=false，限制展开与公式尺寸，宏不跨论文共享，错误保留原文。没有将全部未定界普通文字当 TeX，也未改图谱 SVG 的原生纯文本标签。
- 浏览器隔离验收：使用已有隔离预览和明显 QA 数学题名，1280px 与 390px 论文库均显示 2 篇、1 处公式、无整页横向溢出。修正 inline-block 导致的公式基线偏移为 inline-flex；窄屏关闭导航后公式完整换行。预览所有 API 留在隔离夹具，不把它当生产质量验收，也不代做阅读。测试中发现公式布局不应进入仅配色的 interface-theme，已拆独立 math.css；旧组件测试加载器增加真实 MathText 依赖，未放宽业务断言。
- 生产恢复观察仍有风险：原数学空间再次在 12:35:55 UTC 保存请求超时、active_job_id=null、下一次重试 12:55:55 UTC。v205 证明可获得恢复名额，不证明任务稳定完成；保留逐任务 checkpoint、深评与最终结果核对，不能把自动重试次数视为交付。来源健康检查为 3 个健康，OpenAlex 和 Semantic Scholar 均 429 降级。
- 后续优先级：P1 先验证独立后台真实领取/阶段结果，并定位原任务反复超时；然后完成双领域经典材料真实交付及阅读返回链。P2 记录新增数学库使客户端构建触发 500kB 分块警告，后续按需加载优化，不为消除告警更改质量与身份逻辑。最终测试/发布证据追加在本节，完整论文交付阶段保持未完成。

#### 阶段2推进：阶段归属续评与超时恢复名额（2026-09-09，阶段验收仍未完成）

- 发布落实：最终 lint 通过，v205 于 2026-09-09 12:33:17 UTC 按原公开范围发布成功；生产 DB 已出现 learning_stage_reviews，新迁移已生效。业务源 1aa2fee6a83aad6d700c479a069d538f2db66ca5，部署 appgdep_6aa152024db4819183f8dec56b82c710，环境修订仍为 6。没有读取或变更模型 Key；没有 GitHub CI 新结论。
- 发布后生产原数学空间于 12:33:40 UTC 已从 error 转为 screening，active_job_id 为 b5d3c070-24ab-44f7-9927-201c88ca0ccc；这是实际领取/运行证据，不只是到达重试时间。本轮没有手动恢复或重扫，但曾请求打开现有网站，尚未独立证明触发源究竟是 Cron 还是 visit_backstop，也未逐项核对原 checkpoint 身份与新增深评，因此不能宣布自然恢复或完整交付验收结束。后续继续观察本任务，勿重新创建空间或扫描替代验收。
- 已接入现有学习页 `advance-evidence` 自动续跑：仅使用共享质量通过、未忽略、主题相关且有摘要的候选，由模型判断阶段归属。原始文献阶段必须给出真实摘要摘录；综述、标题复述和虚构引用不能成为原始材料。每次最多新增一批八篇判断，缓存的否定结果允许下一轮继续评后续候选，不是累计模型额度限制。
- 新增 `learning_stage_reviews` 及生成迁移 0056：按空间、路径、阶段和摘要快照缓存，使用持久化租约、五分钟失败退避和过期接管。模型返回后重读路径与资格，写入还比较阶段文本、历史资源快照、当前摘要、质量资格和忽略状态，避免旧结果回写；原历史资源保留，不更新阅读记录或正式路线证据。不在 GET 中调用模型、不保存模型错误体或凭据。
- 边界未关闭：这次挂接的是现有学习页续跑入口；独立定时器尚未直接领取阶段归属任务。下一项必须把无人打开页面时的消费与公平轮转补齐，不能把学习页自动续跑说成完整后台交付。
- 生产原数学任务只读核对：`monitor_runs` 保存超时，next_run_at 为 12:10:10 UTC，之后多次仍为 error、active_job_id 为 null，无暂停设置且用户活跃。页面仍为 360 候选、45/59 筛选；今日旧简报仍是 300/61/11/1，二者不混用。未点击人工重扫/恢复，也未写生产阅读和路线确认。
- 定位并修复可复现恢复资格遗漏：普通队列优先正在运行的任务，专用已保存错误名额原先仅接受 stale_scheduler_recovery，不接受线上这次确切超时文本。新增回归修复前确实失败，修复后超时任务可获得额外恢复名额、健康任务保留；凭据、余额和未知错误不纳入。该机制解释一种饥饿路径，不把它当作已经证明生产恢复成功。
- 验证：Node24 生产构建和完整 547 项测试通过；包括双领域构建 Worker+D1 自动挂载、重复无额外模型调用、模型执行中阶段变化/质量撤销拒写、无阅读/路线确认，以及并发、过期接管、失败退避、分批续评。首次接口夹具撞同名空间唯一约束，改为独立 QA 名称后通过，未放宽业务限制。test:live 四来源健康、Semantic Scholar 429 降级；test:discovery 和 test:discovery:live 通过。最终 lint/发布结果在后续记录，不把隔离模型夹具当作真实生产语义验收。
- 优先级：P1 先发布并验证超时恢复修复及新迁移；随后补独立后台阶段归属消费，完成 Shannon/KLS/Chen 真实逐篇交付与桌面/窄屏同快照验收；再回到跨页体验。遵循紧急可靠性修复可先上线规则，整个阶段仍不能标完成。用户未跟踪 docs/ 保留。

#### 阶段2推进：合格补充阅读与生产暂停核对（2026-09-09，阶段未完成）

- 自动阶段附件与模型规划是不同路径：advanceLearningPath只调用stageFit而不生成专属groundedStageEvidence，因此未有正式路线角色的每日扫描论文不能自动获得基础/里程碑角色。此处不能直接放宽为按年龄、引用数或经典检索锚点认定原文证据；模型阶段判断的自动衔接仍是未完成项。本轮没有声称它已经修好。
- 已实现安全的补充阅读：在stateFor返回值中，从同一contextForSpace已质量通过且未忽略的候选选择最多两篇与阶段明确主题匹配的材料，接入原supplementaryResources及原补充阅读组件。仅作响应投影，不写resources_json、不改变原文缺口/ready/完成状态、不更新路线证据或阅读进度；历史补充材料不删除，泛化阶段没有足够主题信号则不补。综述可以帮助阅读，但绝不代替原始文献。该实现尚未发布，线上仍v204；完整阶段验收结束前不把它当新一轮已交付发布。
- Node24 lint、生产构建及完整537项回归通过。新3项纯函数回归覆盖资格/相关性/数量/幂等/状态保留；实际构建Worker+D1新增双领域用例验证未评/忽略排除、补充材料精确打开、返回原阶段、resources_json保持[]、没有新增阅读或路线证据。首轮新增用例错误假设空阶段为active，实际原语义为pending，已改为前后状态一致且非completed后重跑通过；没有为通过测试改变生产状态规则。官方辅助构建仍因系统Node20/npm路径失败，有效构建来自Node24 npm test。此次未改发现来源，未重新跑实时发现基准，不把上轮结果冒充本轮。
- 线上原数学空间页面实读：论文库1029；09-09简报300候选/61初筛/11深评/1正式入选且今日可见；论文为The KLS constant is $O(\\log^{1/4} n)$，这只是页面投影，本轮没有独立复核该论文内容/质量。另一个当前任务暂停在360候选、45/59初筛，保存原因为模型或来源响应超时，页面提示北京时间09-09 20:10后自动重试。未点击从断点继续、未改Key或人工触发扫描；不能把旧简报与暂停任务混为同一漏斗，不能据此宣称Shannon/KLS原文已交付。
- 改序：先核对该暂停任务到期后是否恢复以及原经典逐篇状态，再补模型阶段判断和完整双领域生产交互验收；补充阅读代码保留在本阶段批次，不抢先宣布整个闭环完成。桌面/窄屏同快照验收仍待做，用户docs/保留。

#### v204公开发布与下一阶段验收（2026-09-09）

- 用户确认“发布”后，按原public范围部署已保存v204，未重建/重复保存；部署appgdep_6aa149540808819197d65276e9bdc0c3于11:56:13 UTC成功，环境修订6，业务源c6fa23d2de8754226b3ef576981655e979ddf206。上线不等于经典论文质量或学习交付通过；本轮没有新跑模型评估或人工重扫。
- 下一阶段以完整研究/学习材料交付验收为单位（沿用2–3工作日时间盒），不继续逐个微调发布：先逐篇确认Shannon/KLS/Chen的可用摘要、共享队列资格、当前任务实际消费与最终结果；按证据区分来源缺失、排队未消费及质量未通过。原经典摘要确实不可得时，核对已有合格综述/里程碑能否作为明确标注的桥接材料，不冒充原始文献、不降低门槛。
- 阶段退出标准：两领域至少有通过共享质量评估且阶段匹配的真实可读材料；今日推荐与论文库状态一致，打开材料返回原学习阶段正常；未交付原始文献继续明确标缺；用户阅读/正式路线确认不代做。按逐篇结果记录，不用累计候选量代替通过。
- 之后才按既有顺序推进跨页面操作和桌面/窄屏一致性，再做隔离反馈闭环。若时间盒结束仍缺关键证据，记录具体阻塞及调整，不能无限重复观察或宣布完成。现有用户docs/保留。

#### 阶段2：深评摘要降级回退修复（2026-09-09，验证完成，已随v204发布）

- 当前生产KLS详情确认DOI为10.1007/bf02574061、等待质量评审、相关分85；页面没有展示摘要或逐任务深评投影，不能据此断言D1摘要为空。仅打开分析及发现记录，未接受、修改阅读状态或触发重扫；页面可能正常记录查看行为，不将其计为学习完成。
- 真正复现的代码断点：enrichDeepReviewAbstracts以Map.has判断补证完成，Semantic Scholar返回不足120字符时阻断OpenAlex；且两来源降级后未尝试健康Crossref。提取实际生产函数的回归修复前断言失败（短摘要候选未交给OpenAlex、无Crossref回退），不是仅更改提示文字。
- 修复继续使用原120字符证据条件；按最长摘要合并SS/OpenAlex/Crossref，Crossref逐DOI身份一致才采纳JATS摘要，整个回退共用15秒期限、每请求一次尝试并使用原共享来源退避。来源失败停止该来源后续请求但保留已成功证据；不读取全文，不改变质量门槛、推荐/路线证据状态或清空任务。此回退仅在既有深评补证阶段运行，不另起后台流程。
- 独立只读Crossref实查：KLS原始DOI与Shannon 1948年7月DOI均HTTP200/身份匹配，但摘要字符数均0。因此不能声称此回退独自解决原经典论文交付；其他来源摘要、生产任务实际消费及最终质量结果仍待核对。该独立探测不是生产来源重试、不是D1已补证证据。
- 基线Node24 lint、含构建完整530项通过；修复后lint、含构建完整533项通过，新3项覆盖短证据续查/身份不匹配拒用/部分失败保留/实际D1旧长摘要及评审状态保护。test:live四来源健康，SS HTTP429降级；test:discovery和test:discovery:live双领域通过。官方build-site辅助脚本仍误用系统Node20/npm路径失败；有效构建为Node24原npm test生产构建，未为环境问题修改业务。没有本轮新增真实模型推荐漏斗，不以这些回归替代生产交付验收。
- 优先级不变：此补证降级修复先于装饰性界面调整；上线后接续原任务核对经典摘要与评估结果，不扩大搜索量、不人工清除退避。公开站点现v203，按Sites发布边界准备已验证批次后等待本批公开发布确认；用户docs/继续保留。
- 发布准备落实：c6fa23d2de8754226b3ef576981655e979ddf206已推送Sites源仓库，官方打包器完成后保存v204（appgprj_6a83f86ecca081919f3094b285bc2b1d~appgver_6db046127f0c8191b1dad30b19006cae），归档sha256:9c98254c8be66759a79b15a5b8444aa8a8e5f9087c14cfa6f3a8c893c1e40559。尚未部署，收到本批公开发布确认后直接使用此版本，不重复保存。临时源凭据已从会话存储清除。

#### v203真实页面验收：连接恢复，经典仍待评（2026-09-09）

- 直接选择现有 In-app Browser 恢复控制，沿用原标签与登录会话，无需重填 Key。窄屏侧栏收起时 DOM 仍含导航按钮，须先展开菜单并核对导航后的主内容；不能将隐藏按钮点击无效果判成产品故障。控制调用仍约21秒且偶发超时，连接恢复不等于控制性能问题完全解决。
- 信息论/Yilin 的全部发现为1091篇；按完整标题检索，Shannon 1948年7月及10月两条均保留、等待质量评审、未加入阅读。高斯极值性路径0/5完成、4篇材料，基础阶段0篇且来源暂不可用/将重试，完成按钮禁用，其余四阶段各1篇。这里只按当前页面空间报告，未取得空间ID映射，不冒充原确切信息论验收空间的逐篇数据库投影。
- 完整空间选择器实际加载出原“P1 应用数学路线恢复 0901”，正常切换进入而非更换演示空间。KLS原始论文《Isoperimetric problems for convex bodies and a localization lemma》（1995）在全部历史可检索，等待质量评审；Chen 2021《An Almost Constant Lower Bound of the Isoperimetric Coefficient in the KLS Conjecture》正在核对，均非正式推荐。KLS路径0/5完成、2篇材料，方法/前沿各1篇；基础来源暂不可用、里程碑/研究问题尚缺可靠材料，基础完成按钮禁用。
- 数学页面保留09-04旧简报11篇推荐、展示6篇并注明不计入今日；其中旧末批漏斗360候选/59筛选/13深评/0新增只属于旧简报，不是本轮漏斗。论文库从初次813刷新为1029，后续库页同为1029；不同时间快照不足以解释216差值，不计为本轮新增。页面显示旧任务断点恢复，期间用户没有手动扫描；此观察不能计为独立Cron样本，也未验证逐ID跨重扫保留或桌面/移动同快照一致。
- 下一优先项仍是阶段2：在正常授权边界核对已入队经典的摘要是否补齐、当前任务是否实际消费、质量结果及阶段匹配。发现存在已被证明，不用扩大搜索量替代消费诊断；来源退避仍遵守，不能因等待便清空任务、降低门槛。生产完整闭环继续未通过。此轮仅真实页面验收与计划更新，未改业务/部署、未重新运行530项测试；未读取Key、未代用户标记阅读或确认路线，用户docs/保持不动。

#### v203发布后验收：访问阻塞与到期边界（2026-09-09）

- 发布后重新只读核对两条原缺口：Shannon仍attempt7/queued5、next_retry_at=09-09 15:03:33.168 UTC；KLS仍attempt6/queued5、next_retry_at=09-09 18:53:45.618 UTC。两者最近来源执行均早于v203上线，本次尚未到其下一重试时间，不能据此判定新补证修复失败，也不能说新版本已经让这两篇完成评估。
- 浏览器控制getState失败，重置控制会话后再试仍nodeRepl.fetch request failed，未取得标签清单。未重试被安全阻止的内部诊断地址，未读取Cookie、伪造空间所有者或扫描整张论文表。当前精确逐篇生产评估/今日/学习交互验收因正常会话控制不可用而受阻；需恢复浏览器控制连接，不是要求更换模型Key或重建研究空间。
- Sites最近15分钟错误查询仅返回/api/spaces与/api/model-settings两次canceled事件，不能单凭取消归因为后端宕机或Key失效，也不是全部接口健康证明。没有触发重扫或新增发布，没有把已有530项回归当本轮新测试。
- 该访问阻塞明确保留，不通过重复增加测试或只改文案来宣称生产闭环完成。自动周期保持既有任务与原计划；可安全读取的自然来源进展仍可观察，待正常会话恢复后优先补逐篇结果。用户docs/保留。

#### v203公开发布落实（2026-09-09 11:07 UTC）

- 用户明确回复“发布”后，复核现有访问为public，直接部署此前保存的v203归档，未重复构建、保存或扩大访问。部署appgdep_6aa13dfe1aec819192aba0d6b5bf60a9于11:07:58 UTC返回succeeded，环境修订仍6；线上版本现为v203，业务提交df33d6529857286ee9dc8d9fd803c7b53dbcc70f。
- 本次上线包含摘要合并保留、有效Retry-After期限保留、学习指导/载入/嵌套卡片修复及评估中候选补证。其后98c4020只增加消费端回归，无新增业务代码，不需要为测试另发版本。未触发生产重扫或代用户确认；发布不等于Shannon/KLS逐篇质量及今日/学习交付通过，该验收继续保持未完成。
- 发布终态已由Sites确认；浏览器标签清单读取失败，不把它归因为网站故障或模型Key问题，不据此声称生产页面交互已复验。现有用户docs/保持不动。

#### 阶段2：补证后共享队列实际消费验收（2026-09-09 10:54 UTC起）

- 承接b637262和待确认v203，不重复发布、保存或询问确认。此包只增加隔离验收，不改业务源码或公开受众，不触发生产来源/模型。阶段1自然观察已收口，不再重复读全调度表。
- 新测试从真实monitor路由AST提取pendingCandidateQueue及其三个实际常量，使用实际来源有效性SQL与来源解析函数，复用repository的九张表定义和唯一索引，在Miniflare D1执行；不以手写数组过滤冒充消费者，也不初始化生产Worker。
- 信息论/数学两个明确标注的QA空间分别覆盖初筛/待核对状态：固定候选ID先读到空摘要，真实enqueue补证后，普通队列及冻结ID读取均取得新摘要，learning优先通道与两份来源仍在。已推荐记录从普通队列退出但可被冻结ID读取用于当前任务对账；用户忽略后两者受抑制，论文记录保留。空ID集合仍空，不意外退化成全空间读取。这里模拟冻结ID输入，不宣称执行过生产完整job或模型评审。
- 源码核对深评与核对阶段均重新调用此数据库读取函数；因此没有证据支持为此另建任务、清空deepIds或重跑已完成阅读。该调用接线核对与真实D1读取测试相互补充，但不能证明生产Shannon/KLS已经通过质量或进入今日。
- 专项、变更文件lint及Node24完整530/530回归通过，零失败/跳过。此前构建与v203归档保持不变，未重跑实时检索或页面验收；保护用户docs/。主优先级仍为安全取得生产逐篇质量投影与v203公开确认后的验证，后续界面工作不抢占。

#### 阶段2：评估中候选重新发现的补证断点（2026-09-09，实现及回归完成，待发布）

- P1证据补充优先于后续纯界面清理：enqueueMonitorCandidates只更新metadata/route-gap洞察，导致deepseek_screened和deepseek_verification_pending收到健康来源摘要时仍保留空摘要。新增真实Miniflare D1测试已复现screened期望新摘要、实际空字符串；这是代码层断点，尚无生产逐篇投影证明它是Shannon/KLS未交付的唯一原因。
- 修复边界：仅补充仍在评估、从未推荐且未被用户忽略的候选的更完整摘要，不修改评估状态、分数、推荐结果、评估时间或历史阅读；完成评审、被拒绝及曾推荐记录保持原样。不增加调用/解除退避、不把补证当质量通过。已有持久化和来源归属保留；验证后再按现有公开发布审批流程处理批次。
- 验收：两种待评状态、空摘要重扫、已评审/被拒绝/用户忽略/曾推荐历史保护；实际入队结果仍处共享评估队列、不产生正式推荐。lint、完整构建测试及来源/双领域发现回归后记录实测结果；生产逐篇闭环仍独立待验。
- 落实：新增带状态条件的摘要补充UPDATE，仅更新abstract_text，保留全部其他洞察字段；同批去重身份和来源照常保存。真实D1回归修复前因空摘要断言失败、修复后两项集成测试通过，六种状态逐项验证全部其他字段（包括评估时间）不变，重复空摘要不回退。
- 本轮Node24.19.0完整lint、npm test（含构建）及529/529测试通过，最终修改文件单独lint通过。Sites build-site辅助脚本因子进程误用系统Node20/npm路径失败，未修改业务兼容代码；有效构建证据为项目原npm test链路的Node24生产构建，不将辅助脚本失败隐藏成通过。test:live四来源健康、Semantic Scholar fetch failed如实降级；离线/实时双领域发现均通过，实时precision@10信息论0.9、数学0.8。这些是检索基准，不是生产完整模型漏斗。
- 此次无前端修改，不重做已通过的隔离页面验收。生产Shannon/KLS的逐篇筛选、深评、今日投影及基础匹配仍未证实；此次没有读取Key、人工重扫或代用户确认。现有网站访问范围已核实为public，仍v202；新批次须经当前Sites公开发布确认，不能把本地修复描述为线上已修复。
- 发布准备完成：业务提交df33d6529857286ee9dc8d9fd803c7b53dbcc70f已推送Sites源仓库，已验证构建使用官方打包器生成归档并保存v203（appgprj_6a83f86ecca081919f3094b285bc2b1d~appgver_10bd1f96a3fc8191a2f49c41860d874e）。打包首轮Windows盘符被GNU tar误认为远端，改用同一脚本的MSYS绝对路径后成功，没有更改打包器或业务。此版本包含此前已验证未发布批次，不含用户未跟踪docs/。尚未部署，等待公开发布确认；恢复时直接部署此已保存版本，不重复保存或混用之后文档提交的SHA。

#### 阶段1观察收口与阶段2交付边界（2026-09-09 08:53 UTC起）

- 观察到期结论：自然调用稳定性子项通过；整个可靠性/论文交付验收仍为部分通过，不将整个7–10工作日周期标完成。不再延长本次24小时观察，不再以等待Cron样本阻塞后续工作。
- 本轮只读分页到表尾，共408条monitor_scheduler_ticks；修复后06:23:26（09-08）至08:53:51（09-09）约26.5小时，共160条记录，全部completed_at非空、error为空、failed_count=0，最大相邻开始间隔11.878分钟。160是各触发来源的调度记录，不是160次独立Worker调用或论文数；不能仅凭external_watchdog归因。初始三次独立调用已有Cloudflare/GitHub/D1对照；上一轮06:51读取的Cloudflare最近一小时含06:33和06:43自然Cron成功，跨越24小时。该Cloudflare证据不是本轮新取的全天事件导出，不宣称全年SLA或每个调用均已独立归因。本轮GitHub最新自然schedule仍05:21:28成功（34314546893）。
- 六个原异常任务完成子项沿用02:48–02:54确切ID验收，未重复读取或冒充本轮新完成。后续轮次是否持续取得资格与领取、每篇推荐是否实际投影到今日，尚未完整验收，作为后续闭环工作保留，不能用零失败心跳替代。
- 本轮继续核对原定两空间而非另换演示空间：KLS缺口06:53:45自然重试到attempt6/queued5，Crossref保留原文身份、共享基线6项，OpenAlex冷却，仍retryable/source_unavailable，下次18:53:45.618 UTC。Shannon保持attempt7/queued5，下次15:03:33.168 UTC。累计queued数不是去重论文数或质量通过数；没有手动触发、重扫或清除退避。
- 两条原学习路径仍存在且为waiting_evidence；确切foundation步骤的resources_json均为[]，没有completed_at。故“经典已成为基础材料”本轮明确未通过，不把来源找到原文、任务重试或历史推荐量当通过；没有读取完整逐篇质量投影，筛选/深评/今日可见性继续记未知，不猜测为零或被拒绝。
- 阶段2主工作包（预计2–3工作日）维持逐篇闭环：以两条确切DOI/space/path/job为固定验收对象，先取得既有候选的摘要、质量阶段及未通过原因，再核对阶段匹配和今日投影；只能通过已有安全读取边界取数。此前浏览器阻止的诊断入口不改协议/身份绕过，不扫描整张论文表寻找替代证据。若原定空间无法通过正常所有者会话验收，明确该项受阻；隔离验收可继续但不得替代生产结论。
- 已验证未发布批次（摘要保留、Retry-After、学习指导、加载计数、嵌套卡片样式）独立于经典来源恢复，不应无限等待经典补齐才发布。发布必须遵守当前Sites技能的现有公开受众审批流程，保持受众范围不变；本轮未保存/部署新版本，不宣称线上已包含本地修复。上一轮528/528为历史测试证据，本轮仅证据与计划更新，不重复报为新运行。
- 后续顺序：阶段2逐篇链路与已验证批次发布准备 → 阶段3剩余逐页操作/引用清单 → 阶段4隔离反馈及跨重扫。P0仍即时抢占，来源等待不再触发重复泛化检索、扩图谱或重做已完成的学习页夹具。保留用户docs/及全部研究历史；既有自动任务继续完整周期，不增建任务，不要求每个小步骤回复“继续”。

#### 部分来源失败的共享队列持久化验收（2026-09-09 04:50 UTC起）

- 只读确认Shannon缺口03:03:32自然执行到attempt7，queued_count仍5，保持retryable/source_unavailable，下一次15:03:33 UTC；不是上一轮刚到期后卡死。KLS仍attempt5/queued4，06:43重试尚未到期。不把来源任务次数或累计queued_count当独立论文/通过数，不人工唤醒。
- 新增本地Miniflare D1集成回归，直接调用生产enqueueMonitorCandidates和completeResearchGapDiscovery，并复用repository里的候选、洞察、来源、反馈、阅读及monitor表定义与唯一索引。不是仅检查提示文字或用Node SQLite假装D1；没有连接生产数据库、使用Key或请求外部论文。
- 实际覆盖：空摘要原候选先入队但不推荐→健康来源补充摘要、另一来源失败→缺口retryable且释放锁→重复发现仍复用同一paper ID、两份来源均保留、短/空摘要不覆盖较完整证据。旧已评审摘要、忽略反馈和reading笔记跨重复入队保留；未评审原候选的llm_recommended与ever_recommended仍0。任务累计queued_count可增而独立论文数不增，测试明确区分两者。
- 专项、变更测试文件lint及完整528/528回归通过；此包只新增测试与落实记录，业务源码/已验证构建不变，没有重复执行实时来源测试，不因生产来源等待而扩展检索或前端。生产逐篇筛选、深评与今日可见性仍未知，不能用隔离用例代替。GitHub最新自然schedule仍00:42:31成功；24小时观察按06:24后首次运行总结，未提前关闭，也未再增加调度器。保留用户docs/，未部署，线上仍v202。

#### 六个原异常任务完成验收（2026-09-09 02:48–02:54 UTC，24小时观察未关闭）

- 用已记录的六个确切任务ID进行有界单行查找，并核对space_id；六个任务均ready/checkpoint=complete、error=null且completed_at非空。它们不是重新生成的替代任务，至此“恢复执行→原任务完成”子项通过。记录的是任务持久化计数，reviewed_count不冒充深评数，recommended_count不冒充今日可见/用户接受数，也不跨空间合计成去重论文数。

| 空间前缀 | 原任务ID | discovered_count | reviewed_count | recommended_count | completed_at（09-08 UTC） |
| --- | --- | ---: | ---: | ---: | --- |
| 2df5dc33 | f2282641-3d69-4195-9bb8-ffb45bb6a915 | 299 | 59 | 2 | 17:43:35.811 |
| f1e323ac | f3c635ba-75e7-45ab-9c68-2c17dbe87113 | 80 | 12 | 1 | 11:33:33.642 |
| 1ea51c2c | 608a0329-ff00-44a0-b96d-ef1c6b017226 | 306 | 59 | 3 | 19:43:34.757 |
| 48e4cd89 | 3b2227e4-bd79-4be4-bca2-6b7b7774b8f7 | 310 | 58 | 4 | 18:43:56.937 |
| ce679f42 | 0d859dd1-af22-4dae-aaed-d8d6c01efd8d | 311 | 59 | 1 | 12:53:34.891 |
| 303047e8 | ad3f3121-8000-4dee-b15b-0c5ee62124b1 | 307 | 59 | 3 | 19:13:39.045 |

- 完整读取42条monitor_runs，六空间均ready/error=null/active_job_id=null，历史任务仍存在。其next_run_at已过去不自动等于故障；本轮未取得后续轮次调度资格/领取全链路，不宣称后续周期已验收。2df5dc33的last_run_at仍09-02；源码明确quality_queue完成不会改last_run_at（该字段是来源扫描时钟），因此不用该字段单独否定质量任务完成，也未把未知scanMode猜成已证实。
- 原定Shannon任务仍attempt6/queued5，KLS仍5/4；02:54只读复核Shannon刚到02:53:33重试期限但未见领取，不以不足一轮的延迟判断卡死，更不手动触发。Crossref身份记录不等于经典质量通过，生产逐篇筛选/深评/今日可见性仍未取得。本轮没有增加泛化检索或访问用户论文详情。
- 生产OpenAlex00:13:30再次429，failure_count43，next_allowed_at=04:13:30.368 UTC；没有清除或绕过。GitHub自然schedule最新00:42:31成功（34296148198）；它和external_watchdog不能单独证明独立Worker触发。浏览器控制恢复遇到超时/未附着，未取得新Cloudflare页面证据，未绕过被阻止的诊断入口、读取凭据或伪造工作区身份。
- 阶段1剩余明确为跨24小时自然调用复核、后续调度与逐篇交付一致性；按原计划06:24 UTC后首次运行作通过/不通过总结，不无限延长。阶段2继续确切经典质量链路，不重做已完成的隔离学习页面验收。此次是生产验收工作包，无业务代码改动，不重复报告上一轮527项为新测试；保留de3b13c及未跟踪docs/，未部署，线上仍v202。

#### 已有学习路径双领域隔离验收与卡片样式根因修复（2026-09-09 00:48 UTC起，未发布）

- 为避免本地无凭据/无路径阻碍前端验收，新增独立preview-learning-fixture脚本，直接加载真实ResearchApp及样式；不加载项目Vite配置、环境文件、Worker或数据库，API全部截获，不转发；不支持的写操作在读取请求体前拒绝。两领域所有材料均标QA/fixture，仅测试呈现，不是实际发现、质量通过或正式推荐证据。该脚本不接入生产路由或配置。
- 实际重现P1可用性缺陷：旧`.v2-learning-path article > button`为时间线标记设置22×22像素，却也匹配新嵌套论文阅读按钮，造成窄屏标题逐字竖排、卡片与指导重叠；总scrollWidth正常并不能证明可用。将该组旧时间线选择器收紧到直接子article，保留旧标记样式，不用新覆盖层堆补丁。
- 修复后390×844浏览器视口（有滚动条时布局宽375）阅读按钮约269px宽、数学长标题约130px高，scrollWidth=clientWidth，无横向溢出；实际视口截图确认标题、原文入口、三块指导与完成按钮分区清楚。完整长页截图存在拼接重复，采用DOM唯一节点及真实视口检查，不据长图重复断言DOM重复。
- 双领域均实测基础→方法→论文详情→返回，方法阶段仍选中，路径2篇、2可用/3评估中、0/5完成保持，未改变阅读状态或确认。基础reading-task不展示通用三块指导，方法grounded显示具体指导；无材料里程碑保持来源不可用/待重试，完成按钮禁用。英文切换实际捕获Loading the learning path且没有零篇占位，随后恢复2/3计数和原方法阶段。桌面/窄屏共享相同快照。
- 新增3项回归覆盖夹具无副作用、未知写入口拒绝、旧选择器不得污染嵌套按钮；lint、构建、完整527/527测试通过，test:live、test:discovery、test:discovery:live通过。此轮本地四来源健康，Semantic Scholar429；不作为生产来源恢复结论。
- 此P1卡片可用性优先于后续装饰/文案，已修复并完成隔离浏览器验收。此前学习指导与加载计数批次的“有材料前端验收”子项关闭；生产双领域质量闭环、经典匹配和24小时自然观察仍未关闭，不将夹具的质量标签当真实评估。线上仍v202，未发布；不创建额外自动任务，不改用户数据。下一优先项回到自然观察与生产逐篇质量，不继续扩展测试夹具或重做学习页。

#### 学习载入语义与未知计数修复（2026-09-08 22:46 UTC起，未发布）

- 承接f8ce2a9及既有未发布批次。定位到GET读取使用learningLoading，POST生成独立使用learningAction="generate"；学习主页却把GET状态写为“正在生成”。改为“正在载入学习路径”，删去误导性的“核对方向”说明，保留POST生成按钮、请求边界、跨空间取消保护与持久化逻辑。
- 同时修正未知计数：当前空间未读取完成、正在读取或读取失败且无路径时不显示占位的0篇；成功读取后的真实0仍显示。重新规划失败但旧路径仍在时保留有效计数，不把失败当论文清空。没有改变材料匹配、质量队列、阅读或确认状态。
- 新增3项实际JSX渲染/接线回归，覆盖中英文载入与生成区分、未知/失败/真实零、18可用17评估中及重新规划失败保留计数。首次完整回归523/524，唯一失败为旧图标测试要求错误的“正在生成”文案；更新该断言后重新构建并完整524/524通过。完整lint通过，最终修改文件另跑lint。test:live、test:discovery、test:discovery:live通过；本地Crossref/DataCite/arXiv健康，OpenAlex429、Semantic Scholar fetch failed，未将降级描述为无合适论文。
- 在本地正常网页实测桌面1280与窄屏390×844：学习页读取后真实0篇、无路径，中英文一致，窄屏scrollWidth=clientWidth=390。未点击生成、扫描、阅读或确认，未读取模型凭据；正常访问自动调用本地monitor并返回502，不计作生产Cron样本。本地显示认证失败仅作测试环境状态，不推断线上Key失效。此次只验收空状态；已保存路径的指导呈现和返回操作仍须同批补验，不以空页截图代替。临时页和本地服务已关闭，窗口尺寸已恢复，用户线上标签保留。
- 只读生产复核：Shannon仍attempt6/queued5、下一次02:53:33 UTC；KLS仍attempt5/queued4、下一次06:43:49 UTC，均未到期。OpenAlex自然请求20:53:32再次429，failure_count42、next_allowed_at=09-09 00:00:00.424 UTC；未清除退避。GitHub自然schedule 34287348087于22:43:51成功，不能当独立Worker来源证明。24小时观察仍未满，不提前关闭。
- 优先级不变：先到期自然观察与经典质量；独立时间推进学习可用性验收。此包完成代码、回归及空状态响应式检查，但整个阶段和发布仍未完成，线上继续v202。保留docs/，没有另建自动任务。

#### 双领域网页归属与切换实测（2026-09-08 18:45 UTC起）

- KLS指定来源任务自然重试到attempt5、累计queued4，仍retryable/source_unavailable，18:43:49更新，next_retry_at=09-09 06:43:49 UTC。这只证明来源任务继续执行，不证明经典质量通过。
- 恢复了正常浏览器交互控制，未重试被阻止的诊断入口。当前浏览器的空间选择器只有应用数学Ming、信息论Yilin、机器学习阅读Sarah，不包含此前D1指定的“P1 应用数学路线恢复0901”。不能将Ming页面与另一个匿名工作区的指定空间数据拼接，也不读取Cookie/伪造身份。
- 实测切换到Ming：今日出现旧任务保存点恢复、4篇待处理/备选；进入学习显示“还没有学习路径/0篇可用”。没有创建路径、点击重扫或完成阅读；此状态不能解释为指定数学路径丢失。返回Yilin并等待读取结束：18篇可用/17评估中、路径6篇、基础0、完成0/5，核心方法仍选中且两篇原材料保留，说明跨空间返回没有覆盖原路径或完成状态。
- Yilin今日历史简报明确标2026-09-08：末批360候选/47筛选/12深读/1确认，当日累计3篇保留；当前日期为09-09，页面说明历史简报不计入今日数量。该历史漏斗属于Yilin而不是指定04948d10空间，不作为原定两空间验收。页面正常访问触发自动续跑，必须从独立Cron样本中分开。
- 新登记P2加载语义：读取已有路径期间显示“正在生成学习路径”，结束后恢复原路径；后续区分读取与生成，不为文字调整改变后台行为。优先级低于经典质量与发布前验收，与既有学习指导去冗余同批验证。
- 已恢复原Yilin学习页面；未部署，本轮实际网页仍v202，不能声称未发布指导组件已完成浏览器验收。后续采用隔离双领域测试或确切可访问空间进行一致口径验收；不要求用户反复刷新、不跨所有权边界取数。

#### P1来源退避边界修复（2026-09-08 16:44 UTC起，未发布）

- 只读确认Shannon任务已自然重试：14:53:32 UTC attempt5→6，queued_count仍5，Crossref仍找到原文，OpenAlex冷却约4199秒，next_retry_at变为09-09 02:53:33 UTC。不能把累计入队未增解释为原文被否定或已评估通过。KLS下一来源重试18:33 UTC仍未到期。
- 生产external_source_throttles直接证据：OpenAlex last_status=429、failure_count=41，16:33:30更新，next_allowed_at=20:33:30 UTC。确认生产来源确实限流，而非仅模型/页面提示问题；本地OpenAlex健康不代表生产出口健康。没有读取原始响应头，不能断言生产服务当次要求的完整Retry-After长度。
- 代码复现独立缺陷：fetchExternalSource将有效Retry-After截短为最多4小时；测试以24小时数值头和HTTP日期头复现14400≠86400。修复为尊重可表示的有效来源期限，仅无效头使用原有指数退避。保留单请求边界、来源独立锁、质量门槛及现有持久化记录；不清除生产退避，不更换出口绕过429。
- 专项覆盖超过4小时仍无额外请求、失败次数不因等待增加、到期后恢复、健康来源独立运行，以及超出日期范围的头不会导致RangeError。Semantic Scholar独立实现没有同一4小时截短逻辑，本轮未改动。
- 此P1小修优先于待验收的P2指导呈现，仍进入同一验证批次；生产经典质量、页面验收和24小时观察未关闭，不以修复提前重试代替解决生产来源限流。
- 验证完成：专项7项、lint、构建、521/521完整测试，以及test:discovery、test:live、test:discovery:live均通过。仍未部署，现有生产限流期限没有修改；本地来源结果与上述生产429严格分开记录。

#### 阶段3独立工作包：学习指导去冗余（2026-09-08 14:44 UTC，待页面验收/发布）

- 在来源退避与24小时观察窗口内落实上轮已定位的小范围可用性项，未把它提升到经典质量闭环之前。新增共用LearningStageGuidance，两处学习入口仅在grounded且有实际材料时展示具体解读；reading-task不再重复三块通用话术，空字段也不渲染空面板。阶段目标、材料、降级状态、计数、阅读与完成按钮保持原逻辑，持久化目标和匹配输入没有修改。
- 新增渲染回归覆盖中英文、未核实指导、有/无材料、空字段、两入口共用规则以及不修改步骤数据。该包不是完整前端阶段交付；桌面/窄屏实际页面与批次发布仍待验收，不把静态渲染替代页面使用测试。
- 本轮lint、构建通过；首次完整测试定位到旧契约仍在巨型入口文件内查找已抽出的CSS类名/标题，已改为检查组件接线和组件文本，保留实际渲染及窄屏CSS断言，完整测试重跑通过。test:discovery、test:live、test:discovery:live通过；SS429仍按降级记录，其余四来源健康。不以这些测试代替生产学习材料验收。
- 14:44 UTC来源任务只读复核仍为Shannon attempt5/queued5、KLS attempt4/queued3，重试分别14:46:56、18:33:44 UTC；当时尚未到期，不人工提前触发。本轮没有访问生产研究页面或改变用户进度。

#### 学习页信息层级审查（2026-09-08 12:43 UTC）

- 正常页面只读检查仍为release202/信息论Yilin/核心方法：17篇可用、22篇评估中、路径6篇、基础0篇、完成0/5。没有点击更新、重新生成、确认或扫描；这是页面当前显示快照，不当作刷新后的D1计数。没有重试此前被阻止的内部诊断地址。
- 确认一个可复现的P2界面问题：材料已经展示后，页面仍追加“为什么/读什么/如何决定”三块通用兜底话语。源码presentLearningGuidance把未核实的解读降为reading-task，这是正确的证据边界；但前端仅检查resources.length，导致通用占位指导仍占三块空间，容易被理解为该论文的个性化研究判断。
- 排入阶段3可用性批次，低于逐篇质量与稳定性验收：两个学习入口共用一个指导呈现组件，仅对grounded且有材料的具体指导展示三块；reading-task保留阶段目标、实际材料、来源/缺口状态和完成按钮，不删保存的目标或改变匹配/阅读进度。补中英文、空材料、grounded/reading-task及两入口一致性回归，桌面窄屏验收后再随批发布。本轮仅完成定位和实施边界，没有先行改动前端或宣称该项已完成。
- Shannon/KLS下一来源重试尚未到期，24小时窗口仍未满。现有安全诊断入口限制不绕过；无需用户重复刷新或操作，继续现有周期，不另建定时任务。

#### 同任务增量复核与实际发现函数回归（2026-09-08 10:43 UTC起）

- 按上轮六个确切job ID再次只读核对，而非仅看monitor_runs更新时间：48e4cd89的discovered_count从180到310、303047e8从80到307，均进入deduplicating；ce679f42的reviewed_count从16到59并进入deep_reviewing；2df5dc33的recommended_count从1到2，checkpoint为enriching_abstracts。f1e323ac进入finalizing，1ea51c2c进入deep_reviewing，二者计数保持12/1、8/0。所有任务仍非ready，error为空，不称整轮完成或今日新增可见论文。
- 此对照提供实际同任务增量，替代“更新时间刷新即有进展”的弱证据。尚无逐篇今日投影，recommended_count变化不作为经典材料验收；六任务与Shannon/KLS指定空间仍分开统计。
- GitHub备用自然调度新增09:03:30成功记录34207883361，因此之后的external_watchdog记录必须继续对照具体时间，不能统称独立Worker调用。24小时窗口未满，不提前关闭稳定性。
- 为上轮摘要保留修复补真实函数级回归：从实际route源文件提取discoverCandidates并转译执行，仅替换外部来源；覆盖同论文双来源相反顺序、空摘要高被引记录、第三来源失败，验证最终候选保留摘要且失败来源仍报告。测试不调用生产、不改质量阈值，防止仅测工具函数却漏掉真实路线接线。
- 专项14项、变更测试文件lint及完整测试通过；本包仅新增回归与记录，业务代码及已验证构建不变，未部署。生产逐篇经典质量和学习路径仍待验收，不为补样本触发重扫；例行推进无需用户操作。

#### 周期阶段 1：六空间恢复与同任务计数快照（2026-09-08 08:44–08:53 UTC）

- 六个旧异常空间已全部清除旧错误并持有关联任务；最后一个48e4cd89恢复到discovering_years。只关闭“六空间已重新执行”子项，不关闭整轮质量产出、24小时稳定性或整个阶段。
- 完整读取42条monitor_runs、263条ticks及49条gap jobs。06:23至08:43共有15个已完成tick，全部failed_count=0，07:13有completed_count=1；这是任务完成计数，不是论文数，未映射到确切任务前不把它计为任一验收空间完成。GitHub自然运行仍04:27；Cloudflare最近一小时直接显示六轮Cron、零错误（12个成功事件包含每轮调用与日志），对应07:53至08:43的D1记录。中间其余tick仅有D1样本，不能把trigger_source单独当独立来源证明。未人工触发或访问产品页面。
- 通过有界单行查找取得下列六个确切任务，并逐个核对任务ID与space_id；没有导出整张论文表。以下是当前持久化计数，不是本轮增量，也不是双领域学习路径漏斗。reviewed_count不擅自解释为深评篇数；work_queue_json被工具截断，无法据此取得screen/deep数组。

| 空间ID前缀 | 同一任务ID | checkpoint | discovered_count | reviewed_count | recommended_count |
| --- | --- | --- | ---: | ---: | ---: |
| 2df5dc33 | f2282641-3d69-4195-9bb8-ffb45bb6a915 | verifying_recommendations | 299 | 59 | 1 |
| f1e323ac | f3c635ba-75e7-45ab-9c68-2c17dbe87113 | verifying_recommendations | 80 | 12 | 1 |
| 1ea51c2c | 608a0329-ff00-44a0-b96d-ef1c6b017226 | verifying_recommendations | 80 | 8 | 0 |
| 48e4cd89 | 3b2227e4-bd79-4be4-bca2-6b7b7774b8f7 | discovering_years | 180 | 8 | 1 |
| ce679f42 | 0d859dd1-af22-4dae-aaed-d8d6c01efd8d | screening | 311 | 16 | 0 |
| 303047e8 | ad3f3121-8000-4dee-b15b-0c5ee62124b1 | discovering_months | 80 | 8 | 1 |

- 当前尚未逐篇核对这些任务的今日可见性，不合计为“新增四篇推荐”。后续同ID再采样才能证明具体任务增量；monitor_runs.updated_at会被停滞恢复刷新，不能单独作为质量进展证据。recovered_job_count混合过期锁释放/运行恢复/硬回收，不能当新恢复空间数。
- Shannon/KLS基础来源任务保持上次记录，下一来源重试仍分别14:46、18:33 UTC，均未到期。两确切验收空间monitor_runs虽ready，但不代表上述经典已通过质量或基础阶段已有材料。
- 本包完成六空间归属及六任务有界快照，优先级转向同任务质量推进、经典材料和24小时复核；不再追逐“剩余错误空间”这个已解决子项。上轮摘要保留修复90b5242仍未部署，网站v202；本轮未改业务代码，未重复记515测试为新运行。保持既有自动周期。

#### 周期阶段 2：修复同论文合并时摘要被覆盖（2026-09-08 07:01 UTC，未发布）

- 用户指出此前仅记录观察就结束执行过早。后续不能把每两小时自动任务说成连续执行；仍有可推进诊断时继续实现、测试，外部时间窗口单独验收。
- 本轮代码审查发现普通路线发现和经典候选合并都使用“摘要更长或被引数更高则替换”，使高被引、空摘要记录覆盖健康来源的摘要。先增加回归，实际失败显示54字符摘要被空字符串替换；这是可复现的代码问题，但没有逐篇生产投影，不能断言它就是KLS当前未通过质量评估的原因。
- 修复：同等级候选先按摘要长度选择，仅在长度相同时比较被引数；普通路线去重复用该规则。保留已经评估的受保护基线优先级、书目来源归属及最终质量门槛，不拼造摘要、不改数据库历史。测试覆盖普通/经典候选、来源先后顺序和等长情况，原基线优先级回归继续通过。
- 本轮验证：Node24下lint、构建、完整515/515测试、test:discovery、test:live、test:discovery:live通过；Crossref/OpenAlex/DataCite/arXiv健康，Semantic Scholar仍429。未把来源基准通过称为双领域生产学习闭环验收。
- 自然进展：06:56复核303047e8于06:54:18进入deep_reviewing，六空间五个恢复执行，仅48e4cd89仍旧错误；没有人工触发。此次有界事件页为按ID排序的历史数据，不含目标最新恢复尝试，不能据此断言未尝试。
- 该修复进入阶段2验证批次，暂未部署，网站仍v202；24小时观察、最后一空间恢复、经典逐篇质量和双领域实际学习材料仍未关闭。后续先补这些证据，不以本次局部修复替代完整阶段交付。

#### 周期阶段 1 续验与阶段 2 材料追踪（2026-09-08 06:45–06:55 UTC）

- 三次修正后自然运行已对应：D1 06:23:26–06:24:18、06:33:26–06:34:09、06:43:27–06:43:58 UTC，均 started=3、advanced=3、completed=0、failed=0；Cloudflare 刷新后的 Cron 事件分别在北京时间14:24:18、14:34:09、14:43:59成功。页面六条成功事件包含调用与日志，实际是三轮，不是六轮。GitHub 最新自然运行仍04:27。本轮未人工触发或重扫。
- 完整分页快照：monitor_scheduler_ticks 249→251，monitor_runs 42条保留，gap jobs 49条。六个旧异常空间中四个现为 deep_reviewing 且旧错误清除：2df5dc33、ce679f42，以及本轮新增恢复的 f1e323ac、1ea51c2c。此处仅认定恢复执行，未取得四任务逐篇质量增量，不认定新增推荐或扫描完成。
- 48e4cd89、303047e8仍是 stale_scheduler_recovery。两者所有者类型、未暂停、七天内活动、已到期、无锁、无 active_job 均符合保存错误恢复的基础筛选；尚未取得对应恢复尝试/近期告警抢占记录，不能直接归因为额度、无资格或队列饥饿。代码中的保存错误槽仅在近期事件恢复槽空闲时使用。下一采样优先核对领取与尝试，而不是增加触发频率或放宽状态。
- KLS基础任务6786d1bf自然更新：attempt 3→4、累计queued_count 2→3；Crossref仍找到原文10.1007/bf02574061，jobReviewCanonicalIds还包含10.3103/s1066369x24700622；OpenAlex记录退避约722秒，保留共享基线6项，任务诚实保持retryable/source_unavailable，next_retry_at为09-08 18:33:44 UTC。累计入队3不代表三个唯一论文，也不证明原文已通过深评/成为基础材料。
- Shannon任务仍attempt=5、累计queued_count=5，下一来源重试09-08 14:46 UTC尚未到期。不得为凑验收提前重扫。逐篇生产质量投影仍未取得；monitor_scan_jobs首个有界页被截断且不含目标新任务，不能用非目标历史行替代验收，也不继续无目的导出大表。
- 优先级保持：24小时自然观察与剩余两空间恢复为阶段1未完成项；阶段2并行核对已经找到的经典论文为何尚未成为可用材料，不再把“找到经典”作为新增检索功能。当前三次自然样本已满足次数条件，但24小时窗口未满足；09-09 06:24 UTC之后才可总结时间条件。本轮为生产证据与代码路径复核，只更新计划，不重复发布v202、不把上轮514项通过写成本轮新测试。

#### 周期阶段 1：首次自然成功（2026-09-08 06:26 UTC，稳定性未关闭）

- 06:13 UTC 新分类确认 scheduler_network_failed。依据 Cloudflare 官方 Worker-to-Worker fetch 文档，独立配置补 global_fetch_strictly_public，要求请求走公网入口；redirect 改 manual，只观察 3xx 而不跟随，不转发凭据到跳转地址。此前网络失败未保留平台原始异常，不能断言已观测到特定 1042 错误码。
- 独立 Worker 版本 549b8238-3af5-4604-861c-8e46f9e8d0e9 部署成功。修正后首次自然 tick 为 06:23:26–06:24:18 UTC，started=3、advanced=3、completed=0、failed=0；Cloudflare 同时记录 Cron 成功，生产请求 HTTP 200，GitHub 最新 schedule 仍 04:27，三方证据对应独立触发。未人工唤醒。
- 六空间中 ce679f42-d8e1-4f87-b513-33e019365835 新恢复 deep_reviewing；2df5dc33-fcc0-4869-b133-ec9244159629 继续执行，另外四个仍旧错误。恢复执行不等于深评完成或新推荐。D1 42 条 monitor_runs 保留，tick 248→249。
- 新增公网路由/禁止公开触发及 302 不跟随回归；7 项独立回归、完整 lint、构建和 514/514 测试通过。未修改产品数据库、质量门槛或模型配置；网站仍 v202。
- 学习页并行实测：现有 Edge 信息论页显示 15 可用、31 评估中、路径 6、基础 0、完成 0/5；不能与之前确切验收空间的路径 4 拼接。内部诊断导航被 Edge ERR_BLOCKED_BY_CLIENT 拒绝，已返回，未绕过。返回后出现“还没有学习路径”，生产六条路径仍保留；登记 P1 候选问题，继续核对浏览器历史恢复、实际空间及加载响应，未重建路径或宣称数据丢失。
- 后续验收按完整周期继续：累计三次成功自然事件与 24 小时观察，逐个记录其余四空间资格与任务进展；并行学习材料/返回流程，不因单次成功关闭阶段 1，不把整体 7–10 天计划标完成。
- 浏览器续验：旧 release=196 标签回退后暂显空，重新通过今日→学习导航恢复原 6 篇；在原标签载入 release=202 后，阶段选择器与空阶段文案呈现当前实现。核心方法的 CommOT 论文（DOI 10.1109/itw55543.2023.10161675）可打开正确详情，经“学习路径”返回仍选中核心方法、两篇材料、全路径6篇、完成0/5，未加入阅读。当前版本此导航链路通过；旧标签历史回退异常仍未定位，不据此编造数据库丢失。该现有浏览器空间的结果与此前指定两验收空间分开记录。
- 长周期落实：当前任务只能附一个自动任务，已将 pi-research 从30分钟只读观察合并为每2小时完整周期推进，保留自然事件验收、禁止人为凑样本和敏感数据边界。阶段1观察总结改为09-09 06:24 UTC之后的首次运行（修正后的成功窗口）；整个周期完成或取消时停止，最迟09-22总结剩余后停止。只在阶段成果/重要变化/需决定时通知，不依赖用户反复回复继续；不把自动任务建立当作业务阶段完成。

#### P1-3H：确认自然触发失败，补齐安全错误分类（2026-09-08 06:05 UTC）

- 经现有 Edge 的 Cloudflare 控制台取得直接证据：独立 Worker 在 05:33:38、05:43:38、05:53:38 UTC 三次自然执行，均为 scheduler_request_failed；概览为 3 次调用、3 次错误。日志页的 6 条错误是每次调用事件和异常各一条，不是六次执行。至此排除“Cron 完全没有触发”，但尚不能区分网络失败、HTTP 拒绝或响应解析失败。
- 根因定位到诊断缺口：wakeResearch 在同一个 catch 中将 fetch 异常、非成功 HTTP 和 JSON 解析失败全部改成 scheduler_request_failed。上轮未取得事件不能推断未触发，现以实际事件修正判断。
- 限域实现：保留一次请求、240 秒超时、禁止重定向和原权限；错误仅输出固定分类 scheduler_network_failed / scheduler_request_timeout / scheduler_http_<数字状态码> / scheduler_invalid_json，不输出响应正文、头、原始异常或凭据。没有增加模型重试、改宽健康阈值或旋转 Secret。
- 独立部署版本 9767725e-1ce7-4f3e-9332-17b4df504f15 于约 06:05 UTC 上线；Cron 仍每小时 3、13、23、33、43、53 分。仅更新独立唤醒器，Pi Research 网站仍 v202。6 项独立定时器/鉴权回归与相关 lint 通过，错误分类覆盖网络、超时、401、403、502、无效 JSON，确认不泄露上游内容且不重试。
- 验收状态：自然触发事实已确认；安全分类实现完成；请求失败的最终原因、调用成功和六空间恢复仍未关闭。既有自动验收继续读取本节并核对新版本后的自然结果，不以手动调用充当自然样本。下一 P1 根据新错误类别定位请求边界；P1-C 逐篇材料与 P2 界面任务保持原顺序，不重复泛化检索。

#### 只读替代验收落实（2026-09-08 05:54–05:58 UTC）

- 本轮绕开失效的浏览器控制通道，以生产 D1、服务日志、已部署版本和 GitHub 运行记录继续核验；未操作网页、手动唤醒、重扫、读取凭据或修改研究数据。仓库 main@8cbe4e1，保留此前计划修改与未跟踪 docs/。
- 本轮 Node 24.19.0 下 lint、生产构建、513/513 完整测试通过；test:discovery、test:live、test:discovery:live 通过。实时来源 Crossref/OpenAlex/DataCite/arXiv 健康，Semantic Scholar HTTP 429；本地健康不证明生产出口已解除退避，也不代表三篇经典已通过质量门槛。
- 完整分页读取 42 条 monitor_runs、248 条 monitor_scheduler_ticks、49 条 research_gap_discovery_jobs。最新调度仍是 05:19:45–05:20:58 的受控调用；六个目标空间仍一条 deep_reviewing、五条 stale_scheduler_recovery。独立 Worker 的在线部署版本确认为 fa22d722-0720-4b9e-a5e8-d3a1205a59c3，但部署存在不等于自然执行成功。
- 最近 GitHub 代码检查 34190512550 成功；最新自然调度仍为 04:27 的 34187030402。没有把 push 检查计入自然样本。独立 Worker 临时 tail 未取得事件或明确连接成功回执，已结束监听；不能把无输出解释为没有触发。
- 服务日志补查：05:45–05:48 的空间、研究路线、monitor 请求存在成功 200/202；05:47:49 的 monitor 调用 canceled（约 20 秒），周围请求正常。单条取消不足以认定后端宕机、模型 Key 失效或确定浏览器控制失败根因；返回成功也不证明新推荐已生成。日志查询有条数上限，不能用其中未出现 scheduler 路由证明它从未被调用。
- 数学基础任务仍 attempt=3 / queued_count=2，下一重试 09-07 20:32 UTC 已到期；信息论仍 attempt=5 / queued_count=5，下一重试 09-08 14:46 UTC 尚未到期。均为历史来源失败状态，未取得新的逐篇质量投影；累计入队数不作为唯一论文数或深评数。
- 优先级调整：P1-A 下一步先区分“独立 Cron 未触发 / 发出后请求失败 / 到达但未领取”，须取得云端触发配置与执行事件，不再将浏览器刷新当作前置条件。Cloudflare 文档说明触发变更传播最长约 15 分钟；当前已超过该窗口，不能只用初始化传播解释持续缺样本（https://developers.cloudflare.com/workers/configuration/cron-triggers/）。保留既有只读观察任务，不增加重复定时器或改宽健康阈值。
- P1-C 继续保留逐篇质量与阶段资格验收；没有证据时不改学习路径选择器、不重新泛化检索。浏览器交互、窄屏与三篇生产质量结果本轮未完成；本批只更新计划，不发布无业务变更的新版本。P2 引用清单及视觉整理仍后置。

#### 18A 第一阶段：路径归属与基础任务映射（2026-09-08）

- 已完整读取 learning_paths（6 条）及 learning_path_steps，并按空间 ID 对照 research_spaces。既有数学验收空间是 `01fbe995-9f58-4039-906e-da25514993c7` / “P1 应用数学路线恢复 0901”；路径 `5eb3b683-7c2f-41ef-b823-94d439f1c314` 仍 waiting_evidence，五阶段、两篇材料（method/frontier 各一）、基础 0、完成 0/5。不可与另一个同名 Applied Mathematics 空间混用，不能直接宣称路径丢失或前端隐藏。
- 信息论空间 `04948d10-d2e7-4d0c-9ac5-229e89abfb7e` 的路径 `12534a83-c37b-4c93-ae56-669d8061f0dd` 保留五阶段、四篇材料、基础 0、完成 0/5。这些为持久化路径资源数，不是实时质量重验后的 API 可用计数。
- 数学基础阶段 `43f0f0ae-33c8-4b5f-9aa2-0edcd0e1c8aa` → gap job `6786d1bf-1dec-42df-95c6-0414a41b4b7b`：retryable/source_unavailable、attempt=3、queued_count=2，next_retry_at=09-07 20:32 UTC（已到期）。来源记录 Crossref 找到 KLS 原文 `10.1007/bf02574061`，评估身份列表另含 `10.3103/s1066369x24700622`；OpenAlex 当时冷却。来源日志是该任务历史快照，不证明当前来源依然不可用。
- 信息论基础阶段 `c04dcf42-78fd-4d98-820d-aceba955950e` → job `e9934cd0-3f43-46b0-b032-a4c3c1c67200`：retryable/source_unavailable、attempt=5、queued_count=5，next_retry_at=09-08 14:46 UTC（尚未到期）。最新来源记录 Crossref 身份 `10.1002/j.1538-7305.1948.tb01338.x`；当前记录的 jobReviewCanonicalIds 仅一篇，不把累计 queued_count=5 当五个唯一经典文献。第二条 Shannon 原刊记录的本轮逐篇质量结果尚未取得。
- 前端 GET 按空间读取当前未 superseded 路径；当前生产每空间仅一条路径，没有证据支持本轮改为多路径选择器或改变取最新规则。真实浏览器 getTab 连续两次超时，无法确认当前网页选中空间、API 实际响应以及打开论文→返回的交互；没有读取 Cookie、伪造身份或代用户生成新路径。
- 本阶段落实：完成持久化归属与基础任务映射，修正“数学路径不存在”的过早假设；未修改业务代码或重新发布。未把历史自动测试算作本轮测试。优先级保持 P1-C：下一阶段核对已入队经典论文的逐篇质量结果与阶段资格；浏览器恢复后在确切数学验收空间完成交互，不能靠跨空间拼接或放宽质量门槛补验。

#### P1-3G 自然运行观察（2026-09-08 05:28 UTC 起）

- 本轮只读完整核对 42 条 monitor_runs、248 条 scheduler ticks：仍仅一个目标空间恢复 deep_reviewing，另五个旧错误不变；最新 tick 仍是 05:19:45 的受控调用，无新增自然样本。未手动唤醒、未浏览页面触发兜底、未重扫或重新部署。
- 最新推送检查 `34190512550` 已成功，不是自然调度；不能加入 Cron 验收样本。
- 为避免每次需要用户回复“继续”，已创建当前任务的只读验收自动任务 `pi-research`，每 30 分钟复核，有真实变化/失败/需处理事项才通知；它不调用生产 scheduler，不替代独立 Cloudflare Cron。
- 观察截至 2026-09-09 05:22 UTC 之后的首次总结复核；至少三次独立自然运行并跨 24 小时，分别判定触发、真实进展和六空间恢复。到期无论通过与否都总结并删除自动任务，不无限等待。观察期间可继续计划中的独立产品工作；自然验收仍未关闭。

#### P1-3F 独立定时启用（2026-09-08）

- 网站 v202 已于 05:19:08 UTC 发布成功，业务提交 `878fcef69a8bdee1c3ce45f040e4cb04d2eb49c9`，环境修订 6；公开范围不变。GitHub `34190098940` 全部通过（513 项测试、构建、lint、离线发现）。
- 独立专用凭据仅存于网站和独立 Worker 的 Secret 配置；没有读取或轮换原 GitHub 凭据，也没有接触模型 Key。独立权限只覆盖 scheduler，不覆盖 reliability。
- 受控调用 D1 tick：05:19:45–05:20:58 UTC，started_count=3、advanced_count=3、completed_count=0、failed_count=0，与调用返回一致。这是主动验证，不算自然 Cron 样本，也不是新论文计数。
- 六空间中 `2df5dc33-fcc0-4869-b133-ec9244159629` 已清除旧错误并进入 deep_reviewing，任务 `f2282641-3d69-4195-9bb8-ffb45bb6a915`；05:20 核对时其余五个仍旧错误。只认定一个空间恢复执行，未认定整轮完成或正式推荐。
- 独立 Worker 已部署并确认 schedule 为每小时 3、13、23、33、43、53 分；版本 `fa22d722-0720-4b9e-a5e8-d3a1205a59c3`，GitHub 备用不变。Cron 生效、自然调用、六空间恢复及 24 小时稳定性分别验收，不以部署输出替代真实运行。
- 截至 05:24 UTC 的只读复核，未收到独立 Worker 自然事件，D1 最新仍为上述受控 tick；GitHub 最新自然运行仍是 04:27。首次自然触发、三次样本及 24 小时窗口均待验收，不宣称定时运行已实测通过；本轮结束关闭临时日志监听和本地预览，不影响云端已配置 Cron。
- 优先级：接下来验证自然样本、剩余五个空间及同任务质量进度；长期样本等待不阻塞阶段 18 学习材料诊断，暂不增加新扫描能力或纯视觉改版。

#### P1-3E 专用鉴权接入（2026-09-08，实施中）

- 用户已完成 Workers 子域名初始化；复用同一独立 Worker，空定时配置部署成功，10063 阻塞解除。
- 网站新增专用唤醒凭据，仅授权 scheduler 入口，不授权 reliability 接口；原 GitHub 凭据不变。新调度凭据在内存生成，经服务 Secret 通道配置，不写源码/配置文件、不输出明文，不读取任何模型 Key。
- 网站完整构建及 513 项测试通过、lint 通过；新增实际 scheduler 路由鉴权测试覆盖双凭据、缺失、错误与独立撤销，后台恢复资格/质量门槛未改变。
- 后续顺序：发布网站鉴权支持，受控唤醒验证，再启用独立自然 Cron；部署/领取/论文进展分别验收，六空间仍不提前标完成。

#### P1-3D 账户授权与首次部署（2026-09-08 05:09 UTC，平台初始化阻塞）

- 用户已完成 Cloudflare OAuth 授权；只申请 account:read、user:read、workers_scripts:write，不追加数据库、模型或域名管理权限。
- 只读确认 `pi-research-independent-scheduler` 不存在后执行独立配置部署；Worker 脚本上传成功，但 schedules 配置阶段返回 Cloudflare 10063：账户尚未初始化 workers.dev 子域名，要求用户首次打开 Workers 控制台完成初始化。
- 属于部分成功，不标记完整部署或定时启用。配置仍为空 crons、workers_dev=false、preview_urls=false；未配置调度 Secret，未调用生产扫描，未改动网站或 GitHub。
- 当前需要用户进入 Cloudflare 的 Workers 菜单完成首次初始化，免费方案即可。随后复用刚创建的同一 Worker 继续部署，不新建第二个；再完成专用调度凭据接入、受控验证和定时启用。六空间及自然运行验收仍未完成。

#### P1-3C 独立唤醒器准备（2026-09-08，待账户连接）

- 用户已同意新增独立定时服务。已在 `infra/scheduler/` 准备独立 Cloudflare Cron Worker、禁用状态的部署配置和启用/回退说明；不纳入 Sites 网站部署，不改变数据库、页面或 GitHub 备用任务。
- 唤醒器仅 POST 固定的现有调度入口，禁止重定向、单次 240 秒超时，不连续重试；沿用网站幂等租约。无公开 HTTP 触发、无模型调用或数据库绑定；日志只允许固定错误码与任务计数，错误和缺失计数不伪装成功。
- 已验证：5 项新专项测试、完整构建及 512 项测试、lint 全部通过。仅使用合成测试凭据，没有真实调用生产扫描，六空间恢复仍未验收。
- 独立部署 dry-run 通过；明确指定独立配置，防止误用根目录 Sites 构建配置。该验证未上传或创建任何 Cloudflare 资源。
- 当前阻塞：工具中没有已连接的 Cloudflare 服务，Wrangler 只读身份检查明确未登录。按插件管理技能检查可用集成后，仍需要用户完成 Cloudflare 账户连接/浏览器登录，不能把已同意基础设施方案等同于已取得服务访问权。
- 下一步：确认目标账户和 Worker 名称无冲突，建立专用调度凭据（不读取或轮换现有凭据），验证受控调用后启用每十分钟自然触发；保留 GitHub 备用，并记录各自执行时间与真实任务增量。配置 crons 当前为空，明确尚未启用；不创建 Codex 心跳替代生产定时器。

#### P1-3B 发布后只读核对（2026-09-08 04:50 UTC）

- 已核对全部 42 条 monitor_runs；P1-3A 的六个目标空间仍为 `error / stale_scheduler_recovery`，active_job_id 均为空，原更新时间未前进。结论是尚未恢复，不是质量无合适论文。
- 调度表完整分页读取 247 条；最新 tick 开始于 04:27:10 UTC、结束于 04:28:00 UTC，external_watchdog、advanced_count=2、failed_count=0。v201 在 04:28:22 UTC 发布成功，所以上述推进发生在旧版本，不能用于新入口验收。读取时无发布后 tick；未主动访问页面、手动唤醒或生成新扫描。
- GitHub 自然运行 `34187030402`：04:27:04 创建、04:27:08 唤醒 job 开始、04:28:02 结束；上一自然运行创建于前日 23:58:48，相隔 268 分 16 秒。工作流 active，配置每十分钟且避开整点。证据将主要延迟定位到触发/工作流创建之前，而非运行器等待或扫描执行；无法凭公开元数据进一步断言平台内部原因。
- 官方文档明确 schedule 可能延迟甚至丢弃：https://docs.github.com/en/actions/how-tos/troubleshoot-workflows 。调高频率、重复发布、放宽健康阈值都不能据此保证十分钟供稿。
- 优先级调整：独立唤醒来源评估提升至 P1，先于新增扫描能力；复用现有鉴权入口、持久化租约及幂等机制，GitHub 保留备用。当前 Sites 工具未提供定时触发配置入口；新增外部服务属于基础设施范围扩展，待用户确认，不创建绕行的 Codex 心跳，不读取已有 Secret。
- 16B 与六空间恢复验收保持未完成；新版本自然运行出现后再对比同 job 计数。该外部条件不阻塞阶段 18 学习材料的独立诊断。本轮仅更新实施计划，无业务代码变更、无重新发布，未把上一轮测试结果当成本轮新增测试。

#### P1-3A 本轮落实与验收边界（2026-09-08）

- 已实现：在近期告警恢复位空闲时，为持久化的 `stale_scheduler_recovery` 错误补恢复入口；按已记录尝试轮转，失败不阻塞其他空间，不增加单轮恢复位。暂停、长期不活跃、有效锁及未来重试时间仍受保护。
- 已实现：调度开始/推进后记录同一 job 的检查点与发现、深评、推荐计数；缺失数值保留未知。只记录白名单字段，不保存凭据或租约数据；尝试记录不代表恢复成功。
- 验证通过：lint、完整构建及 507 项测试、test:live、test:discovery、test:discovery:live。SQLite 复现覆盖六个旧错误轮转、失败隔离、访问轮转及排除条件；Semantic Scholar 429 明确降级，其他四个来源健康。
- 尚未验收：生产六个空间的恢复结果，以及同一生产 job 的候选/深评增量。自然定时触发间隔过长、健康检查无法证明旧错误消失仍是开放风险，16B 不标完成。
- 发布落实：2026-09-08 04:28 UTC，v201 发布成功，业务提交 `8fde7ad2337307ef09cf8e26683ec08b74cb28ec`；公开访问范围不变。发布成功不计为六个空间恢复成功。
- GitHub 自动检查：`34187052126` 成功，Node 24 lint、构建、完整确定性测试和离线发现基准全部通过。
- 下一顺序：先核对上线后的真实任务进展和剩余错误，再处理自然触发可靠性；此后继续学习路径的阻塞点实测。暂不追加纯视觉改版或新图谱功能。

- P1-3A 恢复入口实施：六个旧错误空间均确认匿名身份、近七天活动且未暂停；普通续跑优先和近 24 小时告警筛选叠加，会使持久 stale_scheduler_recovery 失去恢复入口。新增持久错误兜底选择，仅复用空闲的事故恢复名额（每次一个），保留正常工作；选择后先追加尝试事件，失败也轮转，不能直接清除错误或标成功。仍经原有认证、暂停、费用和质量判断。追加同 job 的 start/advance 计数快照，区分调度请求与真实候选变化；缺指标保留未知，不保存凭据/租约。自然调度长间隔及健康检查观察盲区独立保留，未声称修复；本批通过后再验路径选取，不扩大功能。

#### 16B 一次性只读验收（2026-09-08 04:02 UTC 起）

- 结论：整体未通过，不能因 GitHub 变绿而关闭可靠性验收。观察窗自 09-07 03:20 UTC 超过 24 小时，取得 5 次自然 schedule 样本，样本数量要求满足；逐空间候选实际恢复/持续消费证据仍不足。
- 自然运行：`34101114217`（09-07 08:32）、`34132866860`（14:25）、`34153166144`（18:49）、`34164699486`（21:52）、`34171801017`（23:58）全部成功。均为 schedule，非手动 `34079344475`，也非 push 的代码 CI。5 次健康日志均 healthy、persistent/current critical 与 degraded 数为 0。自然运行相邻间隔约 126–353 分钟，远大于配置的十分钟；当前证据不能归因到 GitHub 延迟的具体原因。
- D1 调度：分页读至表尾；对应这 5 次自然运行的 external_watchdog tick 都完成，advanced_count 合计 7、completed_count 合计 0，均 recovered_gap，gap_minutes 125–352。advanced_count 是任务推进次数，不是新增/深评论文数；recovered_job_count 是恢复动作计数，不证明任务已成功。另见 visit_backstop 推进，不能把浏览兜底当成自然调度覆盖。最近 24 小时共 8 个 tick（5 外部自然、3 浏览兜底）；此前 03:21 手动与 03:56 浏览超时在该滚动窗外，不混入自然样本。
- D1 逐空间：完整取得 42 条 monitor_runs；以 09-01 04:02 UTC 后活动、未暂停筛选，仍有 6 个空间保留 stale_scheduler_recovery 且无 active_job_id：`2df5dc33-fcc0-4869-b133-ec9244159629`、`f1e323ac-c6a5-47c4-b44a-7ebcef5cac47`、`1ea51c2c-754a-48f4-a737-08064030c6cf`、`48e4cd89-f6fb-4897-826a-8adc59c3ce07`、`ce679f42-d8e1-4f87-b513-33e019365835`、`303047e8-51c7-4b3f-8ce9-4f5e6b83a0bf`。其中 48e4… 在 09-07 21:52 仍更新为该错误。原基线为 5 个，但缺同一基线身份全集，不能声称新增恰好 1 个故障。其他空间有 deep_reviewing 等状态，单次状态不能证明候选前进；未取得同一 job 两时点的候选/深评 checkpoint，因此这一子项未验通过。
- 告警盲区：当前代码只将近 24 小时事件中最近 90 分钟仍出现的告警计入 current/persistent；健康检查在唤醒后执行，最新心跳自然新鲜。因此“零告警”不等于旧错误已恢复，也不覆盖唤醒前数小时断档。此为代码与现场记录之间的可操作风险，不擅自改阈值或断言所有旧错误仍持续阻塞。
- 改序：P1-3 有限诊断第一步先核对这 6 个空间的实际资格、任务终态与恢复事件，以及自然调度长间隔/只在唤醒后检查的观察盲区；随后检查路径选取。保留 P1-2B 学术语义风险，不回到纯文案循环；未确认 P0 数据丢失或越权。此次只读生产，未扫描、部署、读取凭据或操作反馈；本计划仅本地修改，不提交/推送。一次性提醒执行结束后停止，不创建后续监控。

- P1-2B 第二批实施：将短主题与 nextSearchQuery 分离，路线新 JSON 保存 gapSubjectZh/En，旧记录只读回退路线名；正文统一询问当前材料的支持或反例，检索式仍独立保留。研究综合每条缺口使用自身主题/来源，不套主检索式，读写端均限定 current_materials；其他综合种类不变。无 schema 迁移、历史重写或正式证据确认。主题校验仅是格式及明显断言拦截，不是学术语义核验；assessment/opportunity/overview 仍需后续核验。验收本批后回到 P1-3 有限队列/路径诊断；不以持续文案迭代无限占用 P1，发现实际错误正式结论仍立即升级。
- P1-2B 第二批验证：Node 24 lint、构建和完整 503 项测试通过，双领域离线发现通过。新增短主题/检索式分离、路线旧记录回退、主题保存往返、多缺口不同来源与主检索保持、真实综合 readState 执行及来源过期/撤回测试。该读取测试使用模拟数据库返回，不冒充生产 D1 端到端。此次未重跑实时来源，也未触发生产模型重生成；上一批的实时成功不作为本批新成绩。生产展示验收待发布记录。
- P1-2B 第二批发布落实：v200 / `46bc61e` 成功，GitHub run `34185160225` 通过。生产应用数学桌面与 390px 文本/计数一致：1021 候选、776 评估中、2 今日、0 正式证据；信息论桌面 122/61/2/0，四条已有缺口各用自身路线名，不再展示英文检索式。未主动重扫、重生成、接受或改阅读状态；浏览可能唤醒既有后台任务，不能归因为本批新发现。旧记录回退仍较宽泛，新细主题生成及真实多缺口综合的生产验收保留；本轮不是双领域完整模型漏斗或视觉截图验收。该有限批次关闭，下一主批 P1-3 诊断队列进展与路径选取，剩余语义核验列保留项，避免连续文案改动挤占可用性主线。

- P1-2B 第一批实施：路线缺口新生成/旧记录读取及扫描消费端统一投影为待补证检索问题；不再直接使用自由生成的“领域尚未解决”正文。根因是引用身份检查不等于领域空白论证。保留检索式、来源 ID、质量门槛及既有历史，不迁移数据库或自动确认路线。代价：路线缺口暂不展示长篇推断细节；assessment/opportunity 及研究综合的事实核验仍未解决，不能将本批视为完整学术判断验收。研究综合可能有多个不同缺口，不能给它们套用同一个主检索式，单列 P1-2B 后续结构化证据范围工作，先于 P1-3 有限队列诊断，不扩大到新框架。

- P0 事故仍随时抢占；当前未确认新数据丢失或越权。
- P1-2B 第一批验证：Node 24 lint、构建、501 项完整测试通过；新增真实路线清洗/旧 JSON 读取函数执行测试，覆盖来源过滤、检索式保留、旧记录不变及无缺口不制造缺口。双领域离线/实时发现回归通过；来源探针 3 正常，Semantic Scholar 429、arXiv 超时均如实降级。本批无主动生产扫描或确认/阅读写入，不是新的完整推荐漏斗验收。发布及生产展示待核对。
- P1-2B 第一批发布落实：v199 / `766a388` 成功，GitHub run `34184516125` 通过。生产应用数学路线总览及“随机局域化与谱隙→缺口与发现”实际打开，旧缺口转换为待检索问题；工作区仍显示 7 篇候选已进入质量评估、0 正式证据，未点击扫描或确认。此次未补双空间/窄屏验收。新增问题：长英文/布尔检索式作正文可读性差；下一批 P1-2B 应用结构化的本地证据范围与简洁研究问题替代该保守投影，同时处理研究综合多缺口，不能靠加免责声明恢复未经核验的断言。其余研判正文仍有语义风险，未关闭。
- P1-1 / 20A：学习路径阶段浏览。v196 信息论基础阶段无材料、后续 4 篇已就绪，但阶段卡没有选择入口；先允许浏览并打开任意已有阶段材料，浏览状态与正式学习进度分离，不绕过先修、不自动完成。实施中。
- P1-2：昨日简报仍写“今天”、多批待核对数字重复、局部证据缺失推导领域空白；修正状态投影及学术表述边界，不仅改提示。
- P1-2A 实施中：根因是保存时的相对日期直接用于跨日显示，以及同日 watchlist 合并历史后仍被当作同一批状态。采用只读展示投影：旧简报按自身日期呈现，状态仅来自自身最后保存批次的结构化指标，显式标注保存时状态；不借当前扫描数字，不改历史简报、推荐或待核对记录。缺指标不推断为零。本批先验证日期/状态，学术结论边界 P1-2B 单独验收，不能因此关闭整个 P1-2。
- P1-2A 落实：v198 / `828bccb` 发布；Node 24 lint、构建、498 项测试、双领域离线发现通过，GitHub run `34183618813` 成功。新增跨日中英文、结构化单批状态、零值/缺字段和原对象不变测试；保留原始 watchlist，仅不将其多批数字作为当前状态呈现。无数据库/API 变更、无主动重扫或阅读/接受写入；本批未重跑实时来源测试。
- P1-2A 生产核对：两个空间均已自然生成 09-08 简报，不能再使用 09-07/09-05 的旧快照声称跨日已验。信息论保存简报 47 初筛/14 深评/0 入选，10 待核对；应用数学 302 候选/58 初筛/10 深评/1 入选，2 延后重试。当前运行任务独立显示，不混入保存状态；这是现场快照，不是本轮新触发扫描的端到端成绩。跨日逻辑自动测试通过，生产跨日样本待补；旧提醒/周报的相对日期及分享快照仍未纳入本批。下一 P1-2B 核对学术判断的证据范围，不能以引用身份合法或降置信度替代领域空白论证。
- P1-3：有限诊断共享队列与路径选取。双领域路线显示 122/1020 候选、68/815 评估中；单次快照不证明卡死。应用数学当前空间及随机局域化路线入口无路径，需核对与旧验收记录的范围差异。三篇经典文献诊断保留但不阻塞独立可用性修复。
- P1-4：既有阅读反馈→路线→下一轮发现闭环验收；P2 引用清单、窄屏、详情标签及演示；P3 新来源、新图谱和大重构暂缓。
- 本次实测边界：生产双空间导航、今日到论文、路线到学习、论文库搜索；390px 应用数学路线计数一致。未主动重扫、接受或改阅读记录。lint、493 项测试通过；实时发现双领域通过，来源探针 4 正常、arXiv 超时降级。不能作为新一轮完整生产扫描/反馈闭环验收。
- 20A 本批验收：阶段可键盘选择；当前阶段缺材料时后续论文仍可打开；查看不写进度，非当前待学阶段不提供完成动作；空间/路径切换不串选择；返回论文后保留查看位置。完成后记录落实与剩余风险。
- 20A 实现落实：独立阶段浏览导航与本地选择状态，按空间/路径 ID 隔离；顶部进度保持原有当前阶段，选中后续阶段只渲染其已有材料。非当前未完成阶段禁用完成操作，已完成阶段仍可恢复。空材料阶段删除泛化三段说明；保留真实缺失和重试状态。未修改 API、质量门槛、数据库或先修状态。构建及完整 496 项测试通过；新增组件回调/渲染、选择隔离和完成资格测试。生产点击验收待发布后记录；本批不关闭应用数学路径选取、经典补证和整体学习效果验收。
- 20A 第一批已发布验收：v197 / `4d1b73e`；lint、496 项完整测试、双领域离线发现通过。线上信息论从基础缺失（0/5，4 篇材料）键盘选择核心方法，打开真实论文 `10.1109/tit.2022.3194725`，阅读状态仍未加入阅读；返回仍选中核心方法，当前进度仍基础文献、0/5，完成按钮禁用。390px 导航、选中态和同篇材料可读；随后恢复视口。未接受、完成或保存笔记；本轮无主动实时检索、无新增模型调用测试，不能声称经典补齐或双领域完整学习闭环完成。
- 20A 后续与改序落实：本次“已有阶段材料不可访问”断点关闭；旧高级图谱内嵌学习展示仍单列后续，应用数学路径未显示仍属 P1-3。下一批执行 P1-2 日期/批次与学术表达边界，不回到无限诊断。窄屏摘要区偏高、已有材料仍有泛化学习指导登记 P2，先于纯装饰但不抢占 P1。

每次新增任务必须先更新此表，写明用户影响、证据、成本/风险及与旧任务的顺序；不以“最新提到”自动替换旧的高优先项。阶段编号是历史编号，不代表优先级。

| 优先级 | 工作项 | 判断依据与处理顺序 | 状态 |
| --- | --- | --- | --- |
| P0（当前未复现，出现即抢占） | 数据丢失、越权、核心 API 持续故障或错误学术结论进入正式证据 | 旧 500/503 记录不能仅靠首页 200 关闭；按真实接口与受影响范围判断。关系标反、错误讲解至少 P1，进入正式决策且影响严重时升 P0。 | 保留风险检查，不宣称当前存在新事故 |
| P1-A，先建立防回归保障 | 16：v193 持续运行验收与确定性 CI | v193 手动运行及 D1 均确认两空间启动/推进；自然定时、多空间恢复尚未关闭。独立代码 CI 已首次远端通过，不修改生产告警门槛。代码正确、调度有心跳、候选实际前进需分别验证。 | 16A 完成；16B 已安排约 24 小时后只读复核 |
| P1-B，第一批用户可见修复 | 17A：空间弹窗遮挡、模型错误归因；17B：今日计数/进度口径 | 17A/B 已修复并通过自动测试和本地三宽度计数验证；200% 缩放及生产双领域复核另保留待验。17B 修正混合漏斗、无论文占位及来源分组身份。 | 17A 已发布 v194；17B 已发布 v195，保留补验 |
| P1-C，主要产品投入 | 18 / 13B-2E-3：原始文献逐篇去向与可用学习材料 | Shannon 两条原刊记录、KLS 原文已入共享队列；最近一次双领域验收的基础材料仍 0。检索、摘要补证、筛选/深评、阶段匹配各有独立完成条件；外部证据不足时有明确待补结论和合格桥接阅读，不无期限扩大扫描。 | 承接既有任务，未关闭 |
| P1-D，下一项核心能力验收 | 19：研究路线—阅读反馈—下一轮发现的实际效果 | 相关模块与自动测试已有实现；现有记录不足以证明这两条真实路线的反馈完整改变了下一轮发现。先验证既有能力，按实际缺口修复，避免重复建设。 | 待证据链验收；不预设代码缺失 |
| P2-A，功能辨认优先于美化 | 17C / 15B-1：引用清单 | 来源与年份挤在箭头列、标题截断的旧问题仍保留。两端论文、引用方向、来源需能独立辨认和点击；若复核发现语义反向，立即升 P1。 | 已定位；安排有限批次修复，不抢占事故 |
| P2-B | 20：逐页任务验收、演示/正式空间衔接 | 15B-2 承接已有页面整理；以入口、返回、状态和实际任务通过为标准。历史局部窄屏检查不替代本轮全路径验收。 | 待执行，不重新设计所有页面 |
| P2-C，随修改渐进处理 | 性能测量、测试质量和局部代码边界 | 前端主文件约 517 KB、扫描入口约 511 KB、全局 CSS 约 442 KB；体积是维护风险信号，不能直接证明性能故障。先测接口/渲染，再围绕本轮改动提取组件与状态逻辑。 | 尚无新性能基准，不先做全面拆分 |
| P3 | 新增图谱模式、更多来源、装饰改版与大重构 | 当前短板集中在结果可用性、状态理解及证据闭环；保留必要缺陷修复，不先扩大功能面。 | 暂缓 |

本次改序：旧 P2 的空间切换和核心状态歧义提升为 P1-B；13B 仍是 P1 主线，但外部摘要等待不再阻塞所有界面修复。15A/15C 配色、标签及 13B-1 讲解防护维持“实现已发布”，不重复列为下一轮工作。扫描新增能力后置，既有扫描链路的可靠性问题仍优先。

新增任务只可在有更高用户影响、故障证据或明确用户改序时插队；必须同步写出被延后项，禁止静默删除未完成验收。

中途发现的问题先登记“证据 → 任务影响 → 优先级 → 成本/风险 → 验收条件”，再决定是否抢占。关系标反、错误学术结论等可信度问题至少列 P1；数据丢失、核心流程无法继续列 P0；局部拥挤但任务可继续列 P2；仅装饰不满意列 P3。是否升级由影响与复现证据决定，不由截图是否显眼决定。

## 全局评估：功能已有程度与下一轮投入

以下是基于当前代码、测试和最近验收记录的粗略工程评分，满分 10 分；不是用户调研分、自动基准分或线上 SLA。已有实现但真实结果未验收的能力扣分。优先级同时考虑主流程影响、证据和成本，不按最低分机械排序。本轮没有重新扫描生产论文，也没有把旧快照写成当前实时统计。

| 功能面 | 成熟度 / 10 | 当前判断 | 下一步 |
| --- | --- | --- | --- |
| 多来源论文发现 | 8 | 来源、经典检索、退避和持久化覆盖已具备；继续扩来源的边际价值较低 | 保持门槛和健康来源接续，按确切缺口修复 |
| 共享质量评估 | 7 | 入队、续跑、公平接续有运行时测试；指定基础候选的最终去向未全核实 | P1-A/C：检查实际进度、逐篇结果及耗时 |
| 后台调度与告警 | 6 | v193 已修复已知恢复问题；一次手动成功不足以证明长期稳定 | P1-A：自然运行、多空间覆盖、真实恢复 |
| 今日与论文详情 | 7 | 阅读主流程已有；累计、主列表、批次进度混用仍易误解 | P1-B：统一范围与状态，保留历史 |
| 论文库与历史 | 8 | 保存/去重/重扫保留已有多轮证据与隔离测试 | P2：抽样跨重扫、反馈状态和检索验证 |
| 研究线索与路线工作区 | 7 | 决策、缺口、演进、待确认证据已有；实际增益还需闭环证明 | P1-D：一个问题→相关证据→确认变化→新发现 |
| 学习路径 | 5 | 可生成、有材料匹配和讲解防护；两领域基础仍待补，实际学习闭环未关闭 | P1-C：先交付可读材料及诚实的缺失状态 |
| 研究记忆与个性化反馈 | 7 | 显式/推断偏好与反馈入口已有代码和测试 | P1-D：核实生效、撤销和下一轮差异，不再新增一套机制 |
| 引用网络与研究综合 | 7 | 关系类别及综合判断已有；引用清单辨认不足 | P2-A：可读性与语义核验；新图谱模式 P3 |
| 全局交互与视觉 | 6 | 已多轮精简；仍有操作遮挡、误报、计数歧义和样式覆盖 | P1-B 优先入口/状态，P2-B 完成逐页任务 |
| 演示、分享与首次使用 | 7 | 已有只读演示和独立空间入口；最近版本端到端衔接未重验 | P2-B：让用户清楚身处何种空间、下一动作去哪里 |
| 工程保障与维护性 | 6 | 已有 480 项测试的上轮通过记录；缺仓库 push/PR 自动测试，主文件仍大 | P1-A 补 CI，P2-C 测量与局部提取 |

近期主要功能投入转向研究路线和学习路径的可用结果。建议下一轮工作量大致分配：研究/学习闭环 45%、可靠性与回归 25%、操作和信息整理 25%、性能/维护性测量 5%；它是排期参考，不是运行时费用限制。发现 P0 时重新分配并记录。

## 下一轮执行批次与验收

时间为工程工作量估计，来源等待、自然运行观察和需要真实用户反馈的时间另计。一次只推进一个主要修改批次；被外部条件阻塞的验收明确挂起后，可以执行下一个独立批次。以下是批次范围与验收要求，实际完成情况见阶段实施记录。

### 16：可靠性收尾与自动回归保障 — 先执行，约 0.5–1 天 + 自然观察窗口

- 先复核 v193 的自然调度记录与受影响空间：分别记录唤醒、成功领取、实际任务进度和事故恢复。至少覆盖 24 小时与 3 次自然定时运行；这是最小观察样本，不是十分钟准点承诺或长期 SLA。
- 对照现有 45 分钟心跳新鲜度阈值检查实际间隔；若发生超阈值且无人访问时无法续跑，记录真实阻断原因并处理调度可靠性，不能只提高 cron 频率或放宽健康阈值。观察等待不阻塞阶段 17。
- 多空间验收记录每个符合服务资格且到期的任务最后进度、等待原因和恢复结果。源码健康查询只计最近 90 分钟仍被观察到的告警：必须区分“恢复成功”与“未重新观察到”，不能仅凭聚合 healthy 推断所有空间恢复。这是待验风险，不是本轮已经复现的新缺陷。
- 新增 Node 24 的 push/PR 确定性 CI：安装锁文件依赖、lint、`npm test`、离线发现基准。`npm test` 自带构建，顺序运行，避免与另一构建竞争 dist。外部来源探针和生产健康保留独立工作流/结果，不将来源 429 包装为代码编译失败。
- 验收：代码 CI 可追溯到提交；自然调度的间隔/多空间进度有证据；持续事故有恢复事件或明确未关闭原因。不能以关闭邮件、删除检查或一次人工触发绿色替代完成。
- 风险与范围：工作流新增会增加 CI 用量；生产只做授权范围内的核实，凭据只由已有工作流 secret 引用，不读取或输出。若需要更换定时基础设施，先单列成本与迁移方案，不在本批默默迁移。

### 17：日常使用障碍 — 紧接执行，约 1–2 天

- 17A / P1-B：修复空间选择与模型设置弹窗的侧栏遮挡、焦点/关闭/返回问题；将认证失败、余额不足、限流和网络故障的用户状态正确分开。沿用现有界面，不要求用户重新提供 Key 来测试错误分支。
- 17B / P1-B：为今日每个数字核定空间、批次、日期与状态范围；合并 15B-3 及“旧简报/新续跑/100%”问题。以一个结果状态和一个主动作表达当前任务，详细漏斗保留次级查看，数据不为显示整齐而截掉或删除。
- 17C / P2-A：修复引用清单两端标题、年份、数据库来源的布局；来源移出箭头窄列，完整标题可展开或换行，两端可访问。明确“被引工作→引用它的论文”，不改变数据库关系方向、不混入阅读顺序或 Pi 推断。该项可独立交付，不延长 P1 修复批次。
- 验收：1440、700、390 像素宽度及键盘操作；相关局部 200% 缩放；同一数据快照桌面/窄屏计数一致，后台变化单独标记。用渲染/交互回归覆盖实际组件，不能只检查 CSS 字符串或主题对比色。
- 范围与风险：不全面换设计系统；修改共享弹窗与状态分类时核对所有调用点。正式数据历史、论文排序和质量门槛不改。

### 18：经典证据与学习路径可用性 — 主功能批次，约 2–3 天

- 承接 13B-2E-3；先查已有确切 DOI 与任务映射，不重新开展泛化搜文。内部验收表逐篇记录身份、摘要来源/可得性、入队、筛选、深评、最终质量结果、今日状态、阶段资格及时间；缺字段写“未取得”，不拼接不同时点漏斗。
- 对历史 2/4 篇任务补齐逐篇映射；通过实际接口与 D1 读取核对，不把最新入库排序当任务归属。若现有只读能力不足，记录阻断并选择经过审查的最小内部诊断手段，不公开新的数据查询入口或审计台。
- 分支验收：未评估→定位资格/排队原因；来源退避→保留下一次可试时间；无摘要→健康来源补取摘要或可用结构化证据；不合格→保留原因；阶段不符→保留候选或补充阅读。没有足够证据时不入今日、不冒充主要材料。
- 原始论文仍缺证据时，允许另行检索有充分摘要依据、通过共享质量评估且匹配阶段目标的后续解释材料。明确它是后续解释/桥接阅读，不能声称原始文献已补齐，也不能把配角材料改标签充数。先复用已有阶段匹配和补充阅读能力。
- 信息论和应用数学各验一次“打开路径→找到当前可读材料或明确缺失原因→打开论文→返回”；学术讲解只来自合格证据。实际阅读完成与正式路线确认仍由用户作出，隔离测试验证这些动作的状态效果。
- 完成分开记录：逐篇去向验收可关闭；基础材料真正通过资格才标记补齐；真实学习效果要有用户阅读反馈。若外部证据在本批仍不可得，保留待补项、重试资格和已有路径，结束本批诊断并推进其他工作，不把“继续运行”当作交付。

#### 架构实施顺序（2026-09-08，用户确认）

| 顺序 | 范围与方法 | 验收与边界 |
| --- | --- | --- |
| P1 / 18B-1 | 逐篇只读诊断投影，关联论文、证据、来源、近期冻结批次 | 当前空间 + 最多 8 个确切身份；未知不推断为未评估；不读 Key/笔记，不公开 SQL，不增加审计页 |
| P1 / 18B-2 | 显式状态与原因，分离执行、证据、推荐三维 | 复用现有状态机与检查点，约束转换；轻量追加决策记录，不做全面事件溯源迁移 |
| P1 / 18B-3 | 公平调度验证与来源感知补证缓存 | 在现有分组/老化机制上复现等待、退避、旧核对竞争；不降低质量门槛、不绕过来源退避；区分请求失败和成功但无摘要 |
| P2 / 18C | 有约束的先修关系与桥接阅读 | 复用学习阶段匹配；LLM 提议、程序检查证据与完成条件；后续解释不能冒充原始文献 |
| P2 / 19 | 质量过线后的多样性重排、反馈驱动预算 | 同候选池验证重复减少、路线覆盖和反馈效果；不因多样性强制批准论文 |
| P3 / 暂缓 | 向量检索、独立图数据库、多 Agent 框架、微服务迁移 | 先有实际规模/检索收益证据；保留 Workers + D1 + React 和唯一共享质量队列 |

- 18B-1 本批先落地内部可调用的只读查询与隔离 D1 测试，不把鉴权入口和生产调用混进未经审查的公开 API。阶段完成分别记录“查询能力已验证”和“生产三篇结果已取得”；前者不能替代后者。其后才依据逐篇原因修改调度或补证行为。
- 18B-1a 落实：新增 `lib/paper-diagnostic.mjs`，限定非空空间和最多 8 个确切 canonical ID，参数绑定读取摘要字符数、已有评估/核对状态、历史推荐标记、学习来源数量与最近 5 个批次的候选/筛选/深评选择。只返回投影，不返回摘要正文、批次原始 JSON、笔记或凭据；无 HTTP 入口、无数据库迁移、无任务触发。调用方仍须先授权空间，内部函数不能冒充鉴权机制。
- 18B-1a 验收：3 项新增测试，包括生产 bootstrap schema 兼容性和真实隔离 D1 查询；验证注入输入不能扩大结果、空间隔离、缺失 insight/损坏 JSON 显示未知、当前窗口不代表完整历史、重复读不改记录。lint、生产构建、完整 491 项测试、双领域离线发现基准通过。未修改在线检索逻辑，本批未跑实时检索或调用模型；未部署仅供内部使用、尚未连接运行入口的函数。
- 18B-1b 落实中（2026-09-08）：新增受限 GET `/api/internal/paper-diagnostic`，每次最多 8 个确切身份，只读当前拥有的空间。权限方案调整：现有管理员接口需要后台调度凭据，不把该凭据带入浏览器；复用现有匿名工作区 Cookie 的所有权边界，与论文库一致，并非管理员认证或新增账号登录保障。无凭据 401、非拥有/不存在空间统一 404、非法参数 400、存储故障仅返回通用 503；全部禁缓存，不返回正文、原始批次或凭据，不开放任意 SQL，也无用户审计页。
- 18B-1b 本地验收：生产 Worker + 隔离 D1 的接口回归验证上述权限、重复只读不改表/记录、POST 不允许、查询不会唤起扫描；完整 493 项测试与两个领域离线发现基准通过。生产三篇投影尚待发布后读取；不能把本地测试作为生产结果。近期批次之外的完整历史、筛选原始理由、学习阶段资格仍未纳入当前投影；不得把 sourceCount 或 everRecommended 当作当前学习材料通过证明。阶段 18B-1 保持实施中。
- 18B-1b 发布落实：v196（业务提交 `ad5ccb1`）发布成功；本地 lint、构建、493 项测试、双领域离线基准通过，GitHub `Pi code checks` run `34180912464` 成功。生产无 Cookie 请求返回 401 和 `private, no-store`。没有调用模型或实时检索，本批未跑 test:live / test:discovery:live，不将离线基准标成真实发现闭环。
- 18B-1b 阻断与计划调整：浏览器直接打开所属空间的诊断 URL 被客户端拦截（ERR_BLOCKED_BY_CLIENT），随后 DOM 仍为原论文详情；不能宣称取得三篇生产投影。现有页面只确认七月 Shannon 仍显示等待质量评审、未加入阅读，信息论论文库 1089 篇；未触发重扫、接受或阅读操作。下一 P1 是选择不导出凭据、同源受控的诊断读取方式并完成三篇逐项核对；不为了通过验收开放跨空间查询、新增用户审计台或把浏览器 Cookie 搬入终端。阶段 18B-1 继续保持未完成；18B-2 调度/补证修改须等原因证据，不在本批猜测后实施。

#### 阶段 18A 诊断记录（2026-09-07，未关闭阶段 18）

- 基线 main@92ef43a / v195，原未跟踪 docs/ 保留；本批只做生产记录、页面和公共摘要元数据核查，没有修改业务或发布新版本。
- D1 精确任务：Shannon `e9934cd0-3f43-46b0-b032-a4c3c1c67200` 为 retryable、尝试 4、queued_count 5；最新 source_status 的确切原文是 `doi:10.1002/j.1538-7305.1948.tb01338.x`，下一重试 2026-09-07 15:04:03 UTC，OpenAlex 退避。该 5 是任务累计计数，不能冒充 5 篇 Shannon 原文或通过数。KLS `6786d1bf-1dec-42df-95c6-0414a41b4b7b` 为 retryable、尝试 2、queued_count 1，明确命中 `doi:10.1007/bf02574061`；最新任务身份列表另有 `doi:10.3103/s1066369x24700622`，并非原文。旧 2/4 篇任务仍在，旧记录没有逐篇身份，未补全历史映射。
- 公共来源限定探针（Node 24，正常环境代理，无 Key、无全文）：三个确切 DOI 的 Crossref 均 HTTP 200、摘要空。OpenAlex 单 DOI 和与业务相同的批量 DOI 查询均 HTTP 200，Shannon 七月/十月原刊均有 abstract_inverted_index（分别 63/85 个不同词项，不是摘要总词数），KLS 摘要空。Semantic Scholar 的 KLS HTTP 200、摘要空，Shannon 两条 HTTP 404。以上是本机观察，不证明生产 IP 已解除退避，也不证明数据库已有摘要或文本足以支撑具体阶段。
- 线上逐篇：信息论全部发现 1089；按完整题名检索，七月原刊为“等待质量评审”，十月原刊为“发现线索”，均未加入阅读。打开七月详情确认 DOI `10.1002/j.1538-7305.1948.tb01338.x`，相关分 100，但没有最终评审结果；不可把相关分当深评通过。只打开详情，未接受/忽略/保存笔记/确认阅读。
- 代码已核实：摘要补取顺序为 Semantic Scholar 后接 OpenAlex，保留更长的已有摘要；筛选候选 SQL 本身无五年出版日期下界。还未取得这三篇在生产的完整 analysis_source、screening_reason、批次选择和摘要存储值，不能据此认定漏选根因或绕过退避保护。现有 D1 工具只允许分页，无按 DOI 过滤，未全量下载论文表。
- 下一 P1 收窄为：核对 Shannon 七月是否被当前冻结队列覆盖、十月的候选资格与来源映射，再核对 KLS 的具体评估状态。若页面无法提供内部字段，实施经过测试的最小内部只读诊断投影，限定当前空间和给定 DOI，不公开任意 SQL 或用户审计页。证据不足的 KLS 保留待补，不强制设为主要材料；有摘要的 Shannon 仍需共享质量评估及阶段匹配。
- 17B 补验取得部分生产证据：信息论今日主序 4、备选 2、导航 6；保存简报本轮 360/47/14/3，日累计 4，界面明确显示 4/4。未完成生产双领域三宽度及 200% 放大验收。生产页面读取常需 40–60 秒且发生超时，属于验收工具限制，不能据此认定产品接口宕机。
- 新发现按 P2 登记，不抢占本项：原始论文详情侧栏将 1948 年论文标为“近 5 年”，并把出版日期叫“加入时间”；这些标签语义需要后续清理，不改存储年份或历史状态来配合标签。

### 19：研究路线与个性化效果 — 约 2–3 天，依赖共享队列可运行

- 选择已有双领域路线，验证当前问题→证据缺口→发现来源→质量结果→今日→阅读/反馈→下轮查询与排序→待确认路线变化的关联。
- 先读已有反馈、版本和来源记录；不足的部分用隔离空间/固定候选池复现。验证显式偏好优先于推断、撤销反馈生效、忽略不重新冒出、同一候选条件下查询/排序变化可解释。没有真实反馈样本，不宣称个性化效果提高。
- 优先改善已有路线工作区的“下一步”是否可执行、能否直达相关材料；不再增加一组管理面板或重新实现已存在的演进机制。
- 验收：双领域至少各有一条完整可追溯链；分开报告确定性机制验证和真实使用结果；来源清晰，未确认建议不更新正式路线；所有历史处理状态保留。必要修复按实际断点拆批。

### 20：逐页收尾与性能测量 — 约 1–2 天，后于主要闭环

- 顺序：今日/详情→路线总览与工作区→学习→论文库→记忆→图谱/综合→演示/分享/首次使用→剩余弹窗。每页只围绕一个主要任务检查入口、返回、空态、加载、失败恢复、长标题及窄屏。
- 重设计仅用于已证实任务层级有问题的局部；已完成整理的页面以小幅清理为主。继续克制蓝灰配色、小圆角或直角标识、去掉不必要的宣传与内部运行措辞，保留必要的失败与证据限制说明。
- 抽样记录首屏可操作时间、主要 API 耗时与响应大小、重复请求；有足够样本才报告 P50/P95，并注明样本量。确认瓶颈后优化缓存/分页/组件更新；修改触及的单体逻辑逐步提取，不把文件大等同于线上慢。
- 验收记录沿用现有计划，不单开另一套 UI 计划。以任务能否完成及误解是否消除为标准，不用新增截图数、组件数或发布号评价进展。

## 阶段记录和发布门槛

- 每个批次开始写“范围、当前证据、预计成本、验收条件”；结束写“实际修改、测试、真实结果、未完成项、下次顺序”。实施、发布、真实验收分别标明；没有新证据的挂起项不反复记成新进展。
- 无 P0 时按 16→17A/B→18→19→20 推进；16 的自然观察、18 的来源等待与独立任务错开。17C 在主要操作修复后独立安排，若发现关系语义错误升 P1 抢先。
- 每次代码发布运行 Node 24 lint、完整测试；发现/质量/调度相关改动另运行来源、离线与实时发现基准，报告来源降级。页面改动补真实交互和窄屏验收，业务链路变更补实际 Worker/D1 行为测试及双领域抽样。共享状态改变需要检查历史保留。
- 新增测试优先执行真实函数、组件交互或 Worker/D1 行为；现有源码契约测试可保留，但不能代替上述验收。
- 只有计划文档修改时检查 diff 和任务承接，不重复构建或发布网站。用户已授权后续自主发布；该授权不等于本次评估要部署、改凭据或模拟用户确认。

## 阶段 16 起始证据：v193 与本次复核

- v193 已发布实现：持续 critical 空间获得独立恢复候选位，历史 recovered gap 不再持续冒充当前心跳故障，GitHub 调度表达式调整为每十分钟。源码提交 `337cd4c`；上轮记录为 lint、构建和 480 项测试通过。本次没有重跑这些测试。
- 本次 GitHub 只读核对：修复后 [运行 34079344475](https://github.com/LittlePyx/pi-research/actions/runs/34079344475) 于 2026-09-07 03:20:58 UTC 手动触发并成功；此前 [自然运行 34071591716](https://github.com/LittlePyx/pi-research/actions/runs/34071591716) 及更早多次运行失败。此次查询尚无更新的自然运行成功证据，不能宣称后续邮件一定停止或所有空间已稳定。
- 工作流现状：仅 `.github/workflows/pi-background-scheduler.yml`，包含生产唤醒与健康检查，不包含 push/PR 代码测试。需新增确定性 CI，不能把既有 run failed 当作编译失败。
- 论文/学习数据沿用 v192 的最后独立核对：信息论原刊记录 2 条、数学 KLS 原文 1 条已对应队列；学习主要材料分别 4/2、基础均 0、阅读完成均 0/5；论文库 1072/804。它们是 2026-09-04 的验收快照，不是 2026-09-07 的实时量，也不是同一次扫描漏斗。
- 本轮落实：完整复核交接文档、README、既有计划、相关当前代码、分支/提交及 GitHub 最近运行；更新本计划的全局评分、主动任务排序、有限批次和验收规则。未改应用代码、生产数据、凭据或部署；未跟踪 `docs/` 保持不动。

## 阶段实施记录

### 阶段 17B：今日统计与任务进度 — 已发布 v195，保留补验

- P1 根因：详情漏斗将旧简报候选/入选与新任务筛选/深评拼接，`||` 让本轮 0 回退到历史值；深评缺失还会用最多 8 篇推算。现整组使用最近任务，无任务时整组使用简报，未知显示“—”，保留真实 0。活动记录使用当时保存的正文，不再以新任务覆盖历史通知。
- 新发现提升至同批 P1：原主序以论文、signals、readingPlan 三者最大长度计数，能产生无真实论文的占位入选；缺失详情后建议索引也会错位。现只渲染实际论文、ID 去重，并按原简报索引对应建议；显示列表数量/简报记录数，三个新鲜度计数均按当前最多 6 篇计算。详情缺失不再误报“没有论文通过”。未删除简报或历史数据。
- 进度：核对阶段显示“核对中”并保留既有实际核对分数；其他活动阶段最高显示 99%，仅 ready 才允许完成值。没有检索记录的时间窗不再因总任务 ready/error 被推断为已检索。
- P2 来源标识：数据库按 source_key + channel 分组，页面原仅 sourceKey 作为 React key。现与数据库分组身份一致，两条相同来源不同通道均保留，通道保留为说明；没有合并或删除候选。
- 本地实页：信息论已有简报 1 篇、近期优质 1；最近失败任务漏斗准确为 0/0/0/0，而非借用旧简报 295/58/12/1。1440、700、390 宽度计数一致，无水平溢出。页面本地模型认证错误不是生产 Key 失效证据；未重新扫描、写入 Key、确认阅读或研究证据。
- 自动验收：新增纯函数及页面接线回归覆盖跨批次零值、缺失深评、9 篇只显示 6、缺失详情索引、ID 去重、历史输入不变、活动进度和来源身份。旧断言要求混合漏斗，已调整为同范围统计；完整回归及发布结果随后追加。
- 尚未关闭：生产双领域新口径实页与 200% 放大补验；全库逐条跨重扫保留不能由前端只读改动推定完成。16B 自然调度观察和 18 原始文献质量去向仍为 P1，不追加视觉扩展，不将本批视为这两项完成。
- 自动结果：生产构建和完整 488 项测试通过，双领域离线发现基准通过。仅修改展示与测试，无来源检索/模型/数据库写入逻辑变化，本批未重新运行实时来源与实时发现基准，不能沿用旧结果作本批实时证据。
- 发布落实：v195 于 2026-09-07 04:14:07 UTC 成功，业务提交 `58dd03cea8874d6ea069a30ac4315057f54d76bf`；最终 lint 通过，GitHub Pi code checks `34082241437` 全绿。首页和演示 HTTP 200，公开访问不变；生产浏览器读取 60 秒超时，保留上述实页补验，不能用 HTTP 成功替代。只清理本轮发布包、停止本地预览，原未跟踪 docs/ 不动。下一批优先恢复阶段 18 原始文献质量去向，16B 按已安排的自然运行观察时间执行。

### 阶段 17A：弹窗入口与模型状态归因 — 已发布 v194，保留补验

- 根因：移动侧栏 z-index 120 高于弹窗 100，入口不收起侧栏；模型探测将余额不足统一判为 invalid，网络失败回到 checking。删除浏览器凭据后的再次 GET 也未检查 HTTP 成功就采信 configured。
- 落实：弹窗层级提升至 180；空间/模型侧栏入口先收起移动导航。两种弹窗增加初始焦点、Tab/Shift-Tab 循环、Esc 关闭、背景 inert 与滚动锁定、关闭后恢复桌面入口或移动菜单焦点；保留原弹窗布局和所有业务动作。弹窗打开时不响应全局询问快捷键，避免叠开背景面板。
- 状态：区分认证失败、余额不足、限流、连接未确认；普通 workspace 认证或网络错误不作为模型 Key 失效证据。首次探测、重新检测、保存失败及删除后重新探测使用一致分类，删除后的 HTTP 错误不再被显示为未配置。没有读取、修改或提交真实 Key，也不要求用户重新设置。
- 自动验收：Node 24.19.0，lint、生产构建、483 项完整测试通过，新增 3 项覆盖错误分类/双语呈现、实际入口回调顺序以及焦点循环/关闭/恢复。焦点回归使用 DOM 行为替身，不能独立证明浏览器布局；另有下述实页证据。
- 浏览器验收：复用现有标签页验证本地版本；1440、700、390 像素下，空间与模型弹窗均正常打开、侧栏关闭、标题区域可见、无页面横向溢出。700 像素实际 Shift-Tab 回到末项、Tab 回到首项，Esc 返回移动 Menu；1440 关闭后返回模型入口。390/700 截图复核没有侧栏遮挡。没有点击更换/删除/保存凭据、切换研究空间、手动重扫或确认阅读。
- 验收边界：本地已有凭据检测呈认证失败，只代表本地连接，不能推断线上用户 Key 失效。200% 浏览器缩放的按键未被工具支持，保留未验，不用窄屏结果代替；余额/限流等状态的覆盖来自确定性测试，未故意制造生产故障。
- 计划调整：下一代码批次为 17B 今日计数/进度口径；本项的 200% 缩放补验并入后续实际交互验收。16B 的一次性自然运行复核继续保留；18 的原始文献质量去向未关闭。本批不改调度、数据库、论文历史、质量门槛或研究证据状态。
- 发布落实：源码 `5fc1c3cb74f3cc9bf370e7d18166ab9657a391b0` 已同步 GitHub 与 Sites；v194 于 2026-09-07 03:55:33 UTC 发布成功，沿用原公开访问和环境。生产首页、演示均 HTTP 200；同一浏览器标签页回访超时，后续交付返回 queued，因此不宣称线上弹窗已再次验收。已请求恢复临时 viewport 覆盖；本地三宽度结果与生产可达性分别记录。
- [GitHub 代码检查](https://github.com/LittlePyx/pi-research/actions/runs/34081192484) 全部通过，干净环境 job 约 2 分 24 秒。本次新检查没有重现 run failed；生产后台健康仍由独立的阶段 16B 验收，不能据此宣布全部恢复。
- 收尾新发现 / P2：本地今日页控制台重复提示 React key `research-route:gap` 冲突，可能导致来源项重复或遗漏；尚未定位具体列表，也未证实线上数据丢失。并入 17B 的来源/计数呈现检查，先定位真实去重身份再修渲染 key，不能删除候选来消除警告。不抢占已发布的 17A，不据此重扫生产数据。

### 阶段 16A：代码 CI 与运行基线复核 — 已完成；16B 持续运行待验

- 本批范围：新增 `.github/workflows/pi-ci.yml`，监听 main push、PR 与手动触发；Node 24、锁文件安装、lint、一次构建加产品测试、离线发现基准顺序执行。Actions 引用官方 v6 标签解析到的完整提交 SHA，仓库权限只读，不保留 checkout 凭据，不使用应用密钥或生产扫描接口。
- 生产 scheduler/health 工作流保持独立，未关闭、静音或放宽告警；README 明确两种 run failed 的含义，并将“每十分钟执行”修正为配置目标而非准点承诺。本批不改网站业务与部署版本，线上仍为 v193。
- 本地落实：使用提供的 Node 24.19.0；lint、生产构建、480 项完整测试通过（0 失败/跳过），信息论与数学离线发现基准全部过线。工作流将继续在 GitHub 的干净 Linux 环境验证 `npm ci` 与完整链路；本地安装不冒充干净安装验收。
- 2026-09-07 03:36–03:38 UTC 只读复核：完整分页读取 237 条 D1 调度记录，最新为 03:21:05–03:21:40 的外部手动唤醒，started_count=2、advanced_count=2、failed_count=0。近 24 小时共 9 条唤醒记录，该窗口各记录的 gap_minutes 为 86–309；v193 刚发布，尚不具备修复后 24 小时或三次自然运行样本。
- 对应原告警空间的持久化状态已从旧 error 进入 deep_reviewing，另一个空间处于 discovering_days；这些只证明获得执行和状态推进，不证明完成推荐。完整读取 41 条 monitor_runs 后还发现五个近七天活动、未暂停空间保留 stale_scheduler_recovery，不能把单个空间恢复扩大为所有空间健康。需续核任务资格和逐空间恢复记录，不直接清空错误或强制重扫。
- 最近 30 分钟错误窗口仅返回四次 canceled 请求，无从这些记录确认新的 D1 500；取消不等于服务端故障，也不能证明所有接口均正常。未读取 Key 或用户 Cookie，未改变阅读/正式路线确认。
- 计划调整：16A 的代码检查交付后，16B 的自然时窗与多空间恢复保持 P1 待验；不人工反复触发制造连续绿色样本。下一独立实施批次仍为 17A 的弹窗和模型错误归因，未把它记为本批完成。
- 远端落实：实现提交 `8273ae3` 已推送 main，[Pi code checks 首轮运行](https://github.com/LittlePyx/pi-research/actions/runs/34080338378) 于 2026-09-07 03:41:02 UTC 完成且全绿。干净 Linux runner 的 `npm ci`、lint、构建/产品测试、离线基准均通过，整体 job 约 2 分 20 秒。本批未增加产品测试用例，因为应用逻辑没有变化；新增保障本身已实际运行，不仅是工作流文本检查。
- 后续验收：已在当前任务创建 `pi-research-24` 一次性约 24 小时后复核，执行后暂停；仅查询自然运行与可获得的持久化证据、更新本计划，不读取凭据、不触发扫描、不部署或提交。稳定性仍未记为通过；此次安排不阻塞阶段 17A。

### 阶段 0：现状盘点与设计基线 — 已完成

落实情况：

- 识别 10 个主要工作面、5 个路线工作区子页面、3 个图谱模式和 6 个弹窗流程。
- 线上当前空间中，“今日”约 2401px、22 个主区按钮、9 个内容区；“研究路线”约 2778px、26 个主区按钮、11 个内容区。
- 确认首要问题是路线工作区内部重复，其次是路线总览卡片把详情提前展示。
- 确认 `research-app.tsx` 与 `globals.css` 已累积多轮样式覆盖；本轮以页面边界内的定向重组为主，不做会影响业务语义的大规模重写。

计划调整：

- 将原计划中的“路线工作区”和“路线总览”合并为第一批发布，因为二者共用路线状态、缺口和动作语义。
- “高级图谱”保持按需入口，本批不增加图谱能力。

### 阶段 1：路线工作区收敛 — 已完成

验收标准：

- 第一屏只突出当前研究判断、关键不确定性和一个主动作。
- 路线说明、运行状态和管理信息不重复占据主视觉。
- 五个专业子页保留，但去掉编号式仪表盘感，名称和层级更直接。
- 低频路线管理默认折叠；只有真正需要用户处理时才展开。
- 窄屏不会出现五列挤压或关键动作脱离上下文。

落实情况：

- 将“当前判断、关键不确定性、下一步”合并为一个决策面，主要动作与其原因保持在同一屏。
- 路线说明改为按需展开，页头只常驻路线名称、运行状态和证据计数。
- 五个专业子页去掉编号，改为直接命名的导航；窄屏使用横向滚动，不再压缩成五列。
- 来源或模型降级仍显示真实状态和恢复动作，但不再自动展开整套路线管理；待确认版本和用户主动暂停仍自动展开。
- 增加源码级回归断言，防止重新引入编号导航、重复决策区或降级时自动撑开管理面板。

### 阶段 2：路线总览压缩 — 已完成

验收标准：

- 每张路线卡只展示名称、定位、健康状态、一句摘要和当前最重要动作。
- 完整研判与完整证据缺口只在路线工作区展示。
- 去掉页面底部与页头重复的高级图谱介绍。
- 降级路线仍能看到真实状态与重试入口。

落实情况：

- 路线卡常驻内容收敛为路线名称、角色分组、热度、健康状态、一句路线摘要和证据计数。
- 完整综合研判、深度/近期/置信指标和最近变化不再提前出现在总览卡；完整内容仍保留在工作区。
- 证据缺口只显示一行提示并直达“缺口与发现”；健康路线只保留“进入工作区”，仅待补路线显示恢复动作。
- 删除页面底部重复的高级图谱宣传区，页头入口和按需展开的辅助视图仍保留。
- 阶段 1–2 的 12 项定向回归测试通过。

### 阶段 3：今日页面重排 — 已完成

验收标准：

- 第一屏只回答“今天读什么”和“为什么”。
- 扫描范围、运行记录、路线变化降为次级可展开内容。
- “入选”“必读”“更多推荐”的数量语言不再互相造成误解。

落实情况：

- 删除顶部三张数字导航卡，首屏直接进入今日研究判断和按优先级排列的入选论文。
- 将候选、筛选、深评、入选漏斗和三个时间窗移入“扫描范围与来源”，扫描动作与异常仍保持可见。
- “今日入选”只使用当日简报计数；其他已通过筛选的论文统一称为“备选阅读”，默认折叠。
- 研究路线变化改为折叠摘要，保留更新数量和直达路线入口。
- 今日相关 21 项定向回归测试通过，桌面和窄屏继续使用相同数据计数。

### 阶段 4：论文详情与论文库 — 已完成

验收标准：

- 论文详情合并重复的适读理由、路线影响和问题影响。
- 页头保留最多两个主要动作，其余收入次级操作区。
- 论文库改为紧凑浏览列表，保留所有过滤、阅读状态、反馈和导出能力。

落实情况：

- 论文库保留搜索、排序和四类状态计数；导出格式与单篇管理动作改为按需展开。
- 列表卡缩短留白，适读理由限制为两行；阅读状态、接受、稍后、不相关、退回和分享均未删除。
- 论文详情页头只保留“适合”和“打开原文”两个主要动作，保存、稍后、不相关和分享收入“更多”。
- 将适读理由、路线影响和研究问题影响合并为一个研究相关性区块；问题影响按需展开。
- 发现来源与评审指标移入折叠记录；阅读状态、笔记、问 Pi 和来源信息仍完整保留。
- 阶段 4 的 19 项相关回归测试通过。

### 阶段 5：研究记忆与资料导入 — 已完成

验收标准：

- 研究画像、阅读记忆、偏好推断分层展示。
- 资料导入安全说明只在需要作决定的位置出现一次。
- 用户明确内容和 Pi 推断继续分开且可撤销。

落实情况：

- 页面首要层级收敛为已确认研究画像、明确偏好/Pi 推断和阅读记忆，不再重复页头说明与导入动作。
- 明确偏好与 Pi 推断并列且标签更直接，Pi 推断仍可逐条停用。
- 阅读记忆改为论文标题索引，结论、方法、问题和连接按单篇展开。
- 兴趣、知识、资料来源、开放问题和研究机会收入“研究画像详情”，所有原有数据仍可查看。
- 上传安全说明从记忆页移除，只在导入弹窗的提交决策位置展示，并保留用户确认勾选。

### 阶段 6：学习路径、演示、分享与弹窗 — 已完成

验收标准：

- 保留学习路径当前阶段优先的结构，折叠未来阶段的说明。
- 删除演示页和分享页中的宣传腔、界面旁白与重复标题。
- 统一空间、来源、模型、反馈和询问弹窗的标题与操作层级。

落实情况：

- 学习路径去掉版本号和“证据驱动”等系统旁白，保留当前路径、当前阶段、可用论文与质量等待状态。
- 当前学习阶段继续完整展示；未来阶段只保留紧凑路线图，移除重复的返回路线页脚。
- 演示页只展开当前学习阶段说明，其余阶段保留标题和论文数；底部入口改为直接的“开始使用”。
- 分享快照去掉模型宣传标签和重复生成说明，保留冻结状态、论文内容、适读理由和原文链接。
- 模型、反馈与导入弹窗标题改为任务名称；导入进度只描述正在处理的内容。
- 阶段 6 的 19 项相关回归测试通过。

### 阶段 7：全局一致性、窄屏与发布核验 — 已完成

验收标准：

- 桌面端和窄屏的关键数量、状态和主要动作一致。
- lint、完整测试和生产构建通过。
- 发布后逐页检查主标题、首要动作、折叠内容和导航。

落实情况：

- 使用 Node 24.19.0 完成静态检查、生产构建和 414 项全量自动测试；两项新增视觉问题均补入源码级回归断言。
- 真实来源检查中 Crossref、OpenAlex、DataCite 与 arXiv 正常；Semantic Scholar 返回 429 时保持“来源降级”，未误报为“没有论文”。
- 信息论真实基准达到 recall@K 1、precision@10 0.90、错误类型率 0、主题覆盖率 0.75；应用数学达到 recall@K 1、precision@10 0.80、错误类型率 0、主题覆盖率 1，均通过发布门槛。
- 在 1365×900 与 390×844 下逐页检查今日、路线总览、路线工作区、论文库、论文详情、研究记忆、学习路径和公开演示；修复研究记忆导入卡重复文案和论文详情窄屏横向溢出。
- 桌面与窄屏核对结果一致：今日 1 篇、候选/筛选/深评/确认 295/58/12/1；路线 8 条、待补 2 条、536 候选/454 评估中/1 今日/0 正式证据；论文库 1/0/0/1291。
- 所有改动只调整信息层级与呈现；研究状态、历史论文、反馈、阅读记录、质量门槛和共享队列语义未删改。

计划调整：

- 视觉验收发现导入卡在空画像时重复显示同一句说明，改为“建立你的研究画像”加一句功能说明。
- 视觉验收发现论文详情顶部来源标签在 390px 视口溢出，改为窄屏换行；复核后页面宽度与视口一致。

### 阶段 8：学习路径查询故障 — 已完成

验收标准：

- 复现 `p.canonical_id` 错误，区分字段缺失与 SQL 兼容性，不凭错误文字新增迁移。
- 使用真实 SQL 执行测试覆盖路线、全空间、跨方向和质量门槛。
- 不修改、清空或重新创建已有研究数据库。

落实情况：

- 复核 `main@d4e821f`，仅有用户原有未跟踪 `docs/`，未改动。
- Node 24.19.0 基线 lint、生产构建及 414 项测试通过；npm 子进程落到系统 Node 20，采用 Node 24 直接运行等价构建和测试。
- 已确认本地 `research_track_papers` 和 `monitored_papers` 均有 `canonical_id`，上轮“本地旧库缺字段”的判断没有依据，予以更正。
- 在临时 Worker/D1 中复现原查询 HTTP 500：子查询 `ORDER BY` 引用外层 `p.canonical_id` 不兼容；Node SQLite 3.53.3 接受相同语法，所以原有测试没有暴露错误。
- 改为依次按 canonical ID、DOI、标题查找，不再在子查询排序中引用外层字段；匹配只限本空间，多个同类匹配按 ID 确定顺序。
- 新增真正执行生产查询的 D1 回归测试，修复前 4 项失败，修复后通过；学习路径相关 20 项测试全部通过。
- 验证待评估、已忽略、已停用和其他空间论文不进入路线学习材料；重复读取不删除论文、反馈或阅读笔记。无数据库迁移，无历史数据写入。

### 阶段 9：前端组件边界整理 — 已完成

- 先拆出路线版本相关独立组件，不改变交互、文案、状态、计数和事件处理。
- 保留现有 CSS 层叠顺序；本轮不同时进行全局样式迁移，以免混合风险。
- 更新原有测试的组件定位，并补渲染或结构等价验证。

落实情况：

- 将 `RouteEvolutionWorkbench` 提取到独立文件；数据、提交动作与日期格式仍由主应用传入，组件不自行请求 API 或持久化数据。
- 逐字核对提取前后的渲染函数主体一致，未改 JSX、文案、计数或 CSS；主文件减少约 68 行独立组件代码。
- 补充真实 React 渲染和回调测试，验证中英文、论文与推断分层、历史版本、确认/驳回参数、执行中禁用和无证据禁用。
- 更新旧源码契约测试的定位，相关 31 项测试通过。

### 阶段 10：回归与发布 — 已完成

- 完成 lint、完整测试、生产构建及相关运行时验证。
- 以最终源代码重新构建后发布，保证打包内容与提交一致。
- 发布后检查服务状态，明确区分已验证项与未验证项。

落实情况：

- 最终业务代码生产构建通过，完整 421 项测试通过（含新增 4 项 D1 执行测试和 3 项组件测试）。
- 本地完整 API 在新建测试身份的信息论、应用数学空间均返回 HTTP 200，空空间为 `path:null`、可用论文 0、待评估 0；不是完整模型扫描测试，不冒充有真实推荐产出。
- `test:live` 在网络权限/代理恢复后通过：Crossref、OpenAlex、DataCite 正常，Semantic Scholar 与 arXiv 返回 429，按可选来源降级处理。
- 最终 lint、离线与实时发现基准通过。实时信息论 recall@K 1 / precision@10 0.90 / 错误类型 0 / 主题覆盖 0.75；应用数学 1 / 0.80 / 0 / 1，OpenAlex 与 Crossref 均无 provider error。
- 未改 CSS，未新增浏览器视觉验收；组件渲染、业务动作和两种语言已自动验证。
- v179 已发布成功（业务提交 `d0a9edf29712c8b32c77c001655ddae9dfea76e9`）。生产首页、公开演示 HTTP 200；独立测试身份的信息论与应用数学学习路径接口均 HTTP 200，空路径及 0 计数正确。
- 本次没有触发模型扫描，也没有读取用户模型 Key；非空论文筛选、身份匹配、历史保留由临时 D1 夹具验证，不能据此声称完成了真实用户的全流程学习验收。

### 阶段 11：非空学习路径与阅读闭环 — 已完成

验收标准：

- 只读检查线上已有路径；阅读、阶段完成等验收写入使用隔离测试数据，不替用户表达学术判断。
- 从学习材料能进入 Pi 的论文阅读工作台，并返回原路径；外部原文链接继续保留。
- 阅读状态保存失败或切换空间时，不显示虚假的成功状态，不污染其他空间。
- 自动与手动阶段完成都提供一次性路线学习信号，不自动伪造论文阅读或重复加权。

落实情况：

- 基线 `main@11b1726`：lint、生产构建、421 项测试通过，仅原有未跟踪 `docs/` 保留。
- 线上只读记录：信息论路径有 4 篇保存材料，基础阶段缺证据，状态 `waiting_evidence`；应用数学路径有 4 篇保存材料、1 个空阶段，状态 `active`。当前 `paper_reading_progress` 没有记录，尚无真实阅读闭环完成证据。
- 发现学习材料只链接外部原文，不能顺接 Pi 的笔记与阅读状态；自动掌握推进阶段缺少手动完成具有的路线学习信号，开始修复。
- 应用数学“Eldan 随机局部化与薄壳/KLS 界”阶段挂载了题名为一般加权 Sobolev 不等式的论文，阶段内容匹配尚未通过验收；不以论文存在代替内容正确。
- 学习材料增加“阅读与笔记”入口，进入 Pi 论文详情后可返回学习路径；独立原文链接保留。旧资源没有准确论文 ID 时不按标题猜测打开记录。
- 论文库支持按本空间的精确论文 ID 定位；隔离 D1 中加入 2,001 条较新候选后，旧学习论文及笔记仍可访问，2,002 条历史记录均保留。
- 阅读状态等待持久化成功后再更新界面；保存期间避免并发写入，切换空间后的旧响应不污染当前页面。保存成功会刷新学习路径。
- 自动掌握和手动完成共用一次性路线学习信号；重复读取、恢复再完成不重复加权，也不替用户确认正式路线证据。手动恢复的阶段不再立刻被掌握状态重新完成。
- 新增 12 项测试，包含完整生产 Worker + 临时 D1 的真实 API 执行，以及中英文组件渲染、回调、保存失败和跨空间异步响应。两组公开论文元数据来自线上信息论/应用数学路径，但所有阅读写入均为隔离夹具，不是用户真实阅读或新的 LLM 扫描。
- 最终生产构建与 433 项完整测试通过。本地首页 HTTP 200；未进行新的浏览器截图/窄屏视觉验收，布局共享同一材料列表，不能据此宣称所有视觉问题已消除。
- 离线与实时发现基准通过：信息论 recall@K 0.80 / precision@10 1 / 错误类型率 0 / 主题覆盖 1；应用数学 1 / 1 / 0 / 1。实时基准时 OpenAlex 每个方向 9 次来源错误，Crossref 保持可用。
- `test:live` 首次失败：仅 Crossref、DataCite 健康，OpenAlex/Semantic Scholar 429、arXiv 超时；一次有限重试后 4 个来源健康，仅 Semantic Scholar 429，检查通过。保留初次失败，不降低门槛。
- 最终 lint 通过；按提交 `857f9e67e1c2b8d5fea35a69c2ec773add560d11` 重新构建并再次通过 433 项测试后，v180 于 2026-09-04 03:12 UTC 发布成功。生产首页、演示页 HTTP 200；隔离身份的不存在空间请求返回预期 404，不泄露其他空间数据。

### 阶段 12：学习阶段的材料匹配 — 已完成

- 复核阶段说明与所挂论文的依据，自动补齐不能只靠论文类型和总质量分；保留历史记录，不把不匹配论文描述成指定经典或里程碑。
- 这是本轮只读验收发现的下一优先级，先于继续拆页面或修改 CSS。

落实情况：

- `main@b005efb` 基线 lint、构建、433 项测试通过，原有 `docs/` 不变。
- 根因不是提示词措辞：五年窗口被当作 foundation、阶段补齐只按角色/总分排序、模型留空后又被目标路线最低配额强制填入。
- 开始增加独立阶段匹配检查：使用题名、作者、摘要及模型提供的可核对依据；质量分、年份、引用量均不独立证明经典或里程碑地位。
- 不匹配或依据不足的旧材料保留为补充阅读，不删除原始路径资源和阅读记录；阶段缺口继续检索。
- 去掉基于扫描时间窗的基础文献分类和模型返回后的强制路线配额。模型分配需要可在题名/摘要中核对的 primary 依据；自动补齐按具体阶段检查，不依赖总分凑数。现有明确路线角色仍需匹配阶段主题，综述不能替代指定原始突破。
- 匹配依据绑定阶段内容与论文元数据；内容或策略变化会允许重新生成，不再复用旧版本。开发无上限模式同样适用于路径模型调用，未新增模型预算。
- 不足材料在当前阶段折叠为“补充阅读（不计入本阶段）”，保留站内阅读和原文入口；缺口查询携带具体阶段主题，并保留已有更精确的作者/年份查询。无新增数据库迁移。
- 新增 8 项测试，完整 441 项测试、lint 与生产构建通过。包含真实 Worker/D1 执行：题名不匹配 → 缺口与补充阅读 → 禁止完成；匹配依据持久化后可成为主材料；重新生成保留旧路径、材料与历史完成记录。
- 信息论/应用数学阅读闭环继续在隔离 D1 通过。模型输出解析使用受控响应测试，未读取用户 Key、未替用户触发真实模型重建或完成阅读；不能把这些测试写成真实推荐产出。
- `test:live` 4 个来源健康，Semantic Scholar 429 优雅降级；离线及实时发现基准通过。实时信息论 recall@K 1 / precision@10 0.90 / 错误类型率 0 / 主题覆盖 0.75；应用数学 1 / 0.80 / 0 / 1，OpenAlex/Crossref 无来源错误。
- v181 已于 2026-09-04 03:39 UTC 发布成功，业务提交 `959eca0c4cf9fc3feae20e2913ceac378272d955`。生产首页/演示页 HTTP 200，隔离身份访问不存在学习空间返回预期 404；未新建用户路径或操作用户阅读记录。未新增浏览器/窄屏视觉验收。

### 阶段 13：真实学习材料的语义验收 — 执行中

- 在网页已配置模型的前提下，对信息论、应用数学分别完成受控路径重建，核对模型的主要材料、补充材料和待补经典清单，不替用户确认阅读或正式路线证据。
- 当前确定性检查是保守的主题/角色防护，不是完整学术语义判断。真实同义表述可能被暂留补充阅读，模型的 primary 判断也仍需用真实样本验收。
- 核查指定经典、原始突破与后续综述的区别；重点看缺口是否真的找到正确文献，并通过共享质量队列补入路径，而不是继续调整页面排版。

落实情况：

- `main@ff87a7c` 基线 lint、生产构建、441 项测试通过；用户原有 `docs/` 未改动。
- 在已连接模型的网页触发应用数学 QA 空间学习路径生成（5 篇可用论文）。页面长时间停留在“正在生成”，尚未取得可验收的模型结果，未重复提交或写入阅读状态。
- 代码复核发现模型请求没有超时边界，且相同证据版本会直接复用降级路径，连接恢复也不能重试；先补请求恢复与明确状态，再继续双领域语义验收。
- 13A 实现完成：服务端模型请求设 240 秒技术超时，网页设 270 秒等待边界，不新增每日调用/费用上限；失败重新规划返回可重试错误，不创建替代版本或覆盖旧路径。首建降级框架明确标为模型规划未完成，允许再次规划；成功提示按实际主要材料去重计数，不拿候选池数量冒充挂入论文数。
- 新增 3 项自动测试，完整 444 项测试、lint、生产构建通过；真实 Worker/D1 覆盖同证据降级重试、失败不写新版本、成功模型缓存复用与跨目标失败保留。首次两次夹具失败为测试空间同名/缺链接，已修正，未更改业务门槛。
- `test:live` 4 个来源健康，Semantic Scholar 429；离线与实时信息论/应用数学发现基准均通过。该基准不等于学习路径语义验收。
- 应用数学真实模型最终成功，04:51:42 UTC 保存路径：5 篇可用材料中挂入 1 篇（Hit-and-Run Mixing via Localization Schemes），5 阶段中 4 个阶段缺证，路径 `waiting_evidence`；页面 7 篇评估中、0/5 完成。未触发任何阅读确认。挂入论文主题与作者由 [原始摘要](https://arxiv.org/abs/2212.00297) 复核，支持局部化混合时间这一前沿阶段。
- 13B 尚未通过：空的 Chen 里程碑阶段把改进描述为“从次多项式提升至多项式”，与 [原论文摘要](https://arxiv.org/abs/2011.13661) 的 almost constant 结论不符。材料匹配约束没有覆盖空阶段讲解；下一轮须增加讲解依据约束，并验证经典实际通过队列补入，不能仅修这一句文案。

计划调整：

- 将阶段 13 分为 13A「生成失败可恢复」和 13B「真实内容与经典补齐」。13A 的自动测试不能替代 13B；未取得真实生成结果前不关闭本阶段。
- 浏览器操作确认应用数学生成成功，先前长等待不能记为模型失败；另有浏览器控制读取缓慢，两者分别记录，不混淆。

本轮收口（2026-09-04）：

- **13A 已完成并发布 v182**，业务提交 `1cd7944d74f6b91f172aa5f9fcf479aefca433e7`，05:16:42 UTC 发布成功。首页/公开演示 HTTP 200，隔离不存在空间学习接口 HTTP 404。没有数据库迁移。
- 发布凭据直接放入命令参数的方式被安全检查拒绝；改用已获准的不回显标准输入与仅子进程内存环境传递，未写文件/Git 配置，也未输出凭据。旧输入辅助进程已停止。
- 信息论真实模型于 05:12:03 UTC 保存路径：10 篇可用、4 篇主要材料、基础阶段 0 篇、0/5 完成，`waiting_evidence`。页面显示 27 篇评估中；缺口为 Shannon / Gaussian maximum entropy，关联持久化发现任务。现有路径数由 3 增至 5，未删除旧路径。
- 信息论 4 篇的资源均保留来源与摘要摘录依据；其中 [高斯向量 RDP](https://arxiv.org/abs/2406.18008)、[边信息 RDP](https://arxiv.org/abs/2305.13116)、[多重功率约束](https://arxiv.org/abs/2511.14849) 的阶段主题已用原始摘要交叉核对。能量界材料尚未完成独立摘要复核；不以相近题名的旧版本代替正式发表版本。
- 信息论桌面与 390×844 窄屏的 10 篇可用 / 27 篇评估中 / 4 篇主要材料 / 0/5 完成一致；只做学习页功能与计数复核，不宣称新一轮全站视觉验收。未替用户标记已读、掌握或确认正式路线证据。
- 本轮是学习路径生成验收，并非新的完整每日扫描：没有记录新扫描完整候选→筛选→深评→推荐漏斗，不把学习池与挂入数写成新推荐数。两组经典均尚未补齐，13B 保持执行中。
- 验收期间旧版生产 `/api/monitor` 及内部健康查询出现 D1 调用 500/503（约 05:07 UTC），不是本轮新增迁移，日志未提供足够具体的 D1 错误原因；保留为独立可靠性风险，不将来源检查通过当作全站健康证明。

#### 13B-1：阶段讲解依据防护 — 已发布 v186（2026-09-04）

根因与实施：

- 材料的阶段匹配只证明“这篇可用于本阶段”，不能证明阶段讲解中每个结论正确。旧 API 原样返回十个中英文字段，即使主材料为 0；自动补入新论文后也会继续沿用旧讲解。
- 新生成的有材料阶段增加一次独立模型核对，输入仅为该阶段主要论文的题名、作者、摘要和待核对文字，不提供质量分作为证明，不取全文。明确核对上下界、改进方向、原始工作与后续讨论、开放与已解决问题。
- 核对必须返回 supported 和可逐字核对的摘要摘录；缺失、重复阶段结论、拒绝、来源不符或请求失败均不放行具体讲解。起草模型的自我批准无效；空阶段不为核对另发模型请求。
- 核对记录绑定所有阶段文字及当前主要来源，保存在已有材料 JSON 中，不新增数据库迁移。材料或讲解变更使记录失效；后续仅补到一篇论文不等于旧讲解被批准。
- 对外统一投影为“已核对讲解”或“阅读任务”；未核对阶段不展示旧学术结论。内部继续使用原始阶段目标检索/匹配，原始路径、材料、补充阅读、完成时间、阅读笔记均保留。生成与核对共享原有 240 秒技术边界，没有新增开发费用上限。
- 有材料但核对未完成时，生成结果明确提示“部分阶段讲解尚未完成”；有足够材料时允许同证据重试。不到模型规划最低材料数时仍复用原框架，避免无变化重复建版本。

验证与边界：

- `main@758aed9` 基线 lint、构建、455 项测试通过；原有未跟踪 `docs/` 未改动。
- 使用旧构建的真实 Worker/隔离 D1 重现新增测试失败；修复后完整 461 项测试通过（新增 6 项），含空阶段、质量门槛前后、旧内容保留、摘要变更失效、独立核对拒绝/失败及无材料不额外调用。
- 最后复核同证据重试条件后再次生产构建并运行完整 461 项测试，全部通过；这不是仅测试旧构建。只做本地 HTTP 可达检查，没有新增浏览器视觉验收，也没有真实模型重规划。
- 最终完整 lint 通过。v186 于 2026-09-04 07:05:39 UTC 发布成功，业务提交 `98c34fe32ef382dad6c8f6cfa04016e807e7c8f4`；GitHub 已同步。首页、演示页 HTTP 200；未携带登录身份的隔离学习请求为 401，停在身份校验，没有据此宣称生产学习内容已验收。未读取用户 Key 或 Cookie。
- 核对模型测试使用受控响应，不是新的真实 LLM 结果；摘要摘录存在也不等于数学推理已被形式化证明，独立模型仍可能判断错误。真实语义验证继续留在 13B-2，不宣称 13B 全部关闭。
- 重新只读核对生产：应用数学路径保存 1 篇主要材料、4 个空阶段、0/5 完成；信息论保存 4 篇主要材料、1 个空阶段、0/5 完成。两条路径均 waiting_evidence；这些是现存路径计数，不是本轮新推荐漏斗。
- 数学基础任务 `699d74e0-8bdd-41b3-8657-523e9d31df9e` 与信息论基础任务 `85b6e834-02dc-4a20-b5b1-690404e1e7c4` 均 pending / attempt_count 0 / queued_count 0。数学后续 Chen 阶段尚无独立任务，因为现有机制先处理第一个缺口。下一项复核调度资格/触发，不把零尝试说成来源失败；本轮没有替用户触发扫描或确认阅读。
- `test:live` 通过：Crossref、DataCite、arXiv 健康；OpenAlex、Semantic Scholar 429 降级。离线发现基准通过；实时信息论 recall@K 0.8 / precision@10 1 / wrongType 0 / coverage 1，应用数学 1 / 1 / 0 / 1；两个领域 OpenAlex 各 9 次错误、Crossref 0。未降低测试或推荐门槛。
- 优先级复核：13B-1 防护实现后，13B-2 的“任务真正执行 + 真实内容核对”仍为下一 P1；引用清单、逐页布局、错误文案清理不抢占。

### 阶段 14：功能标识与可信计数 — 已完成

按用户新增的两张截图反馈，先处理学习页空状态靶心、大蓝圆 π 及全站同类标识；阶段 13B 不提前关闭。

验收标准：

- 品牌沿用已有 Pi 图形；功能状态不再用 π 字符冒充图标，不新增图片依赖。
- 导航、空状态和处理中使用同网格、同笔画的功能图标；取消无意义大圆底，保留状态文字和恢复动作。
- 图标不独立承担状态语义；装饰图标对读屏隐藏，加载动效尊重减少动态效果设置。
- 空间切换只展示已有可信字段，不再按列表序号编造论文或路线数量。
- 新增渲染与交互回归，完成全量检查后发布；没有实际执行的浏览器验收不记为通过。

落实情况：

- 已复核两张截图对应的学习页 `.v2-learning-empty` / `.v2-learning-loading`；相同字符还用于导入、问答、路线和研究记忆。
- `main@52c602e` 基线 Node 24 下 lint、生产构建和 444 项测试通过；用户原有未跟踪 `docs/` 未改动。
- 开始统一功能图标，并去除空间弹窗的 `index + 3` / `17 + index * 5` 示例数量。
- **14A 已完成**：新增同一 24px 网格、1.65px 笔画的功能图标；侧栏的 01–05 改为日历、路线、阅读、论文库和笔记。学习页移除靶心与 52px 蓝圆 π，采用书本空状态和无底色加载图标。
- 路线加载/缺口/提案、导入、问答和研究记忆中的同类字符同步替换，标题前重复的 π 装饰删除。既有 Pi 品牌图片、空间身份、论文/推断的文字区分均保留。
- 空间切换只显示空间名、成员名和说明；不新增假计数，也不把当前空间的数量套给其他空间。真实今日与论文库导航计数不变。
- 新增 5 项渲染/交互回归，覆盖图标可访问性、中英文学习状态、错误重试、空间选择和导航真实计数；路线提案的 3 项渲染/回调测试继续通过。首次测试夹具缺 `exports` 已修正，非业务失败。
- 新图标从图谱大画布 SVG 选择器中排除，避免误放大；减少动态效果设置会停止图标动画。未进行新的截图或窄屏浏览器验收，不将渲染测试当作视觉验收。
- 生产构建和完整 449 项测试已通过；首次 lint 发现新增测试的正则空格风格问题，已修正并重跑。离线、实时发现基准通过；`test:live` 为 Crossref / DataCite / arXiv 健康，OpenAlex 与 Semantic Scholar HTTP 429 降级，检查通过。
- 14B 进入最终检查与发布；无数据库迁移，未读取用户模型 Key，未主动触发生产扫描或写入用户的论文/阅读状态。
- 最终 lint、生产构建与 449 项完整测试全部通过；本地更新后首页 HTTP 200。Sites 发布沿用当前公开权限及用户已授予的自主发布授权。
- **14B 已完成**：v183 于 2026-09-04 05:46:22 UTC 发布成功，业务提交 `cc1953d93940682e696e42d2a956aa1f3a8e1d1b`。生产首页、演示页 HTTP 200；隔离身份访问不存在学习空间为预期 404。复用同一个预览页交付线上学习页，未做新增截图/窄屏视觉验收。
- 这次仅完成标识体系与示例计数清理；阶段 13B 的空阶段学术表述及经典实际补齐、此前记录的生产 monitor/D1 风险仍未关闭。
- 本地预览按应用原有逻辑自动发起初始化/后台请求，停止预览时日志显示模型设置及部分 monitor/research-map 请求 502；本次没有专门诊断该本地模型连接，首页 200 不代表本地模型业务闭环通过。生产本轮检查限于上述只读请求。

计划调整：

- 本轮以截图问题及同类标识为边界，不同时重构全局 CSS、数据库或学习语义。
- 14A 完成图标/可信计数与自动回归，14B 完成发布；随后回到阶段 13B 的空阶段讲解依据和经典文献补齐。

### 阶段 15：直角标签与克制配色 — 15A 已完成，15B 待验收

参考与判断：

- [Linear 2026 界面调整](https://linear.app/now/behind-the-latest-design-refresh)：降低导航等辅助区域的视觉权重，减少饱和底色和重复边界。借鉴信息层级，不照搬其圆角。
- [Vercel Geist 标签](https://vercel.com/geist/badge)与[配色](https://vercel.com/geist/colors)：把背景、边界、文本色分别管理；标签只承载短元数据，操作仍用按钮。Vercel 本身也有 pill，本项目按用户偏好不采用胶囊形。
- Pi 的方向：白/浅灰工作面、墨色正文、一个蓝灰强调色；不再给基础/近期/前沿、稍后、作者、来源分别铺彩底。标签直角，筛选按钮小圆角，卡片只保留轻微圆角。
- 异常用深红文字，待补/重试用棕色文字和细边界；健康状态不铺大绿底。所有语义保留文字，不只靠颜色。图谱的已验证引用、相似、推断和阅读路径编码不纳入本轮去色。

15A 验收标准（本轮）：

- 今日、论文库、路线、学习、记忆及共用弹窗的主要元数据去胶囊化、去多色底；优先清理高频工作面。
- 采用集中主题变量及明确的组件范围，不全局给所有元素清色，不改图谱连线或隐藏警示。
- 演示/分享只同步基础底色和标签，不进行第二套布局重设计。
- 保留计数、筛选、焦点、禁用和恢复动作；新增样式契约、文本对比度和状态例外测试，完整检查通过再发布。

落实情况：

- 复核 `main@65fde06`，仅保留用户原有未跟踪 `docs/`；Node 24 下基线 lint、生产构建、449 项测试通过。
- 发现历史样式同时使用绿、蓝、橙、紫标签；大量 99/999px 胶囊和多层色底来自分散的组件规则。开始以共同语义统一，而非逐页再发明一套配色。
- 15A 实现完成：共用主题集中定义中性色、蓝灰强调色及警示例外；主要元数据直角化，来源/日期改为无底色文本，必读使用黑白强调。侧栏、主要工作面和学习/路线当前动作同步；图谱连线、节点编码未改。
- 演示和分享复用基础变量与直角标签，只同步视觉，不改变示例论文和页面布局。新增主题文件限定已有组件范围，不用全局清色、隐藏元素或不可见过滤器。
- 新增 5 项样式契约测试；与图标相关测试合计 10 项通过。覆盖七组前景/背景文字对比度 ≥4.5、警示例外、选中/焦点样式、无图谱编码更改、主题变量完整性。不是浏览器实际渲染验收。
- 当前进入 lint、完整测试与发布前检查；没有业务 API、数据库或模型策略改动。
- 最终 lint、生产构建、454 项完整测试通过。离线与实时发现基准通过：信息论 recall@K 0.8 / precision@10 1 / 错误类型 0 / 主题覆盖 1；应用数学 1 / 1 / 0 / 1。两领域 OpenAlex 各 9 次来源错误，Crossref 0；不将可用来源降级写成无论文。
- `test:live` 为 Crossref / DataCite / arXiv 健康，OpenAlex 和 Semantic Scholar HTTP 429，检查通过。发布前只读查看最近 15 分钟生产错误日志，返回 0 条；仅说明该窗口未返回错误，不能关闭 P0 历史风险或替代端到端扫描。
- 优先级复核：当前未取得阻断性故障复现，允许本轮纯前端发布；完成后 13B 仍是下一项 P1，15B 和重构不插队。
- 本地新增样式文件时，热更新保留了短暂的未找到模块错误，首页一度 500；重启预览后恢复 HTTP 200。生产构建已包含主题变量与样式，未通过修改业务代码掩盖该预览故障。
- **15A 已发布 v184**：2026-09-04 06:08:18 UTC 发布成功，业务提交 `07f82f07a7856367862e2977a1a2595e38f843d3`。线上首页/演示 HTTP 200，隔离身份访问不存在学习空间 HTTP 404；没有触发新的生产模型扫描或阅读确认。
- 优先级再次更新：15A 从待办移到完成；13B 保持下一项 P1，15B 保持 P2。现有共用规则不等于全部遗留样式已清理，七组色值对比度测试也不等于整站实际显示对比度验收。

15B（P2，未验收）：逐页桌面/窄屏任务可用性复核，然后处理视觉和旧样式归并。自动渲染和对比度测试不能替代该验收；不宣称整站视觉完成。

#### 15B-1：引用清单 — 已定位，待 13B 后实施

问题与证据（2026-09-04，用户提供截图）：

- 清单显示 3 条关系；来源 `Semantic Scholar` 挤入相邻年份，长标题只剩相似开头，用户难以辨认两篇论文。
- `CitationFlowWorkbench` 将来源文字嵌套在箭头内；`.v2-citation-ledger article` 中间列固定 `25px`，与来源文字长度不相容。窄屏规则旋转整个 `span`，连来源文字一起旋转。这是布局约束缺陷，不是仅换颜色能解决的问题。
- 页头“知识流向 / 后续论文”要求用户先理解专门术语；截图存在 2023 → 2022 的年份排列，容易误解为方向错误。当前代码按被引论文与引用方映射，尚未复核这条真实引用，不能仅按发表年份判定或翻转关系。

实施边界与验收条件：

- 主任务是认出“哪篇论文引用了哪篇”，并能分别打开两端；优先考虑显式标注“被引论文 / 引用它的论文”，不把数据库引用表述成已证明的思想传承或推荐阅读顺序。
- 来源独立放在关系的次级信息行，不与箭头、年份争用列宽；窄屏正常阅读，不旋转文字。
- 标题允许必要换行；若仍折叠，必须有可点击/键盘操作的完整标题入口，不依赖鼠标悬浮。年份只作元数据，不负责推断方向。
- 用紧凑分隔行替代重复大边框卡片，沿用中性色与直角标签；不新增标签和说明段来解释已有说明。
- 验证桌面、390px 窄屏和 200% 文字放大：长题名、中英文、长来源不重叠；两端打开正确论文并可返回；键盘焦点清楚；同一数据的引用计数一致。
- 自动测试覆盖两端 ID/来源/计数，浏览器验收覆盖实际排版和点击；未执行的项目不得打勾。数据库引用、相似、Pi 推断和阅读顺序不合并，不改历史数据。

本轮落实与优先级复核：

- 仅更新计划并定位截图问题，没有临时修改 CSS 或抢先发布；线上仍为 v184。
- 13B 保持下一项 P1；引用清单排在其后、15B 其他页面清理之前。被延后的是泛化美化与组件重构，不延后内容可信度修复。
- 复核 13B 代码：材料已有阶段匹配检查，但 `readPath` 仍直接返回保存的阶段讲解；已有空阶段错误尚未修复。下个实施批次先处理这条通用依据约束，再做经典补齐的真实验收，不只替换一处措辞。
- 仓库仍为 `main@09a792f`，原有未跟踪 `docs/` 保留；未操作生产扫描、用户模型配置或阅读记录。
- 本轮基线静态检查、生产构建和 454 项测试通过；npm 启动包装仍落到系统 Node 20 并出现权限错误，改用提供的 Node 24 直接运行等价命令，未修改业务代码。以上不是截图问题已经修复的证据，也不是新的浏览器验收。

15B-2 验收顺序：每页先验证“主要动作能找到 → 结果/异常能理解 → 返回与恢复不中断 → 计数一致 → 排版清晰”，最后检查视觉一致性。不要用截图整洁代替完整任务可完成。

计划调整：

- 15A 是用户当前明确要求的限域批次；13B 保持 P1，15A 完成后优先回到内容可靠性，不自动开始新页面美化。
- 15B 属于验收债务，记录实际范围；若发现遮挡、无法操作或状态不可辨，升级为本轮阻断问题。

### 阶段 15C：今日简报浅底浅字回归 — 已完成

优先级依据与边界：

- 用户明确要求“先改一下这里”；截图显示主信息难读，属于 P1 可读性回归，不作为扩大美化范围的理由。完成后恢复 13B/P1 → 15B-1/P2 → 15B-2/P2 → P3 的顺序。
- 根因是 v184 共用主题把 `.v2-daily-brief-lead` 变为白底，组件仍保留原深蓝渐变背景对应的白色/浅蓝字。`h2` 继承白字，次要分享按钮也为白字白边；上轮只测主题色值，未测组件是否实际使用它们。
- 直接修正该组件已有规则，不再追加一层互相竞争的覆盖；背景与前景成对使用主题变量。标题/正文为墨色，日期/来源为中灰，主动作蓝灰，分享动作蓝灰字，过期状态保留棕色语义。
- 右侧同一简报内的出版信息和“继续观察”统一浅灰中性色及直角/小圆角，不调整布局、文案、论文排序、计数或业务状态。

落实情况：

- Node 24 基线 lint、构建及 454 项测试通过；原有未跟踪 `docs/` 和上一轮计划修改保留。
- 新增组件级 CSS 契约：检查简报的实际样式声明及 8 组文本/背景对比度，包括标题、日期、正文、过期提示与分享按钮；修复前失败，修复后 6 项主题测试全部通过。它不等于浏览器计算样式或全站视觉验收。
- 本地预览 HTTP 200；按 Sites 现有项目流程复用预览，未新增截图或窄屏浏览器测试。
- 13B 再次定位到通用缺口：`buildDraft` 对材料 ID/阶段匹配有约束，却直接采信阶段讲解；`readPath` 又原样返回保存的讲解。修复需同时覆盖新生成、旧路径读取与补齐后解释，不可只改提示词或藏掉一条错误句子。该功能修复不混入颜色热修复，本轮未记为完成。
- 最终 lint、生产构建与 455 项完整测试通过，包含新增的简报组件配色回归。未改业务 API、模型参数、凭据或数据库。
- 预览中的模型校验多次返回 HTTP 502；无用户凭据的隔离本地请求返回 HTTP 200 / configured:false。本地与线上凭据按网站隔离，以上证据不能证明线上 Key 失效。前端 `isModelCredentialFailure` 同时接收认证失败和余额不足，顶部统一显示“失效”，已登记错误归因问题；未读取用户 Key 或 Cookie。
- v185 于 2026-09-04 06:36:19 UTC 发布成功，业务提交 `fb76eb49b66dd7724d725b7b7abd7e4ef69c87f5`。只发布上述颜色及回归测试，未修改模型连接；交付切回线上“今日”。阶段 13B、引用清单及逐页任务验收仍未关闭。
- 发布后首页和演示页 HTTP 200，隔离不存在空间的学习接口 HTTP 404；这些只读检查不代表用户模型认证已恢复。没有触发新的生产模型扫描或阅读确认。

#### 13B-2A：学习缺口访问内续跑 — 已发布 v187（2026-09-04）

- 基线 `main@902f690`：Node 24.19.0，lint、生产构建、461 项测试通过；未跟踪 `docs/` 保留。
- 生产两个基础任务仍 pending、attempt_count 0；两个空间最近活动在 7 天内且 automation_paused_at 均为空。不是长期不活跃待机，也没有来源请求失败证据。
- 208 条调度记录的最新外部唤醒为 05:06 UTC；GitHub 实际两次计划运行相隔约 4 小时 42 分钟。其后 05:46、06:14、07:06 的访问兜底均 started_count 0。现有代码在路线分析/演进/重建任何一项 attempted 后跳过 gap；学习页仅加载 GET，不能保证当前缺口获得执行机会。不能把工作流配置的半小时频率当成实际保证。
- 本批次限域：学习页在当前路径有到期缺口时自动请求续跑；服务端从归属校验后的当前路径解析任务，不接受任意外部任务 ID；复用缺口租约、重试时间和研究路线发现→共享质量队列，材料只在质量通过后挂入。路径切换、并发、失败和已有历史均补自动测试。
- 优先级复核：13B-2A 属 P1 用户主流程修复，先于 P2 引用清单。后台跨任务公平调度与持续真实语义验收仍待闭环，本批次不借机重构整个扫描器或降低质量门槛。
- 落实：新增当前路径 `advance-evidence` 操作及学习页自动续跑；后台与页面共用同一缺口租约，过期 running 可恢复，重试尊重来源退避。新候选唤醒今日既有共享质量流水线。未新增按钮、审核确认或质量审计页面；未读取模型 Key、未新增迁移。
- 测试：真实构建 Worker/隔离 D1 的双领域用例从 pending/0 次到实际来源调用和 `research-route:learning` 入队；断言推荐数 0、正式路线节点 0、原路径版本数仍 1。来源是确定性夹具，不是真实生产推荐。另验证并发只领取一次、跨空间/旧路径拒绝、部分失败/网络异常保留历史入队数、来源退避、超时租约恢复，以及生产客户端 effect 的隐藏页停跑、切换后不回写、生成期间不启动、失败保留页面和新候选唤醒。
- 生产浏览器验证受阻：读取现有线上标签页连续超时，未读取 Cookie 或绕过页面身份。可确认公开页面可达，不据此宣称两个真实任务已执行或原始文献已补入；路线级资格与具体候选的生产验收仍须续查。
- 本批完整 lint、构建及 465 项测试通过（较基线新增 4 项），来源/离线和实时发现基准通过；Crossref、DataCite、arXiv 健康，OpenAlex/Semantic Scholar 429。进入发布收尾，真实补齐未记为完成。
- v187 于 2026-09-04 07:32:10 UTC 发布成功，业务提交 `5437a63986d4c4b0b701f10e1a5488bb9722f96c` 已同步 GitHub。首页和演示 HTTP 200，未认证学习请求 401；现有学习页交付链接已更新。本批未改正式推荐/路线证据门槛，也未提交原有 `docs/`。

#### 13B-2B：访问兜底的任务轮换 — 已发布 v188

- 生产证据：信息论基础任务在 07:44:17 UTC 完成首次检索；Crossref 三组共返回 13 条候选记录（非去重论文数），入队 2 篇。07:46 的路径仍 waiting_evidence、基础材料 0，旧四篇材料仍在、阅读完成数仍 0；数学基础任务仍 pending/0 次，原五阶段/一篇材料保留。不能把检索 ready 当作材料补齐成功。
- 代码证据：访问兜底固定执行路线分析 → 演进 → 重建 → 缺口，任一 attempted 又跳过今日扫描；持续存在前置任务时，后面的类型没有执行机会。该缺陷影响后台主流程，列 P1；不声称已证明数学这条任务的全部阻断原因。
- 实施边界：仅访问兜底按已持久化的实际触发次数轮换六类任务（缺口发现、过期缺口恢复、路线分析、路线演进、路线重建、今日扫描），跳过无可领任务；每次最多一类重任务。不以墙钟取模，避免不规则触发反复命中同一类。继续复用任务租约、暂停/活跃资格和来源退避；普通 cron/watchdog 批次不扩容。
- 验收：自动执行实际 sweep，覆盖连续六次都有任务、空类别跳过、抛错后下次轮换、未获租约不消耗位置、访问不并发叠加重任务及原定时批次。无迁移，不改质量门槛和历史。成本/风险为小范围 Worker 调度变更，须完整回归再发布。
- 浏览器自动化读取现有页（含替代 DOM/截图及既有浏览器页）均超时；已按工具故障指引核对。未读取 Cookie/模型 Key，不伪造身份。生产桌面/窄屏可用性和双领域模型闭环保持未验收。
- 优先级：本项属于 13B/P1，P2 引用清单继续排在其后；没有增加装饰或新前端审计页面。
- 落实：使用现有调度记录的 `trigger_source` 统计访问次数，轮换六类任务；全程不新增迁移或修改任务自身的领锁/资格规则。定时批次仍先发现再选今日待办，避免漏掉本批新入队的论文。
- 自动验证：基线 465 项通过；新增 4 项行为测试，执行实际 Worker sweep 而非只测字符串。最终 lint、构建及 469 项完整测试通过；旧固定优先级断言已替换为轮换契约。两领域离线、实时发现基准均通过。
- 来源检查：Crossref、DataCite、arXiv 健康；OpenAlex HTTP 429，Semantic Scholar 超时，按降级通过。以上基准不等同于两条生产学习路径的深评/推荐漏斗。
- 本地预览 HTTP 200；浏览器交付返回 queued，未据此宣称画面/窄屏验收成功。生产模型 Key、用户阅读确认及正式路线证据均未由本轮操作。
- 发布落实：v188 于 2026-09-04 07:59:10 UTC 成功发布，业务提交 `edd4c81266741b1791954d8819ab8346f3ce7664` 已同步 GitHub。生产首页/演示 HTTP 200，未登录学习接口 HTTP 401。已有浏览器页交付仍返回 queued，不能将它视为实页验收。
- 未关闭风险：不保证外部定时唤醒的实际间隔；类型轮换不等于每条任务已通过资格校验和完成质量评估。当前 2 篇候选的筛选/深评/正式推荐与今日可见性未取得独立生产证据，数学补齐仍待验证；未执行跨重扫全论文库及桌面/窄屏计数复核。继续 13B/P1，不提前开始 P2 视觉清理。
- 发布后真实访问调度记录：07:59:40–08:00:03 UTC 正常完成，failed_count 0；started_count 0 只表示本次没有启动今日扫描，不能推断整轮没有研究任务，也不能反推具体学习任务已经执行。该窗口未发生调度 SQL/空对象回归。
- 停止本地预览时，自动发起的模型验证/monitor 请求仍有 HTTP 502，属此前本地连接风险；本轮未操作凭据或将它解释为线上 Key 失效。临时预览已停止。

#### 13B-2C：学习候选的访问内质量续跑 — 已发布 v189；完整补齐未关闭

- 真实页面：信息论路径保留 4 篇、0/5 阶段完成，基础显示“候选正在质量评估”；今日同时显示“当前质量队列已完成”和 6 篇候选评估中，论文库 1068。这不是缺失模型 Key 的证据。
- 根因一：学习入队后唤醒今日用 trigger=visit；`shouldStartMonitorQualityQueueContinuation` 却只允许 scheduled。最近已完成扫描、无 carryover 的情况下，即使有新共享候选，visit 仍落入 CADENCE_MS 缓存返回。学习页后续只刷新缺口，没有在质量重试到期时再唤醒。
- 根因二：今日的完成提示读取尚未加载的 `researchMap.routePortfolio` 默认 0，而不是今日接口的候选状态。未读取路线页不等于质量队列为空。
- 实施边界：允许 visit 复用已有 quality_queue 路径；保留手动重扫、来源扫描时钟、持久化并发锁、资格与最终质量门槛。学习页仅在有未完成候选、monitor ready 且 nextRunAt 到期、无本页在途任务时续跑。完成提示依赖当前 monitor 返回的候选/待核对信息，未知时只说本轮扫描结束，不宣称整队列清空。
- 验收与风险：新增 admission、到期/在途/取消及完成提示回归，并跑完整测试；真实两空间补文与今日仍须继续核对。属于 P1 主流程阻断，优先于 P2 引用清单；不新增审计台、不扩大装饰、无迁移。
- 浏览器恢复方法：保留现有标签页，独立读取页面并给予足够响应时间，未提取 Cookie 或读取模型 Key。已确认数学目标是 `P1 应用数学路线恢复 0901`，不是另一个名为“应用数学”的空间；只访问了后者，未创建路径或写阅读确认。
- 落实：visit 已可接入既有 quality_queue 模式；学习页面在已入队候选的重试到期时再次唤醒，并防止在途重复启动。今日提示改用 monitor 自身的保存候选/核对积压，删除未加载路线计数造成的假完成。保留来源时钟、锁和门槛，无迁移。
- 自动验收：Node 24，lint、生产构建、471 项完整测试通过；新增到期/未到期/在途/完成路径守卫和状态语义测试，执行真实客户端 effect 验证旧候选可唤醒而不依赖新增数。离线与实时发现基准通过，来源检查 3 健康、OpenAlex 与 Semantic Scholar HTTP 429，按降级处理。
- 生产检索事实（发布前）：信息论基础任务尝试 1 次、入队 2 篇；数学目标任务于 08:17:12 UTC 完成首次检索，Crossref foundation/milestone/frontier 分别返回 4/2/14 条记录，实际入队 4 篇。这些记录跨查询未去重，不能当成 20 篇独立论文。两条基础阶段均仍 0 篇材料、0/5 完成，不冒充质量通过。
- 历史保留观察：信息论原 4 篇路径材料与论文库 1068 保留；数学原 1 篇路径材料保留，论文库由 801 增至 803；数学后台评估继续推进，可用论文 6→8、评估中 9。此处为不同时间快照，不混成同一次检索的筛选/深评/推荐漏斗。尚未取得新基础候选逐篇质量通过的生产证据。
- 发布：v189 于 2026-09-04 08:30:27 UTC 成功，业务提交 `31b318e155ed9be0ed5d40ce3fb47cea61e5c21b` 已同步 GitHub。首页、演示 HTTP 200，未登录学习请求 401；保留同一线上标签页。PowerShell 首次健康检查出现本地 TLS 错误，改用项目 Node 24 的正常代理请求后确认上述状态，未禁用证书验证。
- 双领域实页：数学今日已保存简报为 360 候选 / 59 筛选 / 11 深评 / 3 正式入选；续跑独立显示 4→5 篇深评已保存（目标 8），主序 3 + 备选 3 = 导航 6。信息论已保存简报仍为 320 / 59 / 14 / 1，主序 1 + 备选 5 = 导航 6；访问后核对任务从保存点继续，1/4 已核对、第 14 次尝试，待处理 16 并未停止服务。这些均不是基础缺口那 2/4 篇的专属漏斗。
- 窄屏复核：真实浏览器 1440×1000 与 390×844，两领域学习页分别一致显示信息论 10 可用 / 29 评估中 / 路径 4 / 完成 0/5；数学 8 / 9 / 1 / 0/5。基础均“候选正在质量评估 · 0 篇”，完成按钮禁用，截图未见横向溢出或遮挡。已恢复用户原窗口尺寸；不是全站窄屏验收。
- 发布后 D1 两条原路径仍为 waiting_evidence（信息论 08:37:00、数学 08:33:08 更新）；没有重建路径、确认阅读或纳入正式路线证据。有限页面计数观察不能替代全库逐条跨重扫保留验证。
- 未关闭项与下一 P1：两个基础任务虽已入队，但新候选独立的筛选/深评/拒绝或通过结果尚未取得，材料仍 0。信息论旧核对多次续跑说明需核对新候选队列位置与旧任务 carryover 的竞争，不预先认定再次来源不可用或模型 Key 失效。继续逐篇资格与学术语义验收，不能以降低门槛或自动确认收尾。
- 清理范围：停止本轮本地预览，仅清理本轮临时发布包；原未跟踪 `docs/` 不提交、不删除。

### 后续优先项

#### 13B-2D：旧核对与新候选之间的公平接续 — 已发布 v190，逐篇材料验收未关闭

- 证据：信息论两篇最近入库论文仍为“发现线索”；旧核对已多次续跑。代码在 qualityCarryover 时不读取共享候选，直接回到旧 verification checkpoint，现有候选老化排序根本不会执行。不能把旧任务一直运行解释成新基础候选正在深评。
- P1 限域方案：只在前一批已完成、兼容当前流水线、仅有核对 carryover（不含未生成完的深评草稿）、至少已尝试续跑一次，且有旧批次之外的路线/缺口/学习候选等待满现有 30 分钟阈值时，切入既有 quality_queue 通道。旧任务、论文、筛选与核对草稿保持持久化；仍使用原公平选择器和质量门槛，不开第二条流水线，也不抢占在途租约。
- 验收：新增策略边界及真实构建 Worker / 隔离 D1 回归，验证旧工作队列原样保留、新候选获得筛选位置、旧核对复用已保存草稿、来源时钟不被移动、并发启动去重。真实双领域结果与未关闭限制另记；本项仍先于 P2 引用清单。
- 落实：已完成的核对重试遇到等待满 30 分钟且未进入旧批次的学习/缺口/路线候选时，切入既有 quality_queue；不抢占正在进行的任务，不重建路径，不自动确认阅读或路线证据。未生成完的深评草稿仍走原恢复分支，不能将本项说成所有重试竞争均已解决。没有 D1 迁移或新用户审计页面。
- 自动证据：基线 471 项、最终 473 项测试通过，lint 与生产构建通过。新增实际 Worker / 隔离 D1 用例并发启动只创建一批，新候选进入真实筛选队列，旧筛选结果复用；旧 work_queue_json、paper_insights、反馈和阅读记录逐字保留，来源扫描时钟不变，全程外部请求 0。另覆盖手动/在途/旧版本/首次尝试/未满时限/已有候选及未完成草稿不切换。
- 检索基准：离线两领域全部过线；实时信息论召回 0.8、数学 1.0，两者 precision@10 1、错误类型率 0、领域覆盖 1。Crossref 正常，OpenAlex 9 次错误/领域仍降级；来源契约检查 Crossref/DataCite/arXiv 健康，OpenAlex/Semantic Scholar 429。一次未启用本地环境代理的复测出现两源全失败，启用 Node 24 正常环境代理后通过，未修改业务代码或关闭证书验证。首次基线构建与测试并发曾造成 dist 读取错误，顺序重跑 471 项全通过，不算业务回归。
- 发布前真实观察：信息论论文库仍 1068，两篇最近发现的最大熵论文仍是“发现线索”、未完成质量评审；当前普通页面来源不足以独立证明它们就是基础缺口那两篇，不能混同。数学论文库仍 803；保存的简报 360/59/11/3 与当前 3/4 篇核对进度属于不同批次。上述不是新基础候选的专属深评漏斗。
- 发布落实：v190 于 2026-09-04 09:06:56 UTC 成功，业务提交 `ea459f85667881a9ac4ff4c8dc062f00d2606614` 已同步 GitHub。首页和演示 HTTP 200，未登录学习接口 401，未更改访问权限。现有线上标签页已更新，没有创建演示/研究空间副本。
- 发布后真实观察：数学原路径由 1 篇材料增至 2 篇（核心方法 1、前沿 1），可用论文从 11→12→14，基础仍 0、完成 0/5；D1 出现新活动任务 `5c63c584-b271-49d7-aff9-28a20ac660a7`，scanned_count 273，来源时钟仍为 09-03 11:49:08。信息论页面当前批次 360 候选、47/47 筛选已保存、核对 0/4；保留的简报仍 320/59/14/1。两空间历史库分别 803/1068。以上支持流程继续推进，但没有独立证明新活动任务必由本次 handoff 分支触发。
- 逐篇未关闭：发布后信息论两篇最近最大熵论文仍为“发现线索”；数学最新两篇为 Large deviations, moderate deviations, and the KLS conjecture 与 An Analog of the Poincaré Metric and Isoperimetric Constants，亦尚未完成质量评审。没有将这四条的普通页面状态误报成基础任务那 2/4 篇的完整漏斗。下一 P1 需追踪具体入队记录、筛选资格与论文身份/原始工作匹配；不可用提高自动推荐数代替原始文献补齐。
- 实页新问题按优先级入计划：窄屏空间选择弹窗被侧栏遮挡（15B-0/P2）。关闭侧栏后可正常切换；本轮只登记，不插入已验证发布的后台改动。近期生产错误窗口仅见一次取消的 spaces 请求及预期未认证 401，没有由此认定所有生产故障风险均已关闭。
- 本轮响应式复核：信息论 1440×1000 与 390×844 都显示可用 10、路径 4、基础 0、完成 0/5；队列计数在两次快照间从 26→25，后台正在评估，不能假装是同一时点的冻结比较。窄屏截图可读、无水平挤出；数学只复核了当前正常窄窗口，没有将上轮的完整宽窄复核算作本轮结果。已恢复原窗口。
- 收尾边界：无手动重扫、无接受/忽略/阅读确认、无正式路线证据确认；打开论文详情只留下正常查看记录。保留原未跟踪 docs/，本轮只清理临时发布包并停止本地预览。新候选进入真实筛选的确定性证据来自运行时测试；生产具体 handoff 命中及新基础候选最终通过仍未独立确认。

#### 13B-2E：原始文献身份与逐篇质量去向 — P1，实施中

- 输入证据：基础检索已执行、质量任务持续推进，但两个基础阶段仍 0。普通页面最近候选包含 2026 最大熵相关工作和 2021/2024 KLS/等周相关工作，不能只因标题相关就视为 Shannon 或 KLS 原始论文；也不能仅凭最新入库排序把它们对应到指定缺口任务。
- 执行顺序：先从现有持久化任务/来源记录解析确切论文 ID，再追踪每篇的候选→筛选→深评→核对→材料资格；若候选没有进入选择，按实际资格排除原因修复，而不是再无差别增加调度。若检索命中的是后续相关工作，定位查询与已知原始工作匹配/兜底逻辑；只用摘要和结构化证据，不引入全文或降低最终门槛。
- 验收：两个真实基础任务都有逐篇去向和明确缺失原因；合格材料自动挂入相应阶段，不合格/来源降级诚实保留待补状态；阅读确认仍由用户完成。补自动回归，并更新双领域与宽窄屏证据。此项仍排在 15B-0/P2 前；若线上核心接口故障复现则按 P0 抢占。
- 13B-2E-1 诊断落实：`main@19f57af` 基线 lint、构建、473 项测试通过。生产两个基础任务仍为 ready、尝试 1 次、入队 2/4 篇。代码确认基础检索下界为 1950（会排除 1948 年工作），经典精确检索也复用滚动年份窗口，且仅在路线冷启动第三次救援启用，学习缺口走通用三角色检索。现有数据库只读工具未提供按任务查询候选的能力，不能把最新入库论文冒充这两个任务的逐篇结果。
- 本轮边界：先修精确经典检索的年份误排、同一文献健康来源接续、学习缺口的入口和来源映射；缺失阶段以新查询版本补检，保留旧任务、候选和所有阅读记录。为新检索持久化有上限的候选身份，方便后续逐篇核实。已知文献锚点只用于检索，不授予推荐或阶段材料资格。随后验证双领域；不以检索成功关闭完整 13B-2E。

- 13B-2E-1 实施：基础通用检索不再以 1950 为下界；命名经典检索移除滚动年份窗口。新增 Shannon 1948 题名锚点，与 KLS 1995 原始工作一起核对返回题名、作者和年份。缺失基础/里程碑阶段生成精确查询，仅命名原始文献的学习任务启用精确分支，普通前沿/未收录主题仍走原检索。每篇最多接续两个来源；保留已命中记录与失败来源。学习来源保持 `research-route:learning` 和原任务 revision 映射，并在首条来源报告保留任务去重后的 `jobReviewCanonicalIds`（最多 24），不是推荐结果。
- 自动验收：新增 5 项执行实际查询构造/检索函数的回归；真实构建 Worker + D1 验证两领域新查询、旧 ready 任务保留、重复访问稳定、候选来源与任务对应、失败来源保留、推荐/正式路线/阅读不越权。原构建在新查询升级断言上失败，修复构建通过。完整测试 478 项通过；lint、构建、test:live、test:discovery、test:discovery:live 通过。在线来源中 Crossref、DataCite、arXiv 健康，OpenAlex/Semantic Scholar 429 如实降级。
- 发布前真实检索：Crossref 精确返回 Shannon 1948 记录 `10.1002/j.1538-7305.1948.tb01338.x`，摘要长度 0；KLS 单次探针遇到 429。论文身份可核实不等于具备足够摘要证据，不能据此批准材料。题名/日期参考 [Bell Labs 原始期刊记录](https://doi.org/10.1002/j.1538-7305.1948.tb01338.x)；不读取全文。
- v191 发布落实：2026-09-04 09:58:52 UTC 成功，源码 `70f028a2c249ecda96397c2270225084bc42ac09` 已同步。信息论生成新任务 `e9934cd0-3f43-46b0-b032-a4c3c1c67200`，旧 2 篇任务仍保留。首轮新任务入队 0，来源降级如实显示，没有提前关闭验收。
- 13B-2E-2 随后定位并修复：真实 Crossref 记录的纸本日期为 1948-07，上线日期为 2013-07-29；路线原归一化优先 online，导致严格身份核对误排。今日扫描也有同样实现，现共同使用首次有效出版日期，不把数字化上线当新论文。KLS 真实记录核对到 `10.1007/bf02574061`、1995 年、三位原作者，同样没有摘要。用混合日期元数据重跑旧构建，在“进入共享队列”处失败；修复后 lint、构建及 479 项测试通过。此问题延续 P1 原任务，不插入新的视觉工作。
- v192 发布落实：2026-09-04 10:16:14 UTC 成功，业务提交 `9119cc764b38c3e476e03fb84b15f5bf5238697e` 已同步 GitHub 和网站源码。首页/演示 HTTP 200，未认证学习接口 401，最近 15 分钟生产错误日志为空；保留公开访问配置。PowerShell HTTP 探针遇到本机 TLS 错误，改用 Node 24 的既有代理通路验证，未关闭证书校验。
- 生产原文身份与来源：信息论新任务 `e9934cd0-3f43-46b0-b032-a4c3c1c67200` 第 2 次在 10:21:52 UTC 命中并入队 2 条记录：`10.1002/j.1538-7305.1948.tb01338.x`、`10.1002/j.1538-7305.1948.tb00917.x`。数学新任务 `6786d1bf-1dec-42df-95c6-0414a41b4b7b` 命中 KLS `10.1007/bf02574061`，任务共享身份列表共 3 个（另有保护基线 `10.1002/mana.70237`、`10.1016/j.disc.2026.115188`）。数学任务完成时 `queued_count=0` 不是候选总数；随后原始论文实页明确显示“等待质量评审”，不能凭这个计数推断已评审或无论文。旧 2/4 篇检索任务仍保留。OpenAlex 冷却仍在，两个任务诚实保持 retryable。
- 真实漏斗边界：原始文献层面本轮核实了信息论 2 条、数学 1 条，尚未证实任何一条通过最终推荐与阶段材料核对。独立的正常今日批次仍在推进：信息论实页候选 360、已筛选 47、深评保存 11/14，本轮暂未新增、保留较早 1 篇主序；数学最近简报候选 360、筛选 59、深评 8、该批正式 3，日累计 9、主列表 6。以上是页面观察时的不同批次，不把它们归因为本轮原文检索，也不把较早 320/59/14/1 漏斗覆盖掉。
- 历史与响应式验收：信息论论文库 1070→1072，数学 803→804，原始 KLS 可按完整题名搜索且未加入阅读。信息论最终 1440×1000 与 390×844 均为可用 12、评估中 26、路径 4、基础 0、完成 0/5；数学成对宽窄检查均为可用 16、评估中 6、路径 2、基础 0、完成 0/5。正常后台评估让早先计数发生变化，不将不同时点差异误判为布局问题。本轮未手动重扫、未接受/忽略、未确认阅读或正式路线证据；只在隔离自动测试中验证这些状态不被覆盖。曾短暂进入主应用数学空间，随即切回既有 P1 数学验收空间，主空间的扫描数不混入上述漏斗。
- 13B-2E-3 下一 P1：沿以上确切 DOI 和任务来源继续核对摘要是否可补、候选筛选/深评与最终门槛的去向，区分“没有摘要”“来源冷却”“尚未评估”“质量不通过”“阶段不匹配”。历史 2/4 篇任务的完整逐篇映射仍未全部读取；不得宣称完整验收完成。不要重新泛化查询、清空旧任务、解除来源限流保护或降低最终门槛。13B 完成后依次处理 15B-0 操作遮挡、15B-1 引用清单、15B-3 累计/主序计数与重复进度提示的歧义，再回到其他视觉清理。

- 前端可信计数：空间切换示例数量已在阶段 14A 删除并补渲染/选择回归；若未来恢复跨空间计数，必须使用各空间独立的真实汇总数据。
- 2026-09-07 承接：13B-2E-3 保留全部验收要求，纳入上方阶段 18 / P1-C；先执行阶段 16 的可靠性复核与 CI，再修阶段 17A/B 的主要操作和状态问题。外部证据等待不再无限阻塞其他批次，不用“已入队/在运行”关闭材料验收。
- 页面提取与 CSS 归并纳入阶段 20 / P2-C，仅围绕实际修改渐进进行，保留层叠顺序和窄屏回归；不安排独立的大规模重构。

## 实施记录

- 2026-09-07：基于 `main@337cd4c` / v193 完成全局重评；更新 12 个功能面的工程成熟度、阶段 16–20 及验收门槛。将空间遮挡、模型误报和今日状态歧义提升为 P1-B；13B 承接为 P1-C，未关闭经典质量与语义验收。新增自然调度持续核对和 push/PR 确定性 CI 为 P1-A。仅更新计划，未实施新业务、触发扫描或发布。
- 2026-09-03：建立计划；完成阶段 0。
- 2026-09-03：完成阶段 1；阶段 2 开始。
- 2026-09-03：完成阶段 2；阶段 3 开始。
- 2026-09-03：完成阶段 3；阶段 4 开始。
- 2026-09-03：完成阶段 4；阶段 5 开始。
- 2026-09-03：完成阶段 5；阶段 6 开始。
- 2026-09-03：完成阶段 6；阶段 7 开始。
- 2026-09-03：完成阶段 7；进入提交、发布与线上复核。
- 2026-09-03：v178 发布成功；首页和公开演示 HTTP 200。学习路径本地报错未解决，不能将阶段 7 视为所有业务页面验收通过。
- 2026-09-04：补充阶段 8–10，优先处理学习路径故障，再做小范围组件整理。
- 2026-09-04：完成阶段 8；开始提取路线版本组件。
- 2026-09-04：完成阶段 9；进入完整回归与发布核验。
- 2026-09-04：阶段 10 完成；v179 上线并通过生产页面、空空间学习路径接口复核。保留原有 `docs/`；清理本轮临时发布包。
- 2026-09-04：完成阶段 11 的实现与隔离运行时验证；433 项测试通过，进入最终 lint、发布与生产只读复核。新增阶段 12，优先处理材料匹配风险。
- 2026-09-04：阶段 11 已发布为 v180；生产只读检查通过，未触发用户模型或写入用户阅读进度。下一阶段为学习材料与阶段目标匹配。
- 2026-09-04：阶段 12 的内容匹配防护与历史保留实现完成，441 项测试通过；进入发布，后续真实模型语义验收单列阶段 13。
- 2026-09-04：v181 发布与生产只读复核完成；阶段 12 关闭，下一步为阶段 13 的真实模型与经典补齐验收。
- 2026-09-04：阶段 13A 完成并发布 v182；双领域真实生成与学习页计数已核对。阶段 13B 因经典尚缺、空阶段讲解错误保持执行中，未提前关闭。
- 2026-09-04：按用户截图新增阶段 14；14A 图标统一与可信计数完成，449 项测试通过，进入 14B 发布。13B 的内容验收继续保留。
- 2026-09-04：阶段 14A–14B 完成，v183 发布并通过生产只读检查；下一步回到 13B 的内容依据与经典补齐，不扩大新的前端功能。
- 2026-09-04：按用户新增偏好重排优先级并完成 15A；v184 发布。454 项完整测试、lint、来源和发现基准通过，15B 视觉验收及 13B 内容可靠性仍未关闭。
- 2026-09-04：根据用户引用清单截图，将“顺手、少歧义优先于美化”写成全局验收原则；定位 25px 来源列、窄屏旋转和方向文案歧义，登记为 15B-1/P2，不抢占 13B/P1。本轮仅修订计划，未改应用或发布。
- 2026-09-04：按用户明确改序先处理 15C 今日简报可读性回归；局部修复与失败→通过的组件样式测试完成，进入完整回归和发布，随后恢复 13B 优先。
- 2026-09-04：15C 完成并发布 v185，455 项测试、lint、构建通过；新增本地模型状态归因问题待复核，未将它误报为线上凭据失效。下一实施项恢复 13B。
- 2026-09-04：用户确认当前模型连接没有问题，恢复计划并完成 13B-1，发布 v186。461 项完整测试、lint、构建及来源/发现基准通过；新增独立摘要核对和历史讲解投影保护。13B-2 仍为下一 P1，明确记录两个基础任务零尝试，不以守卫测试代替真实经典补齐。
- 2026-09-04：完成 13B-2A 并发布 v187，新增当前路径自动续跑并唤醒共享质量流水线；465 项测试通过。生产验证因现有浏览器连接超时未完成，保留双领域真实补齐、讲解语义和桌面/窄屏计数验收为 P1；不把 HTTP 200 或夹具入队结果当作真实论文补齐。
- 2026-09-04：完成 13B-2D 并发布 v190，473 项测试通过，旧核对不再独占已完成批次之后的新研究候选选择。生产双空间历史保留、学习阶段未假完成；基础原始材料仍 0。新增 13B-2E/P1 逐篇身份与质量资格追踪，以及 15B-0/P2 窄屏侧栏遮挡空间弹窗，按可用性优先规则排序，未提前关闭完整经典补齐。
- 2026-09-04：13B-2E-1/2 发布 v191–192，修复经典精确检索、学习来源映射和数字化日期误排，479 项测试通过；线上 Shannon 两条原刊记录与 KLS 原文已对应共享队列，基础材料仍为 0。下一 P1 为确切原文的摘要与逐篇质量验收；新增今日累计/主序计数歧义为 15B-3/P2。保留全部历史与未跟踪 docs/。
